

## 项目概述

**项目名称**：麻将消一消（Mahjong Match-3）

**类型**：单机休闲消除手游

**核心玩法**：经典"上海麻将"类二消——点击两张相同花色且左右不被挡住的牌即可消除，特殊牌含暗牌（需翻面再消除）和吐牌机（消耗次数补充新牌）。底层采用 6 列 × 8 行的棋盘，多层 3D 堆叠（奇数层半格偏移）。

**当前阶段**：MVP 早期，重点是**批量关卡生产工具链**——因为关卡数量需求大（数百关），手工设计不现实。

**目录结构**：
```
麻将消一消/
├── 麻将消一消_工作日志.md     ← 本文件
├── outputs/
│   ├── 关卡搭建工具包/         ← 自包含工具链（发压缩包给对方，含 README）
│   │   ├── 使用说明.md          工具说明（版本日期 2026-08-13 + 完整流程）
│   │   ├── 启动编辑器.bat      搭形状（网页编辑器）
│   │   ├── 批量生成与填色.bat  一键生成 + 填色（网页批量工具）
│   │   ├── serve_editor.js     本地服务器（纯 Node，同时服务两个网页）
│   │   ├── generate_fill_std.js  一键生成+填色脚本（模板名当参数，一步到位）
│   │   ├── 难度分说明.md        难度分计算原理 + 设计理由（独立文档）
│   │   ├── 批量工具.html       批量工具页面
│   │   ├── shape_editor_std.html 关卡编辑器页面（std 坐标）
│   │   └── 模板/T2_gong.json
│   ├── 游戏Demo/              ← 试玩验证件（独立交付，与工具包分开）
│   │   ├── 游戏Demo说明.md      怎么打开/玩法/暗牌交互/试玩生成关卡
│   │   ├── 打开游戏Demo.bat     双击直接开玩
│   │   ├── game_demo_std.html   游戏页面（含暗牌玩法 + 内置示例）
│   │   └── 示例关卡/          示例_暗牌.json（98张/49对/11暗）、示例_普通.json
│   ├── 关卡配置/               ← 旧工作目录（T1 链路 + 早期 T2 脚本，保留参考）
│   ├── 需求表/                ← 需求 xlsx + 生成脚本
│   └── 输出文档/              ← 玩家体验流程等
├── 包_资源/                  ← 美术资源（ART-0208~0250 麻将牌）
├── vita截图/                  ← Vita 麻将参考截图
└── node_modules/              ← Excel 处理依赖
```

**技术栈**：原生 HTML/CSS/JS（编辑器）、Node.js（生成脚本）、xlsx 库（需求表处理）。

**生产工作流**：
1. 手工准备多个"底子"模板（不同 L0 形状，用编辑器手摆，如 real_v2）
2. 每个模板用 v14 规则生成器产出 20+ 变体（轮换 8 种散落风格）
3. 用户持续提供更多模板 → 每个模板再各自扩展 → 几百关
4. 倒推法填花色（100% 可解 + 自然深度）+ 难度评分
5. 导出 JSON 直接给程序

**当前进度（截至 Day 16）**：
- ✅ 需求文档审计修复（Day 10）
- ✅ 形状生成方案定型：模板底子 + 随机中心塔（v17，纯随机 24/24 通过）
- ✅ 自研形状编辑器可用（支持新坐标系、坐标显示、象限支撑检查）
- ✅ v17 随机生成器跑通（基于 real_v2，无风格命名）
- ✅ 倒推法填花色（backward_fill_v17.js，34 花色 0~3 对，100% 可解）
- ✅ 坐标规则统一（修复生成器/demo 横纵轴不一致，正确规则下 24/24 可解）
- ✅ 消除逻辑按牌修复（matchedTypeIds → matchedTileIds，同花色多对互不影响）
- ✅ 难度量化（公式 + 实测，去上限后原始分 67~117）
- ✅ 输出命名（T1 + 难度分）
- ✅ std 坐标全链路（新增一套 *_std 文件，row=上下/纵、col=左右/横，原脚本不动）
- ✅ 程序需求文档重写（简化版 Word 格式，35 份 PROG 文档，5 章节结构）
- ✅ 分数公式与 Combo 规则定稿
- ⏳ 待做：难度权重校准（顶端仍偏挤 96~117，如需更均匀再调权重）
- ⏳ 待做：用户提供更多底子模板后再批量扩充
- ✅ 暗牌机制落地（填色后标暗，优先钩子深埋牌，难度加权暗钩子，100% 可解不变）
- ✅ T2 模板链路（T2_gong → batch_generate_T2 + backward_fill_T2_dark → T2_暗/，24/24 可解）
- ✅ 脚本/文件全面清理（只留 std 全链路，删非 std 与历史废弃版本 + 非 std HTML/服务器）
- ⏳ 待做：暗牌/吐牌机在编辑器中集成
- ✅ 网页版批量工具（浏览器选模板→选模式→数量→一键生成→成品统计表，Day 16）
- ✅ 一步式生成脚本 generate_fill_std.js（生成+填色合并，去掉 batch_ 中间目录，直接出 <TAG>_暗/ 或 <TAG>_填色/，Day 16）
- ✅ 移除预览功能（网页按钮 + _预览.txt，Day 16）
- ✅ 每批数量可调（1~200 自动夹取）+ 重跑前清空旧输出（Day 16）
- ✅ 游戏 Demo 独立交付件（outputs/游戏Demo/：说明文档 + 打开bat + 游戏页面 + 示例关，Day 16 补充）
- ✅ demo 支持暗牌试玩（PROG-0224 选中态：点暗牌翻面选中→同花色直接配对/异色转移/槽内同色立即消除；内置示例关，普通/暗牌导入都实测通过，Day 16 补充）
- ✅ demo 麻将牌象牙白改版（圆角/斜切棱边/玻璃高光/随层高加深投影 + 真实花色：万红字、筒蓝点、条带竹节竹条、风藏蓝、白板；第一版彩色牌身被否后返工，Day 16 再补充）

---

## Day 10 · 8月3日（周一）：需求文档审计与修复

### 背景

发现需求文档存在大量矛盾：生成脚本 `regen_v3_clean.cjs` 的数据滞后于 Day 8/9 的决策，导致重新生成的 xlsx 总表/清单仍是旧数据，而 md 详细文档部分被手工改过、部分没有，两套数据源不同步。

### 审计发现：共 15 个问题

**严重（4 个，已全部修复）：**

1. **PROG-0100 主菜单仍含"体力条"** — Day 8 已砍体力系统，xlsx 和生成脚本里仍写着"体力条"
2. **PROG-0303 结算奖励仍发"体力"和"金币"** — 无体力系统、无金币系统，奖励描述全部错误
3. **8 个美术编号在美术清单中不存在** — 程序文档引用 ART-0200/0201/0203/0204/0205/0206/0207/0269，美术清单实际从 ART-0208 开始，编号体系错位
4. **特殊牌主题错误** — 美术清单是动物系（锦鲤/仙鹤/瑞鹿/灵狐/骏马/白虎），Day 8 决策是宝物系（铜钱/银锭/金元宝/如意/玉佩/宝瓶/凤/龙）

**不一致（已修复）：**

- PROG-0103/0104 详情文档路径错位（整体错位一行）
- ART-0106 详情路径写成 ART-0105
- PROG-0103/0104 美术依赖互换

**脏数据（已清理）：**

- 生成脚本统计数字错误（主菜单 6 条写成 5 条、局内美术 61 条写成 69 条）
- 注释残留"金色8张"旧数据（金色版 Day 8 已删）

### 修复内容

**修改文件：**
- `outputs/需求表/regen_v3_clean.cjs` — 数据源脚本全部修正
- 重新生成 3 份 xlsx：需求总表、程序需求清单、美术需求清单
- 11 份 PROG md 详细文档（美术依赖编号对齐）
- `PROG-0303.md` — 重写，MVP 阶段不设计奖励内容，仅记录表现

**特殊牌宝物系定稿（ART-0243~0250）：**

| 编号 | 段位 | 宝物 |
|------|------|------|
| ART-0243 | 青铜 | 铜钱 |
| ART-0244 | 白银 | 银锭 |
| ART-0245 | 黄金 | 金元宝 |
| ART-0246 | 铂金 | 如意 |
| ART-0247 | 钻石 | 玉佩 |
| ART-0248 | 史诗 | 宝瓶 |
| ART-0249 | 大师 | 凤 |
| ART-0250 | 荣耀 | 龙 |

### 还遗留的问题（待处理）

**美术资源缺失（程序引用了但美术清单没列入）：**

1. **返回按钮图标** — PROG-0201 引用，局内左上角返回箭头
2. **菜单按钮图标** — PROG-0203 引用，局内右上角菜单按钮
3. **收藏/皮肤/分享三个按钮图标** — PROG-0104 引用，主菜单右上角

这三个暂时在 md 里标记为"待补充"，xlsx 美术依赖列留空。需要和美术确认是否补进清单。

**麻将牌美术详细文档缺失：**

- ART-0208~ART-0250 共 43 份麻将牌美术资源，只有清单条目，没有对应的 docx 详细文档（其他 ART 都有）

**编号断档：**

- ART-0200~ART-0207 空号 8 个（旧编号残留），功能不影响，看着不整齐

### 关于关卡设计的新认知

阅读了领导发的《关卡编辑器（Web 版）使用说明文档.pdf》（3 页），确认了后续关卡制作方向：

- **策划用 Web 编辑器手工摆造型**（鼠标网格点刷、多层 3D 堆叠）
- **算法自动配对保底**（导出时后台模拟消除一遍，自动填入 100% 可解的花色）
- **三种画笔**：普通牌、暗牌/反面牌、吐牌机机关
- **导出格式**：level_X.json，提交给程序即用

这是行业标准做法的可视化版本：造型手工搭，配对算法填。当前项目还在"Excel 配置表 + 生成脚本"阶段，编辑器是后续升级方向。

### 棋盘改版

- 关卡配置从 6×7 改为 **6×8**，总牌数仍 110 张（55 种 × 2），四层阶梯金字塔结构不变
- 左右镜像在 6 列偶数下天然成立（x=0↔5、1↔4、2↔3），无单数轴问题

### 心得

- **生成脚本是双刃剑**：保证批量输出一致性，但数据源一旦滞后，错误会被反复复制。改数据时一定要先改脚本，再重新生成，最后同步手工文档。
- **审计要系统化**：这次用子 agent 并行扫了所有文档，一次找出 15 个问题。人工一个个看很容易漏。


## Day 11 · 8月4日（周一）：关卡形状生成器探索（暂停）

### 背景

研究关卡自动生成。目标：写一个形状生成器，自动产出像 Vita 麻将那样的关卡布局（只有位置、没有花色），后续再接反向生成填花色 + 难度评分。

### 已完成的部分

- **填花色算法**（`level_generator_demo_v3.js`）：随机填花色 + 解算器验证可解性，跑通了，成功率 100%
- **难度评分**：基于初始可点率、层数、最少步数加权打分，0-100 分
- **完整流水线**（`shape_v3.js`）：形状生成 + 填花色 + 难度评分串起来，能跑通

### 卡住的部分：形状生成器

**当前方案被否决。** 生成的形状太规律、太死板：

- 底层就是个满铺的大矩形
- 上层只是简单地在中间放几张牌
- 整体看起来像"一个平面上摆几个麻将"，不像真实关卡

参考了 Vita 麻将的 12 张截图（`C:\Users\Administrator\Pictures\Screenshots\1.png` ~ `12.png`），真实关卡的形状更有变化：
- 不是规整的矩形 + 横条
- 有不规则的边缘、凸起、洞
- 每关形状都不一样，看起来像手工设计过的

**尝试过的方案（都不行）：**
1. 阶梯金字塔、钻石、十字、环形等几何模板 -- 太规整，不像
2. 6×N 网格 + 随机选格子 -- 底层还是大平面，上层只是零星几张
3. 矩形 + 横条 -- 太简单

**核心难点：** 用算法自动生成"看起来自然、有变化、不规律"的形状，比想象的难得多。Vita 麻将的形状库大概率是**人工设计的几十上百个模板**，不是算法生成的。

### 下一步方向（待定）

可能的路线：
1. **手动做模板库**：从 1-12.png 提取 12 个真实形状做成 JSON 模板，算法只负责填花色 + 算难度（这部分已经能跑）
2. **改进生成器**：加更多约束（不规则边缘、多凸起、非对称局部），但效果难保证
3. **混合**：少量手工模板 + 算法在模板基础上做变体

倾向方案 1 -- 先有能用的形状，算法部分（填花色+难度）已经有了，缺的只是形状库。

### 产出文件

- `outputs/关卡配置/level_generator_demo_v3.js` -- 填花色 + 解算器（可用）
- `outputs/关卡配置/shape_v3.js` -- 形状生成器（被否决，保留作参考）

---

*-- 本日志随项目进展持续更新 --*

## Day 12 · 8月5日（周二）：关卡形状生成器 —— 从算法生成转向"模板 + 变体"（关键突破）

### 背景

前几轮尝试纯算法生成关卡形状（v5/v6/v7/v8/v9），始终不满意：要么形状太规整（金字塔、矩形堆叠），要么太乱（随机散点），要么堆叠感不对（全格覆盖 vs 半格覆盖混乱）。今天终于找到正路：**先手工做模板，再用算法做变体**。

### 关键认知（从 real_v2 学到的）

拿到真实官方关卡 `real_v2`（86 张牌、6 层、36→25→15→6→3→1），分析出关键规律：

1. **逐层递减**：从底部（L0）向顶部（L5）逐层减少牌数。越靠近底部牌越多，越靠近顶部牌越少。本层 ≈ 下面一层 × 0.6（即 L0=36 → L1=25 → L2=15 → L3=6 → L4=3 → L5=1）
2. **底层不追求满铺**：左右分坨、有空隙、形状不规则（中间密两边疏）
3. **支撑规则（象限规则）**：上层牌的 4 个象限中心子格 (r,c)(r,c+1)(r+1,c)(r+1,c+1) 每个正下方都要有下层牌——可来自不同下层牌（不能用"单张覆盖 50%"或"必须 2 张托住"，那些是错误结论）
4. **无全覆盖**：上层不能与下层完全重合（遮挡隐藏下层牌）
5. **严格左右对称**：row ↔ -row（row=0 中轴牌镜像到自身，允许单张）
6. **自由偏移**：底层整格，上层可错位半格（不强制整格/半格层交替）

### 形状编辑器（自研）

为了手工做模板，写了一个 HTML 关卡形状编辑器 `shape_editor.html`：
- **坐标系**：row=横（-5..5），col=纵（-8..6），每张牌占 2×2 子格，左上角 (row=-5,col=-8)
- 分层编辑（Q/E 切层），每层颜色不同
- 对称绘制开关（画左边自动镜像右边，row ↔ -row）
- 支撑限制（上层只能在有支撑的位置放，红叉提示）
- 悬空检查（象限规则：4 个象限中心子格下方都要有牌）
- 三种画笔：普通牌、暗牌、吐牌机（含方向、次数）
- 导入/导出 JSON 与编辑器格式兼容
- 坐标显示开关（牌面显示 row,col）
- 俯视视图，上层牌向上偏移露出下层（能看出叠层关系）

### 突破：模板 + 变体（v10）

方案：**手工准备一批模板（真实关卡样式）→ 用变体生成器批量产出**。

变体机制（`shape_generator_v10.js`）：
- **平移**：整体上下移动
- **挖洞**：随机去掉几对（被上层依赖的牌不能挖，保持支撑）
- **加牌**：合法位置补几对
- **删层**：随机去掉最高层

结果：20/20 变体全部通过校验，保留了模板的"真实手感"，但每关都有细节差异。**最终确认可行**。

### 产出文件

- `outputs/关卡配置/real.json` — 官方真实关卡（模板源）
- `outputs/关卡配置/shape_generator_v10.js` — 模板变体生成器
- `outputs/关卡配置/batch_generate_v10.js` — 批量生成脚本
- `outputs/关卡配置/batch_v10/` — 20 个变体关卡（JSON + 预览 txt）
- `outputs/关卡配置/shape_editor.html` — 自研形状编辑器
- v5~v9 生成器保留作参考（已废弃）

### 填花色方案研究：倒推法（Backward Generation）

形状问题解决后，研究怎么给布局填花色（typeId）。对比两个方案：

**方案 A：随机填 + 解算器验证**（`level_generator_demo_v3.js` 已实现）
- 随机分配花色，用 DFS 解算器验证可解性，不可解就重试
- 缺点：成功率低（大布局常试几十次才中一次）、不可控

**方案 B：倒推法（推荐）**
- 从空棋盘开始，模拟"消除的逆过程"：随机选一个当前可见（未被更高层挡住）的位置，放一对同花色牌上去
- 每放一对，新牌可能盖住之前放的牌 -> 棋盘逐渐变满，深层牌自然产生
- 因为是消除过程的倒放，最终布局 **100% 可解**，且深度自然产生（不需要人为设定"先放深层再放表层"）

**关键澄清**：倒推法不是"从顶层往下"填（那样正着玩会一路直接消下去，太简单），而是"随机挑当前可见的位置"填。随着牌越放越多，后放的牌会盖住先放的，形成表层/深层的差异。

**深度差异的成因**：同一对牌 A1/A2 在倒推时同时放置，但位置不同。A1 放边缘后续少被盖 -> 保持可见；A2 放中央后续多次被盖 -> 深埋。正着玩时玩家看到 A1，必须先消掉压在 A2 上的牌才能匹配--这正是真实关卡里的"钩子"结构。

**钩子偏置**：纯随机倒推的"钩子"是概率出现的。要让每关稳定有几个"A1 可见 / A2 深埋"的钩子，需要在倒推时加引导：少数几对牌故意拆到边缘和中央，其他对正常随机。

**难度量化方向**：
- 初始可点率（越低越难）、层数、平均选择数（每步可选花色数，越少越难）、牌数
- 倒推时可直接控制生成顺序来设定难度（先放表层牌多 = 简单，先放深层牌多 = 难），难度是"设计出来的"而非事后评估

### 下一步

1. 用户继续提供更多"底子"模板（不同 L0 形状）
2. 每个模板用 v14 规则生成器产出 20+ 变体 → 几百关
3. 接倒推法填花色 + 难度评分（level_generator_demo_v3.js 已验证可用）
4. 特殊牌（暗牌、吐牌机）在编辑器中摆放

### 心得

- **纯算法生成"有设计感"的形状极其困难**，因为好看的形状本质是"手摆的"——有语义、有意图
- **"模板底子 + 规则生成"是最终方案**：用户提供不同 L0 底子（摆方块规则），算法按规则生成上层散落形状（v14）
- **变体不能只是"微调模板"**（v11 挖洞/加牌/平移变化太弱），要**按规则重新生成上层**（v14 8 种散落风格）
- **支撑规则是"象限规则"**（4 个象限中心子格下方都要有牌），不是"4/4 全覆盖+2 张托住"（那是错误结论，real_v2 大量牌只有 1 张托住）
- 生成器、校验器、编辑器三处规则必须一致，否则会出现"生成器说行、编辑器说悬空"的冲突
- 坐标系 row=横/col=纵 容易搞混，导入导出要往返测试（导出→导入→对比）确认

---

## Day 12 补充 · 8月5日：坐标系重定义 + 规则最终确认 + v14 规则生成器

### 坐标系最终确定（重要）

之前 real.json 的坐标语义含混（row 到底是不是左上角、col 范围多少），反复出 bug。经用户确认，**重新定义坐标系**：

- **row = 横坐标**（左负右正，-5..5，12 子格 = 6 麻将位）
- **col = 纵坐标**（上负下正，-8..6，16 子格 = 8 麻将位）
- **左上角 (row=-5, col=-8)**，右上角 (row=5, col=-8)
- 每张牌占 **2×2 子格**
- 新模板 `real_v2.json`（86 张、6 层、36→25→15→6→3→1）按此坐标

### 摆放规则最终确认

1. **严格左右对称**：row ↔ -row（row=0 中轴牌镜像到自身，允许单张）
2. **象限支撑**：上层牌 4 个象限中心子格 (r,c)(r,c+1)(r+1,c)(r+1,c+1) 每个下方都要有下层牌（可来自不同下层牌）
3. **无全覆盖**：上层不与下层完全重合
4. **同层不冲突**：同层牌范围不相交
5. **半格偏移**：底层整格，上层可错位半格
6. **逐层递减**：从底部向顶部逐层减少牌数（本层 ≈ 下面一层 × 0.6）

### 生成器演进（v11 → v14）

- **v11**：real_v2 微调变体 → 变化太弱（只是挖洞/加牌/平移，骨架不变）
- **v12**：规则生成（8 种底层造型）→ 变化大但不像 real_v2
- **v13**：模板微调 → 变化太弱
- **v14（最终）**：**保留模板 L0 底子，按规则重新生成 L1+ 上层**，8 种散落风格（均匀散落、左偏、右偏、上靠、下靠、中央密集、边缘散落、横条），每关不同形状。240/240 校验通过

### 产出文件

- `outputs/关卡配置/real_v2.json` — 新模板（最终坐标系）
- `outputs/关卡配置/shape_generator_v14.js` — 规则生成器（8 种风格）
- `outputs/关卡配置/batch_generate_v14.js` — 批量生成脚本
- `outputs/关卡配置/batch_v14/` — 20 关（2 轮 × 8 风格 + 4）
- `outputs/关卡配置/shape_editor.html` — 编辑器（新坐标系、坐标显示、象限支撑检查）
- v5~v13 生成器保留作参考（已废弃）

---

## Day 12 再补充 · 8月5日：V17 随机生成器定稿（放弃"风格命名"）

### 背景

v14 的 8 种"散落风格"批量产出的关卡，用户始终不满意：要么太稀疏（上层牌太少），要么太规律（对称方块），要么偏置风格把塔拆散。反复试 v15（有序密排太方块）、v16（删对称/全覆盖规则——**严重错误，违反既定规则，被否决**）之后，最终定位到根本问题。

### 关键认知

1. **"偏置风格"（左偏/右偏）这个设计本身就是错的**。真实麻将塔是"一个整体"：最宽在底、一层层收窄、最后收成一个尖（L5=1 张），所有层在同一条中轴上。左偏/右偏把上层限制到半张棋盘，结果就是"一大片底 + 角落一坨"，视觉上断裂，永远不像真关卡。
2. **读 real_v2 底层坐标发现：L0 根本不是实心矩形**，而是有形的"底子"——左边缘一列 6 张、中间块 12 张、右侧厚块 18 张，左薄右厚、有缺口。这才让关卡看起来像真的。生成时保留 L0 原样就是保留这个"底子"。
3. **上层要立得住，L1 必须"有序互锁"**（棋盘式密排，像骨架，喂饱上层）；L2 及以上才能"随机有机"（打乱挑选）。如果 L1 就随机，上层会因支撑位不足级联塌陷。
4. **淘汰"风格命名"**：不再有"均匀散落/中央密集/横条"这些标签。每次生成时随机抽取金字塔目标（L1 基础 13 张，往上递减，每层加 0.7~1.3 随机抖动）和行偏好（从中抽 1~3 行作排序优先），结果天然每关不同、又都保持中心塔。

### V17 生成方案（定稿）

**整体思路**：L0 用模板原样（保住"底子"形），L1 有序互锁（撑起塔的骨架），L2~L5 随机有机（制造每关差异）。无风格命名、无偏置，始终中心化。

```
L0       = 模板 real_v2 的底层（36 张，原样保留）
L1       = ordered 有序密排（互锁骨架，够密）
L2..L5   = random 随机有机
每层张数   = 随机金字塔目标 × 随机抖动
```

**具体机制（从 shape_generator_v17.js 提炼）**：

1. **每层张数**：基础目标 `baseTargets = [13, 9, 6, 4, 2]`（对应 L1..L5，镜像展开前）。每层实际张数 = `基础 × 随机抖动(0.7~1.3)`，下限 2。所以最终分布约 `36 / 20~28 / 11~18 / 4~9 / 1~4 / 0~2`。

2. **行偏好**：每次从 `[1, 3, 5]`（棋盘中间三行）随机抽 1~3 行，作为该关的"优先行"。行偏好**只影响排序**（先填哪行），**不排除其他行**——保证塔始终占满中轴、不会偏到一边。

3. **候选位置**：遍历 `row 0..5, col -8..6`，保留**被下层完全托住**（象限规则：4 个象限中心子格下方都有下层牌）且**不越界、不与下层同格重合**（无全覆盖）的位置。

4. **L1 ordered 模式**：按"优先行 → 行 → 列"有序密排，尽量挑不互相重叠（2×2 不相交）的位置。这样 L1 形成互锁的密集骨架，为上层提供充足支撑位。

5. **L2+ random 模式**：候选打乱后随机挑，同样保证不互相重叠。有机散落，每关不同。

6. **镜像展开**：选中位置 `(row, col)` 后，自动补上镜像位 `(-row, col)`（左右对称规则）。row=0 中轴牌镜像到自身，允许单张。预选数因此约 ×2 得最终张数。若展开后超出上限则随机裁剪。

7. **兜底**：最后剔除缺下层支撑的牌（含其镜像），保证无悬浮牌。

8. **校验**：每关过完整规则——严格左右对称 / 象限支撑 / 无全覆盖 / 同层不冲突，24/24 通过。

### 结果

一次生成 24 关，24/24 全部通过完整校验（严格左右对称 / 象限支撑 / 无全覆盖 / 同层不冲突）。分布健康：底部 36 → L1 18~28 → 逐层收窄到尖，大部分是 6 层实芯塔，少数（如 L015/L017）顶部收得陡（L4/L5 塌到 0）。用户确认：**"就按这个算法来生成，后面出问题再说"**——算法定稿。

### 产出文件

- `outputs/关卡配置/shape_generator_v17.js` — 随机生成器（定稿）
- `outputs/关卡配置/batch_generate_v17.js` — 批量脚本（PER_TEMPLATE 可调）
- `outputs/关卡配置/batch_v17/` — 24 关（JSON + 预览 txt + 汇总）
- v14~v16 生成器保留作参考（已废弃）

### 下一步（待用户推进）

1. 填花色（倒推法）+ 难度评分（level_generator_demo_v3.js 已验证可用）
2. 用户提供更多底子模板后批量扩充
3. 暗牌/吐牌机在编辑器中集成


## Day 13 · 8月6日（周三）：倒推法填花色 + 横纵轴规则统一 + 难度量化

### 一、倒推法填花色（backward_fill_v17.js，100% 可解）

形状定稿后，用**倒推法**给布局填 typeId（花色）：

- **34 种花色**：万1-9 / 条1-9 / 筒1-9 / 东南西北中发白
- **每种随机 0~3 对（不要求全出现）**：某个万字可能 0 次、另一个万字却出现 3 次——这是难度来源之一
- **倒推法本质 = 正向模拟消除**（不是真的"倒着放"）：
  1. 每步从当前**可点**牌里随机挑 2 张当作一对
  2. 用游戏原生的 `isClickable` 直接判定（被更高层压住 / 被同层左右夹住即不可点）
  3. 记录这对并移除
  4. 移除只会让更多牌变可点（可点集只增、不减少）→ 配对必然走到终点
  5. 放牌顺序的**逆序**就是合法消除序列 → **100% 可解**
- **钩子自然产生**：随机挑对，天然出现"一对里一张早暴露、一张深埋"（layer 差 ≥ 2），正是真实关卡的钩子结构
- **校验白证**：用 `verifyByElimination` 按消除序列正推一遍，每步两张同花色且可点，跑到底清空

### 二、横纵轴规则统一（本次踩的坑，已修复）

**背景**：此前把 `game_demo.html` 的横纵坐标交换过（row=横/col=纵）。交换后 demo 的 `isClickable` 已改为 `row ± 2` 判"左右夹住"（正确）。

**问题**：但生成器 `backward_fill_v17.js`（和旧方案 `color_fill.js`）的 `isClickable` 仍用 `col ± 2` 判"左右"——在 row=横/col=纵 的约定下，`col±2` 实际是**上下**夹住，不是左右。**生成器和游戏 demo 的可点规则不一致**：

- demo（正确）：被**左右**夹住不可点 → 判 `row ± 2`
- 生成器（错误）：用 `col ± 2` 当左右 → 实际判的是**上下**夹住

**后果**：生成器用错误轴保证可解，demo 用正确轴玩。有些对（一个 L0、一个高层）在生成器看来可同时消，真实规则下其中一张被左右夹住、永远点不到 → **"两个都无法消除"**。

**实测**：用正确规则求解，24 关里 **6 关不可解**（含 demo 默认加载的 level_001）。

**修复**：
1. `backward_fill_v17.js` 的 `isClickable` 左右判定改为 `row ± 2`（same col）
2. 重新填色：24/24 成功
3. 用正确规则重新求解校验：**24/24 全部可解**（之前 6 关不可解）
4. 顺带修 `color_fill.js` 的 `isHorizNeighbor` 和 `isClickable`（同 bug）

**教训（重要）**：生成器 / 校验器 / 游戏 demo 三处 `isClickable` 必须**同轴**。坐标一旦交换，必须把三处一起改，并往返核对（生成出的板子用游戏规则重跑求解器）。

### 三、难度量化

**公式**（`evaluateDifficulty`）：

```
难度分 = (1 - 可点率) × 40  +  层数 × 8  +  min(钩子占比, 1) × 30  +  10     （clamp [0,100]）
```

- **可点率** = 初始可点牌数 ÷ 总牌数（越低越难）
- **层数** = 最高层索引 + 1（每层 8 分）
- **钩子** = 一对中两张 layer 差 ≥ 2 的牌对数（占比超 1% 吃满 30 分）
- **基础** +10

**实测（24 关，原始分）**：最小 67 / 最大 117 / 平均 102.3。

**⚠️ 发现公式饱和、区分度不足**：
- 层数项 ×8 主导（6 层就 48 分）；钩子项按"占比饱和"太容易吃满
- 结果：凡 6 层、可点率低、有钩子的关，原始分普遍 >100（clamp 后全 100），**排不开难度**
- 待改进方向：① 降低层数权重 / 用对数压层数；② 钩子改按数量线性积分而非占比饱和；③ 引入解算器 DFS 搜索量（节点数）作为更细的难度指标

### 产出文件

- `outputs/关卡配置/backward_fill_v17.js` — 倒推填花色（34 花色 0~3 对，校验白证）
- `outputs/关卡配置/batch_v17_填色/` — 24 关填色成品（正确规则下 24/24 可解）
- `outputs/关卡配置/color_fill.js` — 旧正推方案（同轴规则已修正，保留参考）
- `game_demo.html` — 花色图形化（万=小号万字，筒=渐变圆球，条=竹青渐变条，花色缩小）+ 被遮挡牌不灰化、点击抖动提示


## Day 13 补充 · 8月6日：消除逻辑修复 + 输出命名与难度落地

### 四、消除逻辑 bug 修复（按牌 id 而非按花色）

**现象**：消掉 L0 的两张「四万」后，L4 相邻的两张「四万」也同步消失了。

**根因**：`checkMatch` 用 `matchedTypeIds`（按**花色/typeId**）记录"已消除"，匹配 2 张就把整个花色判为已消除；`getActiveTiles()` 按花色过滤，于是同花色其余牌（比如另一种子出 2 对的、共 4 张的花色）被一并剔除。

**修复**：改为按**具体牌 id** 记录（`matchedTypeIds` → `matchedTileIds`），匹配时只把进卡槽那 2 张的 id 记录为已消除，同花色其他牌不受影响。

**关联**：这个 bug 与"两个无法消除"（生成器轴错）是同一套"按花色 vs 按牌"语义的两面——之前按花色记录导致**少消不掉**，这次按花色记录导致**多消**。都已回归"按具体牌"。

### 五、输出文件命名落地（real_v2 → T1 + 难度分）

- 填色成品不再沿用源模板名 `level_XXX_real_v2_随机.json`，改为 **`level_XXX_T1_<难度分>.json`**
- **T1**：用户第一张"只给第一层（L0 底子）"的模板代号（曾暂用 V1，因与生成器 v1~v17 版本号冲突，改 T1 消除歧义）
- 难度分取自 `evaluateDifficulty`
- 实现：`backward_fill_v17.js` 的 `outFilename()`（`BATCH_TAG` 常量，可改），写盘前算难度拼进文件名
- 填色是**确定性**的（RNG 种子由源文件名数字决定），改命名重跑内容不变

### 六、难度去上限（原始分）

- 原公式 `Math.min(100, ...)` 有 0-100 上限，导致大半关卡原始分 >100 被压成 100，文件名里排不开难度
- **去掉 100 上限**（只保留下限 `Math.max(0, score)`），文件名显示**原始难度分**
- 实测 24 关：**67 ~ 117**（平均 102.3），可区分相对难易
- 遗留：顶端仍偏挤（96~117 集中），因层数 ×8 权重主导；如需更均匀可再调权重

### 产出文件（更新）

- `outputs/关卡配置/batch_v17_填色/level_XXX_T1_<难度分>.json` — 24 关成品（T1 命名 + 原始难度分）
- `outputs/关卡配置/backward_fill_v17.js` — 倒推填色 + 难度（无上限）+ 命名
- `game_demo.html` — 默认加载 `level_001_T1_107.json`


## Day 14 · 8月6日（周三）：取消联赛系统，改用排行榜（总榜/日榜）

### 背景

需求方向调整：**彻底取消「段位联赛」系统**，不再做段位、雕像、特殊牌积分、晋级/保级那套。MVP 的社交/成长线统一改为**排行榜**，分**总榜**和**日榜**两个维度。

### 依据（最新需求表）

以 8月6日 最新插入的两张需求清单表为现行基准：

- **程序表** `outputs/需求表/麻将消一消_游戏程序需求清单表.xlsx`
  - **PROG-0500** 排行榜主界面：关卡排行榜弹窗，含**总榜/日榜**两个 Tab，展示排名、头像、昵称、最高关卡数；底部固定显示玩家自己的排名
  - **PROG-0501** 排行榜数据存储与同步：记录最高通关关卡（总榜依据）和当日通关数（日榜依据），同步服务端
- **美术表** `outputs/需求表/麻将消一消_游戏美术需求清单表.xlsx`
  - **沿用（已验收）**：ART-0400 排行榜面板、ART-0402 前三名特殊样式、ART-0403 玩家自己排名行
  - **已取消**：ART-0401 排行榜行组件（复用面板内建行）、ART-0404 排行榜 Tab 切换（复用现有 Tab 组件）

### 排行榜设计定稿

**总榜：**
- 排名依据：最高通关关卡数（降序），相同关卡数按达到时间先后
- 数据来源：服务端

**日榜：**
- 排名依据：当日通关关卡数（当天新通过的关卡数）
- 每日 0 点清零重置（本地时间判断，冷启动检测跨天）
- 同一关卡重复通关不计入日榜（只计首次通关）

**界面：**
- 顶部：标题 + 关闭 + Tab 切换（总榜/日榜）
- 中部：排行列表（前三名金/银/铜 + 奖杯特殊样式，其余数字）
- 底部：玩家自己排名行固定显示；100 名以内显示具体数字，以外显示「99+」

**存储字段（PROG-0501）：**
- `highestLevel`（最通关关卡，总榜依据，只增不减）
- `dailyPassCount`（当日通关数，日榜依据）
- `dailyResetDate`（日榜重置日期，判断跨天）
- 通关胜利后立即同步；打开排行榜时检查同步一次；冷启动跨天重置

### 改动文件

- **`输出文档/玩家体验流程_v1.md`** — 升级 v4，取消联赛、改为排行榜：
  - 主菜单入口：右下侧「联赛雕像 + 倒计时」→「排行榜入口」
  - 主菜单操作表：点击联赛雕像 → 点击排行榜（总榜/日榜）
  - 胜利结算 4.3：特殊牌积分（联赛）→ 排行榜数据更新（通关更新最高关卡/当日通关数）
  - 三、特殊系统 6.1：整段「段位联赛系统」替换为「排行榜系统」（总榜/日榜规则 + 数据存储 + 美术依赖）
  - 待确认问题：#7 联赛周期移除，重新编号
  - 图索引：去掉 8 张段位雕像 + 联赛引导/晋级图，排行榜配图待补（参考 ART-0400 已验收）
- **`输出文档/_gen_flow_docx.cjs` + `玩家体验流程_v1.docx`** — 同步重排，移除段位雕像 2x2 排版与图片，docx 体积 13.7MB → 5.7MB

### 遗留 / 待确认

- 排行榜面板（ART-0400）已验收，但本地没有放大参考图，文档配图位置留「待补充」
- 排行榜服务端后端 MVP 阶段是否落地、还是先本地模拟，待定

### 补充：特殊牌废除（同日）

原联赛专属的「宝物系特殊牌（ART-0243~0250」随整体方向调整**一并废除**，连带清理建立在它之上的收藏系统：

- 特殊牌（铜钱→龙 8 种宝物系）不再出现于局内，与排行榜无关
- **收藏系统 6.3**（特殊牌图鉴）废除——收藏入口无内容，从主菜单功能按钮和操作表移除
- 主菜单右上角功能按钮组：~~收藏~~ / 皮肤 / 分享 / 设置（保留皮肤/分享/设置）
- 待确认问题 #8 改为「皮肤系统做到什么程度」

> 注：特殊牌相关美术（ART-0243~0250）与收藏入口（PROG-0104 中的"收藏"）在两张需求清单表中仍留有条目，**尚未同步清理**，待后续更新 xlsx 时一并处理。

### 心得

- **需求变更要"先改表、后改文档"**：这次基准是 8月6日 最新两张清单表，所有下游文档（体验流程 md + docx）都以它为准重写，避免文档与表脱节。
- **取消系统要连带清理**：段位雕像图（fig6-1~6-10）从 docx 排版里一并移除，避免残留死内容；docx 体积从 13.7MB 降到 5.7MB，就是删掉 8 张大图的收益。


## Day 14 补充 · 8月6日：std 标准坐标全链路（生成新文件，原脚本不动）

### 背景

老板的 Unity 关卡用的是 **row=上下(纵)、col=左右(横)** 的"标准"坐标，而我们内部工具链是 **row=横/col=纵**。此前做了隔离的导出转换脚本（`export_to_unity.js`，只交换导出值）。用户决定干脆**把整条工具链翻成新文件**，统一成标准坐标，但**不碰原来成功的脚本**。

### 方案：翻成新文件（_std 后缀）

把 5 个文件复制成新版本，只改坐标语义，算法逻辑不动：

| 新文件 | 改了什么 |
|--------|----------|
| `shape_generator_v17_std.js` | 算法内部仍用 row=横/col=纵 运算，只在 `toEditorJSON` 导出处交换 row/col 值 |
| `batch_generate_v17_std.js` | 指向新生成器，输出到 `batch_v17_std/` |
| `backward_fill_v17_std.js` | 可点判定"左右夹"轴从 `row±2` 改为 `col±2`（std 下横=col） |
| `shape_editor_std.html` | 编辑器内部本就是 row=纵/col=横，把 JSON 边界的交换去掉，直接读写 |
| `game_demo_std.html` | 渲染 row→Y、col→X；可点判定改 `col±2` |

**关键发现**：形状编辑器 `shape_editor.html` 的内部坐标**本来就是 std 约定**（key=`"row,col"`=纵,横），只是之前在 JSON 边界做了交换。所以 std 编辑器只需把边界交换去掉即可，改动最小。

### 验证结果

- 生成：24/24 关通过完整校验（对称/支撑/无全覆盖/同层不冲突）
- 填色：24/24 可解（消除序列白证），难度 93~116，平均 103.7
- 坐标方向：**row 范围 -8..4（纵）、col 范围 -5..5（横）** ✓
- 浏览器验证 std 编辑器：成功加载 std 关卡（L0:36/L1:22/L2:15/L3:7/L4:2=82 张），内部 key 直接是 `row,col`（如 `-8,-5`=左上），canvas 正常绘制
- 浏览器验证 std demo：成功加载 `level_001_T1_104.json`（82 张/41 对）

### 产出文件

- `outputs/关卡配置/shape_generator_v17_std.js`、`batch_generate_v17_std.js`、`backward_fill_v17_std.js`
- `outputs/关卡配置/shape_editor_std.html`、`game_demo_std.html`
- `outputs/关卡配置/batch_v17_std/`（24 关形状）、`batch_v17_std_填色/`（24 关成品，T1 命名+难度分）
- 原文件（shape_generator_v17.js 等）**完全未动**

### 心得

- **"翻坐标"最干净的做法**：算法内部保持一套坐标没问题，只在 JSON 边界交换。这样生成器/校验器/编辑器/游戏 demo 各自逻辑都不用大改，只动边界。
- **编辑器内部早已是 std 约定**：读文件时发现编辑器的 key 一直是 `"row,col"=纵,横`，只是导入导出时做了转换。所以"翻成全链路 std"比想象中小得多。
- **可点判定三处必须同轴**（沿用 Day 13 教训）：生成器/校验器/demo 的左右夹轴，std 下统一用 `col±2`。
- 预览窗格（preview pane）里 `board-wrapper` 高度为 0 会导致 demo 的 `cell` 变负、渲染上下颠倒——这是**预览环境缩放问题，不是代码 bug**，真实浏览器有正常高度时渲染正确。用浏览器开发者工具/真实窗口确认过方向无误。


## Day 15 · 8月10日（周日）：简化版程序文档重写 + 分数公式与 Combo 规则定稿

### 背景

原详细程序文档（35 份 PROG md）结构太复杂，每篇都有「概述 / 玩家场景 / 触发与前置 / 正常流程 / 详细规则 / 交互与表现 / 验收标准」七大章，实际开发看的时候信息密度低、翻半天。决定**重写一套简化版**，统一 5 个章节，导出 Word 格式，直接发给程序用。

### 简化版文档规范（5 章节）

| 章节 | 内容 | 格式 |
|---|---|---|
| 1. 功能描述 | 一句话说明这个功能干什么 | 正文段落 |
| 2. 逻辑流程 | 程序执行顺序，用 ↓ 连接 | 顺序行 |
| 3. 表现需求 | 告诉程序调用什么表现（视觉/音效/动效） | 项目符号 |
| 4. 特殊规则 | 只写特殊约束 | 编号列表 |
| 5. 验收标准 | 什么时候算完成 | √ 标记 |

**格式要求**：标题四号黑体加粗，正文小四宋体，全黑。

### 产出

- `outputs/需求表/简化程序文档/PROG-0100.docx ~ PROG-0501.docx` — 共 **35 份** Word 文档
- 覆盖：主菜单（5）、局内游戏（19）、结算界面（3）、系统后台（3）、排行榜（2）
- 生成脚本：`outputs/需求表/_gen_simple_docs_docx.cjs`（数据 + 渲染一体，便于后续批量修改）

### 分数公式定稿（PROG-0202）

之前草稿写的是「基础分 320 + Combo × 40」的线性加法，感知不强。改为**倍率制**：

```
单次消除得分 = 基础分 × Combo 倍率
```

| Combo | 倍率 | 单次得分 |
|---|---|---|
| 1 | ×1 | 100 分 |
| 2 | ×1.5 | 150 分 |
| 3 | ×2 | 200 分 |
| 4 | ×2.5 | 250 分 |
| 5 | ×3 | 300 分 |
| 6 | ×3.5 | 350 分 |
| 7 | ×4 | 400 分 |
| 8+ | ×4.5 | 450 分（封顶） |

- 基础分 100（整数好记，分数干净）
- 8 连封顶 ×4.5（防止数值爆炸）
- 估算单局总分：正常通关 5000~8000，高手 12000~15000

### Combo 中断规则修正（重要）

之前草稿写的是「入槽不消除 = Combo 中断」，讨论后修正为：

- **Combo 清零（中断）**：玩家点击到**锁定牌**（被压住 / 被左右夹住的不可操作牌）—— 这是操作失误
- **Combo 保持不变**：入槽但没配对消除 —— 正常选牌，不算失误
- **Combo 保持不变**：使用道具（洗牌/提示/撤销）

同步更新了 PROG-0202（分数显示）和 PROG-0221（Combo 连击激励）两份文档的特殊规则和验收标准。

### 心得

- **简化文档的核心是"砍掉铺垫"**：概述、玩家场景、触发前置这些章节信息其实都能合并进逻辑流程和特殊规则里。5 章结构比 7 章少了 30% 的文字量，但关键信息一点没丢。
- **Combo 中断的定义一定要在文档里写死**：「什么叫失误」直接影响分数和爽感，程序、策划、测试三方必须对同一句话，不然后面扯皮。定义成"点到锁定牌"是最清晰的——玩家犯错就惩罚，正常选牌不惩罚，手感上也更宽容。


## Day 13 补充 · 8月6日：std 坐标全链路（row=上下/纵, col=左右/横）

### 背景

老板的 Unity 关卡用的坐标是 **row=第几列/纵向、col=第几行/横向**（即 row=上下、col=左右），和我们内部 **row=横向/col=纵向** 正好相反。之前已做过一版"仅导出转换"（`export_to_unity.js`，isolated，低风险）。但用户决定**干脆把整条工具链统一成标准坐标**，于是新增一套 `_std` 文件，**原脚本一字不动**。

### 方案：新增一套 *_std 文件，原脚本保持成功

内部算法全部不动，只在**坐标边界**交换，风险最低：

| 文件 | 改动点 |
|------|--------|
| `shape_generator_v17_std.js` | 与 v17 相同算法；仅在 `toEditorJSON` 导出处把每张牌的 row/col 值互换 |
| `batch_generate_v17_std.js` | 指向 std 生成器，输出到 `batch_v17_std/` |
| `backward_fill_v17_std.js` | 与 v17 相同；仅 `isClickable` 左右夹轴从 row 改为 col（std 里左右=col±2） |
| `shape_editor_std.html` | 关键发现：编辑器**内部本来就是 row=纵/col=横**，原来在 JSON 接口处交换。std 版直接去掉交换，JSON 与内部一致 |
| `game_demo_std.html` | 渲染 x←col、y←row；`isClickable` 左右夹轴 col±2；默认加载 std 关卡 |

### 内部坐标约定差异（容易踩坑）

- **生成器/填色内部**仍是 row=横（-5..5）、col=纵（-8..6），只在导出时交换
- **编辑器内部**本来就是 row=纵、col=横 —— 与 std 目标一致，所以 std 版只是"不再交换"
- **std 成品 JSON**：row=纵（-8..6，上负下正）、col=横（-5..5，左负右正）

### 验证结果

- 生成 24 关 → 填色 24/24 成功、**24/24 校验可解**（白证）
- 坐标方向实测：row=-8 渲染在顶、row=4 在底（row=纵 ✓）；col=-5 在左、col=5 在右（col=横 ✓）
- 可点判定：被左右(col±2)夹住的牌不可点 ✓；正常消除 82→80 ✓
- 编辑器往返：读 std JSON 层数 [36,22,15,7,2] 一致；导出 82 张 0 缺失 0 多余 ✓

### 产出文件

- `outputs/关卡配置/shape_generator_v17_std.js` / `batch_generate_v17_std.js` / `backward_fill_v17_std.js`
- `outputs/关卡配置/shape_editor_std.html` / 根目录 `game_demo_std.html`
- `outputs/关卡配置/batch_v17_std/`（24 关形状）/ `batch_v17_std_填色/`（24 关成品，T1+难度命名）
- 原 v17 脚本、编辑器、`game_demo.html`、`export_to_unity.js` 全部保持不动

### 心得

- **"翻坐标"最省事的位置是坐标边界**：算法内部用哪套都行，只要 JSON 进出统一。生成器在导出交换、填色在判定交换、编辑器干脆内部就是目标约定，三处思路不同但结果一致。
- **编辑器内部恰好是目标约定**是意外之喜——std 编辑器只是"删掉交换"，几乎零风险。
- **换坐标最容易漏三处**：渲染映射（x/y ← row/col）、可点判定的左右轴、JSON 导入导出的字段对应。三处都验证到了。
- **预览窗格高度为 0 会让麻将堆叠渲染翻车**（cell 变负、牌堆上下颠倒），是预览 iframe 尺寸问题，不是代码 bug；把视图调大后恢复正常，验证时务必先确认视口有真实高度。

---

## Day 15 · 8月11日（周一）：暗牌机制 + T2 模板链路 + 全面清理

### 一、暗牌机制（新玩法落地）

决定做**暗牌**玩法：填色完成后随机挑几张牌标为暗牌（`specialTiles`，`type:'dark'`），玩家看不到牌面、需翻面才能匹配。**关键设计**：

- **暗牌只隐藏牌面，不改变"哪些牌可点、消除序列成立"** → 任意暗牌数量下关卡仍 100% 可解，倒推填色逻辑零改动
- **优先把"钩子对里深埋的那张"标暗**（layer 差 ≥ 2 对里更深的那张 = 被盖住/夹住、看不到是什么的那张）——这是暗牌最能加难度的场景
- **难度公式加权**：新增 `暗钩子占比 × 20` 项

```
难度分 = (1-可点率)×40 + 层数×8 + min(钩子占比,1)×30 + 暗钩子占比×20 + 10
```

**产出**：`backward_fill_v17_std_dark.js`（T1 用，输出 `batch_v17_std_填色_暗牌/`，24 关，实测每关 100% 含暗钩子，平均难度 103→123.7）。

### 二、T2 模板链路（新模板 T2_gong）

用户手工摆了个新 L0 模板 `模板/T2_gong.json`（std 坐标，44 张，仅 L0），要求生成 `level_001_T2_xxx.json` 到 `T2_暗/`。配了专用脚本：

- `batch_generate_T2.js` — 用 T2_gong 生成形状 → `batch_T2/`
- `backward_fill_T2_dark.js` — T2 暗牌填色（BATCH_TAG='T2'）→ `T2_暗/`

**踩了两个坑（都修了）：**

1. **std 模板坐标不匹配**：T2_gong 是 std 编辑器摆的（row=纵/col=横），生成器内部用 row=横/col=纵，只在导出时交换。直接喂 → 模板 L0 被当内部坐标，对称校验全挂（row=-8 镜像到越界）。修法：T2 批量脚本加载模板时先把坐标 std→内部对调，生成完再换回 std。
2. **模板只有 L0 → 生成的全是 1 层平面**：生成器默认按模板层数生成，T2_gong 只有底层。修法：`generateFromTemplate(template, { layerCount: 6 })` 强制向上堆 6 层塔。

**结果**：24 关，24/24 可解，6 层塔，难度 62~136，命名 `level_001_T2_114.json` 这种，坐标 std（row=-8..6 纵 / col=-5..5 横）。

> 暗牌数后续从固定 4 张改为**按总牌 10%**（`DARK_RATIO=0.1`，约 8~12 张/关），随关卡规模缩放。难度权重只算"暗钩子"，加普通暗牌数量不涨分。

### 三、脚本/文件全面清理

删除了所有非 std 与历史废弃版本，只留 std 全链路：

**删除的脚本**：旧生成器 shape_generator v2~v17 / batch_generate v5~17（非 std）、旧填色 backward_fill / backward_fill_v17 / color_fill、早期方案 shape_v3 / shape_simple / shape_rect_bars / shape_showcase / shape_gallery / shape_gallery_v2、根目录 bundle_levels / export_to_unity / level_generator_demo v1~v3、关卡配置里重复副本 batch_fill / levels.js / bundle_levels / export_to_unity / level_generator_demo*、普通版 `backward_fill_T2.js`。

**删除的目录**：batch_v5~v16、batch_v17、batch_v17_填色（非 std）、export_unity（旧隔离转换）、T2（普通版，被 T2_暗 取代）、排版（空目录）。

**删除的旧 json**：real.json（旧模板）、generated_sample / shape_sample / shape_gallery_sample（旧样本）。

**删除的非 std HTML**：`shape_editor.html`、`game_demo.html`；`.claude/launch.json` 只保留 `shape-editor-std`（8997）一个服务器。

**保留**：`模板/T2_gong.json`、`batch_v17_std`（T1 形状）、`batch_v17_std_填色`（T1 成品，**game_demo_std.html 仍在引用**）、`batch_v17_std_填色_暗牌`（T1 暗牌成品）、`batch_T2`（T2 形状，填色的输入）、`T2_暗`（T2 暗牌成品）、presentation（演示 pptx）、`关卡麻将分布统计_v1.md`（Vita 参考）。

### 当前工具链（std 全链路）

```
生成形状:  node outputs/关卡配置/batch_generate_v17_std.js   (T1, real_v2) → batch_v17_std/
           node outputs/关卡配置/batch_generate_T2.js        (T2, T2_gong) → batch_T2/
填色:      node outputs/关卡配置/backward_fill_v17_std.js         (普通)
           node outputs/关卡配置/backward_fill_v17_std_dark.js    (暗牌) → batch_v17_std_填色_暗牌/
           node outputs/关卡配置/backward_fill_T2_dark.js         (暗牌) → T2_暗/
```

> 注：T2 模板是 std 坐标，喂生成器前必须先在 batch_generate_T2.js 里对调 row/col；模板只有 L0 时记得传 layerCount。

---

## Day 16 · 8月13日（周四）：网页版批量工具 + 一步式生成（去掉中间目录）

### 一、网页版批量工具（不再看黑白终端）

「批量生成与填色.bat」原来是终端黑白菜单，对策划不友好。这次做成和编辑器一样的**网页点选**操作：

- `serve_editor.js`（本地服务器，纯 Node 零依赖）新增 API：
  - `GET /api/templates` —— 列出 `模板/*.json`
  - `POST /api/run` —— 一键跑「生成布局 + 填花色」，stdout 流式推给浏览器
- 新增 `批量工具.html`（双击 bat 自动打开），四步点选：
  1. **① 选模板**（模板卡片，系列名自动识别，换模板不用改代码）
  2. **② 选填色方式**（暗牌推荐 / 普通）
  3. **③ 一键生成**（「每批数量」可填 1~200，默认 24；「重跑前清空旧输出」默认勾选）
  4. **④ 成品统计表**（每关难度分 / 可点 / 钩子 / 暗牌 / 层数 + 难度最小-最大-平均）
- 修了一个 bug：模板名重复加 `.json`（/api/templates 返回带后缀、前端回传又拼一次 → `T2_gong.json.json`，统一在服务器端去后缀）

### 二、移除预览功能（用户：没什么用）

- 网页统计表的「预览」按钮、`pv()` 函数、相关 CSS 全部删除
- 生成脚本不再写 `level_XXX_<TAG>_预览.txt`（`_预览.txt` 与 `drawShape` 一起从生成器里清掉）
- README 同步去掉预览相关描述

### 三、每批数量可调 + 重跑语义

用户问「点两次开始生成会一样吗」：布局用 `Math.random` 每次不同 → **内容不会重复**；但文件名固定 `level_001~XXX`，第二次点开始会**同名覆盖**；填色后难度分写进文件名 → 难度分变了就留下两份同名不同分的文件，反复重跑越堆越乱。

解决（网页 ③ 区）：
- **每批数量**：1~200，默认 24；超出范围自动夹到 1~200（0/负数/空/非数字 → 24）
- **重跑前清空旧输出**：默认勾选，点开始时先把同系列同填色方式的旧成品目录清掉再生成 → 永远干净的整数份
- 不勾选时旧文件「难度分没变」的同名覆盖、「变了名」的残留（可能出现同编号两份），README 已写清

### 四、一步式生成脚本 generate_fill_std.js（去掉 batch_T2 中间目录）

用户要求不要 `batch_T2` 中间目录。把「生成布局 → 填花色」合并进**一个脚本、内存里完成、直接写成品**：

- `node generate_fill_std.js <模板名> [数量] [dark|plain]` → 成品直接进 `<TAG>_暗/` 或 `<TAG>_填色/`，全程不落 `batch_<TAG>` 中转
- 层数自动判断：纯 L0 模板 → 堆 6 层；自带多层 → 按模板层数（把 Day 15 手传 `layerCount` 的坑自动化了）
- 暗牌/普通两模式统一（`evaluateDifficulty(filled, darkIds)` + mode 分支），统计行 / 汇总行格式统一
- **删除**三个旧脚本：`batch_generate_std.js` / `backward_fill_std.js` / `backward_fill_std_dark.js`（被合并取代）
- 顺带删掉无人用的 `/api/list` 端点；`批量生成与填色.bat` 的 auto 模式改为单脚本调用

**验证**：dark → 只出 `T2_暗/`，plain → 只出 `T2_填色/`，无任何中间目录；bat auto 全链路可用；网页统计表对暗牌 / 普通两种行格式都能解析。

### 产出文件（工具包最终形态：10 文件 + 模板/）

- `使用说明.md` / `难度分说明.md` / `serve_editor.js` / `generate_fill_std.js` / `shape_generator_v17_std.js`
- `批量工具.html` / `shape_editor_std.html`
- `启动编辑器.bat` / `批量生成与填色.bat`（`auto <模板名> [dark|plain] [数量]`）
- `模板/T2_gong.json`

### 心得

- **网页化是质变**：终端黑白菜单 → 「选模板 → 点开始 → 看统计表」，策划自己就能跑整条生产链，不用碰命令行、不用改 JS。
- **中间目录是历史遗留**：两步脚本必然要落盘中转；合并成一个脚本后，中间产物留在内存里，目录直接少一层，交付物更干净——「少一个目录」和「少两个脚本」对使用方是实打实的体验。
- **网页 UTF-8 天然绕开 Windows 终端 GBK 乱码**：node 输出在 cmd 里显示正常、重定向到文件才乱码，浏览器流式日志不存在这个问题，中文模板名全链路可用。
- **发压缩包前自查**：README 版本日期（2026-08-13）、目录结构与实际文件一一对应、README / 脚本 / 网页三处流程描述一致（本日已核对）。

---

## Day 16 补充 · 8月13日：游戏 Demo 独立交付件（含暗牌试玩）

### 背景

用户要求把游戏 demo 相关内容整理成**独立交付件**，与关卡搭建工具包分开（各自单独发给对方）。demo 原在根目录 `game_demo_std.html`，只有硬编码的 T1 默认关（fetch `outputs/关卡配置/batch_v17_std_填色/...`，file:// 下必失败），且不支持暗牌。本次整体翻新。

### 一、目录（outputs/游戏Demo/，独立自包含）

```
游戏Demo/
├── 游戏Demo说明.md        怎么打开 / 玩法 / 暗牌交互 / 怎么试玩生成关卡 / 已知限制
├── 打开游戏Demo.bat       双击直接开玩（cd /d %~dp0 + start）
├── game_demo_std.html     游戏页面（含暗牌玩法 + 内置示例关）
└── 示例关卡/
    ├── 示例_暗牌.json     98 张 / 49 对 / 5 层 / 11 张暗牌
    └── 示例_普通.json     88 张 / 44 对 / 5 层 / 无暗牌
```

### 二、demo 代码改造（新增暗牌玩法，按 PROG-0224 选中态机制）

- **暗牌交互 = 点一下即翻面选中**（PROG-0224《背面麻将的选取》）：点背面 → 翻为正面并选中（金色描边高亮）；再点**同花色**的另一张（明/暗均可）→ 两张一起消除；点**异色暗牌** → 选中转移、旧牌翻回背面（可自由翻面）；**点已选中的牌 → 移入卡槽**（槽内同色随之消除）；卡槽已有同花色 → 翻开后立即进槽消除。
  - **第一版实现踩的坑**：曾做成"点背面只翻面 → 再点一次进槽"的两步，翻完永久正面、无选中态、不能直接配对——与 PROG-0224 不符，被用户指出后重做。
  - **第二版又踩一个坑**：把"点已选中的牌"做成"取消选中翻回背面"——用户指出应是**收入卡槽**（翻回背面只发生在选中转移到另一张暗牌时），已修正。
- **状态**：`selectedDarkId`（当前选中暗牌 id，`isBack = t.isDark && t.id !== selectedDarkId`）+ `justFlipped`（翻牌动画）。统一点击入口 `clickTile(id)`：无选中走首次点击分支（暗牌翻面选中 / 卡槽同色立即消除 / 明牌进槽），有选中走配对分支（同色一起消除 / 异色暗牌转移 / 异色明牌进槽）。
- **加载**：`loadLevel` 重置 selectedDarkId；关卡名追加 `· 含暗牌` 标记（普通关不显示）。
- **内置示例**：把 `示例_暗牌.json` 压缩成 `SAMPLE_LEVEL` 常量内嵌（node 脚本注入，纯 ASCII），file:// 打开即有内容可玩，不再依赖 fetch。
- **CSS**：`.tile.dark` 深蓝背面 + 问号；`@keyframes flipIn` 翻开动画；`.tile.selected` 金色描边高亮；`z-index = layer*1000 + id` 保持不变。

### 三、实测（浏览器验证，全程无 JS 报错）

| 用例（PROG-0224 各分支） | 结果 |
|---|---|
| 页面加载（内置暗牌示例） | 98 张 / 49 对 / 「关卡 1 · 含暗牌」✓ |
| 暗牌背面渲染 | 11 张全部显示为 ？ ✓ |
| 点可点暗牌 → 翻面选中 | 76 变正面 + selected 高亮，selectedDarkId=76 ✓ |
| 选中后点同花色 → 一起消除 | 暗 83 + 明 13 → 两张都消，72→70，选中清空 ✓ |
| 点异色暗牌 → 选中转移 | 76→56：56 翻面选中、76 翻回背面 ✓ |
| 点已选中牌 → 移入卡槽 | 76 进槽（selectedDarkId 清空、离开棋盘）；槽内同色则随之消除 ✓ |
| 卡槽已有同色 → 点暗牌立即消除 | 明 6 进槽 → 点暗 95 → 两张都消、不进入选中态 ✓ |
| 普通明牌进槽二消（回归） | 98→96 消除 ✓ |
| 遮挡暗牌点击 | 抖动，不选中 ✓ |
| 重来 / 导入 | 选中态复位；普通关 88 张 / 0 暗 / 无标记 ✓ |

> 实测方式：Browser 面板视口初始 0×0 导致点击坐标落空（elementFromPoint 全 NONE）——这是**预览环境问题，非代码 bug**；用 `preview_resize` 给真实视口后，经 `el.click()`（走页面自身 onclick 处理器）全链路验证通过。配对/槽内场景需先自动消几对把暗牌的同色对露出来（深埋钩子的对开局不可点，是正常现象）。

### 四、已知限制（诚实说明）

- **可点判定核对**：查了需求表原始文档，**PROG-0209 明文是「被上方覆盖、被左右两张麻将同时夹住的牌为锁定状态」**（简化文档 _gen_simple_docs 里也写「上方压住：同(X,Y)存在Z更高的麻将→锁定」「左右夹住：同一Z层左右两边都有麻将→锁定」）——**两条规则都生效**。demo 与生成器实现的「被压住 + 同层左右夹住(col±2)」与规格完全一致，无差异。
  - 曾一度误以为有差异：因为 MEMORY.md 索引行写着"仅看被压住"（过时摘要），与记忆正文和需求表都矛盾。已修正索引行。教训：**记忆/摘要与原始需求文档冲突时，以原始文档（PROG-0209）为准**。
- demo 是纯前端验证件：无关卡列表 / 结算 / 音效 / 存档；导入只校验 tiles 非空、偶数张、有 typeId，不校验可解性（工具包出的一定可解，手工改坏的不管）。
- 暗牌**可自由翻面**：翻回背面只发生在选中转移到另一张暗牌时；点已选中的牌是收入卡槽，不是取消（符合 PROG-0224）。已消除的暗牌不再显示。

### 五、临时工具（不属于交付件）

- `.claude/serve_demo.js` + launch.json 的 `game-demo`（8998）——本地静态服务器，仅供 Browser 面板实测用；交付件不含此文件，用户双击 bat 即可玩。

### 心得

- **demo 做"独立自包含"的关键**：去掉对 `outputs/关卡配置` 的硬编码 fetch，把示例关**内嵌进 HTML**——file:// 双击即玩，跟工具包彻底解耦，这才配叫"独立交付件"。
- **交互细节必须逐条对着需求文档实现，不能凭直觉"设计"**：第一版把暗牌做成"翻面→再点进槽"两步（我自己觉得合理），被用户指出与 PROG-0224 不符——需求是"点一下即翻面选中、直接配对、可翻回"。demo 是给玩家/程序看需求的，交互必须 100% 还原文档，不是自创。这次教训：做玩法实现前，先把对应 PROG 文档的 flow/rules/verify 逐条列出来对照。
- **验证时的环境坑**：预览面板视口 0×0 会让所有坐标类交互静默失败（点击落空、渲染位置全负），先 resize 再测；截图在后台面板也拍不了（不合成帧），用 DOM/状态断言替代。都是环境问题，不是代码 bug。
- **下结论前先查原始文档**：一度根据 MEMORY.md 索引行（"仅看被压住"）以为实现比规格严、要用户拍板；查 PROG-0209 原文后确认规格就是「被压住+左右夹」，两者完全一致，虚惊一场。记忆摘要可能过时，关键结论要回原始需求文档核对。

---

## Day 16 再补充 · 8月13日：demo 麻将牌改版（第一版彩色被否 → 返工象牙白）

### 背景

用户反馈「当前的这些方块不是很好看」。调研：`包_资源/` 为空（无美术素材），只有 vita 截图参考。给用户三个选项（纯 CSS 改版 / 接美术素材 / 保持现状），用户选了**「纯 CSS 质感改版（推荐）」**——demo 保持自包含、双击即玩、不改玩法。

### 第一版（彩色 3D 牌身）被否：「太丑了」

第一版把牌身做成**每层一个颜色的彩色渐变**（棕/蓝/绿/红/紫/青），加圆角棱边、玻璃高光、层高投影，花色符号做成真实点数。用户看后直接说**「太丑了」**。
**复盘原因**：参考图里的真实麻将牌身是**象牙白**，彩色牌身是「糖果色」，离"麻将"这个主题太远——颜色本身廉价，不是细节不够。教训：**改观感前先看参考图定"牌身底色"这个大方向，别在小细节上自嗨**。

### 返工：象牙白传统麻将（`outputs/游戏Demo/game_demo_std.html`，纯 CSS + 少量 JS）

**牌身**（关键改变）：
- 象牙白渐变 `#fdfaf1 → #f1e8d3 → #ddcfab` + 暖色描边（不再是彩色牌身）；
- 圆角 15%、斜切棱边（上/左亮、下/右暗）、内凹刻字区、顶部玻璃高光；
- 投影随层高加深：渲染时内联 `--ld:层号`，`calc(2px + var(--ld)*2px)`——高层影子更深，牌堆层次立现；
- **层数不靠牌面颜色**：由左上角 L 徽标标注（牌面 = 玩家最终看到的成品观感）。

**牌面符号（真实麻将配色）**：
- 万 = 大红数字 + 小"万"；筒 = 3×3 **蓝点**布局（真实点数，`TONG_PIPS` 摆点阵）；条 = **带竹节**的竹青条（1~4 一列、5~9 两列，`::after` 画竹节线）；
- 风牌 = 藏蓝单字；字牌 = 中红、发绿、**白板 = 藏蓝描边的白底矩形**（`tileLabelHTML` 特判）；
- 全部 `em` 相对字号，棋盘牌和卡槽小牌共用同一套 `tileLabelHTML()`，卡槽自动缩放。

**槽位牌 / 暗牌**：槽位牌同款象牙白；暗牌背面深蓝 + 问号 + 去玻璃高光；选中态金框保持。

### 实测（Browser 面板，全程无 JS 报错）

| 检查项 | 结果 |
|---|---|
| 加载内置示例（98 张 / 49 对 / 11 暗） | ✓ |
| 各花色渲染 | 筒 pip-grid、条 stick-grid（竹节 rgba(0,0,0,.22)）、风牌藏蓝、发×4 绿、万「五」红 ✓ |
| 牌身 | 象牙白 160° 渐变、15% 圆角、暖色描边、`--ld` 0~4 生效、层色全部移除 ✓ |
| 暗牌翻面选中 → 再点进槽 | 翻面选中 → 再点移入槽位，槽位同款象牙白小牌 ✓ |
| 明牌二消 / 重来（回归） | 97→95 消除；重来复位 98/11 ✓ |

> 截图在后台面板拍不了（不合成帧），以计算样式 + DOM 断言代替视觉确认；交互链路走 `el.click()` 实测通过。

### 心得

- **"变好看"的第一性 = 先定牌身底色方向，再抠细节**：第一版细节做得不少（棱边/高光/投影/真实点数）却输在「彩色牌身」这个根基上。参考图是象牙白，就该先抄这个，其余都是锦上添花。
- **demo 牌面 = 成品观感，工具信息另放**：demo 给玩家/程序看的是"成品长什么样"，层数这种工具信息由 L 徽标承担，别混进牌面；这也是象牙白方案能成立的原因。
- **无美术资源时，纯 CSS 足够还原"传统麻将感"**：象牙白渐变 + 暖色描边 + 棱边高光 + 彩色符号，一条资源不引就有成品味。
- **层高投影用 CSS 变量内联**：一个 `--ld` 变量让阴影深度随层数渐变，可维护、不碰布局/命中测试。

---

## Day 16 又补充 · 8月13日：demo 牌面换成真实麻将素材（user 提供 PNG）

### 背景

用户不满意纯 CSS 画出来的牌（哪怕是上一版的象牙白），把真实麻将图放到了 `outputs/游戏Demo/麻将/` 下让替换。明确说「不一定要一个 typeId 定死同一张麻将，只要把这些麻将分配到不同 typeId 上就行」。

### 资产

`outputs/游戏Demo/麻将/` 共 47 张 PNG（259×322 4:5 透明圆角，象牙白牌身 + 斜切棱边 + 高光）：
- `麻将 (1).png` ~ `麻将 (45).png` — 45 张正面（花色随机）
- `A空白麻将.png` — 白板
- `A麻将背面.png` — 牌背

### 实现

1. **CSS 改造**：`.tile` 从「象牙白+彩色符号的牌身」退化成纯定位/投影容器，里面只放 `<img class="tile-img">` 用 `object-fit: contain` 居中显示真实图。`.tile.dark` 删掉（暗牌直接用背面图当 src）。`.slot .slot-tile` 同样换成 img 容器。层号 L 徽标保留。选中高亮从 `outline` 改成 `drop-shadow`（贴合 PNG 透明圆角的剪影）。
2. **JS 改造**（`game_demo_std.html`）：
   - 新增 `shuffleArray(arr)` Fisher-Yates、`buildTypeImgMap()` 会话内只洗一次、`tileImgSrc(typeId)` 取图（带兜底）
   - `render()` 给每张牌 `<img src="${isBack ? TILE_BACK_IMAGE : tileImgSrc(t.typeId)}">`；`renderSlots()` 同款
   - `init()` 顶部加 `buildTypeImgMap()`；`parseTypeName` 删除（不再需要）
3. **构建注入**：写 `inject_tiles.js` 把 47 张 PNG 读成 base64，替换源码里的 `/*__TILE_DATA__*/` 占位 → 输出 `const TILE_FRONT_IMAGES=[45] / TILE_BACK_IMAGE / TILE_BLANK_IMAGE`。HTML 约 6.5 MB，单文件、双击即玩。
4. **两个坑**：
   - marker 原来嵌在 `// ...处（...）。` 注释行尾，第一次注入后 `const TILE_FRONT_IMAGES = [` 被 `//` 吞了，解析成裸 array literal → `TILE_FRONT_IMAGES is not defined`（浏览器静默、看上去就是全局变量没定义）
   - marker 行尾的 `。` 残留在 `const TILE_BLANK_IMAGE = "...";` 之后，V8 直接 SyntaxError
   修法：把 `// 注释` 和 `const ... = [` 拆两行；删掉 stray `。`。验证手段：node `--check` 抽内联 script。

### 随机映射设计

- 页面打开时洗一次（Fisher-Yates）→ typeId 0..32 各分一张不同图（45 张够，34 张富余）
- `白`（typeId 33）固定 → 白板图
- 同会话内点「重来」/导入关卡 → 映射不变（玩家一关内不会看到同一 typeId 突然变脸）
- 重开页面 → 重新洗（保证每次有新体验）

### 实测（Browser 面板）

- 98 张牌全显、object-fit: contain、L 徽标到位
- 11 张暗牌全部显示背面图，点 clickable 暗牌 → 翻面并选中（data-dark=0）、点第二次 → 入槽（槽内也是正面图）
- 「重来」前后 `typeImgMap` 序列化结果相同 → 映射稳定
- 控制台 0 报错

### 交付件

- `outputs/游戏Demo/game_demo_std.html` — 改后版本（6.5 MB，图片全 base64 内嵌）
- `游戏Demo说明.md` — 第二节牌面样式段已同步改写
- 工具：临时构建脚本留在 `AppData/Local/Temp/tilecheck/inject_tiles.js`（不交付）

### 心得

- **嵌入 base64 的 marker 行最容易踩注释的坑**：未来类似注入要么 marker 单独成行、要么用更独特的不可被注释吞的格式（如 `/*@@INJECT@@*/` block）
- **node `--check` 抽脚本验证是 6MB 大文件最快的语法门** —— 浏览器报「变量未定义」可能是 ReferenceError 也可能是 SyntaxError，先在 node 解语法能省一大圈
- **大文件切片检查要小心偏移**：用 `indexOf` 找位置时区分字符/字节偏移（前面有中文 `<title>` 就会错位）

---

## Day 17 · 8月16日（周六）：编辑器一体化 + 关卡共享工作台上线

### 背景
Demo 和编辑器分开体验很割裂（生成 → 下载 → 导入 demo → 玩），用户希望编辑器里就能预览。另外用户想和另一个策划协作，需要「关卡共享工作台」功能。部署从 Netlify Drop 迁到 GitHub + Netlify 绑定。

### 一、去掉「随机生成一关」按钮
- 用户说"我要的是 JSON 文件"，随机生成功能定位尴尬又占空间 → 直接砍掉
- 主按钮只剩一个：**「🎲 生成关卡」**（给当前形状填色）
- 预览面板按钮简化为：下载 JSON · 保存到工作台 · 关闭（去掉了重新生成）

### 二、编辑器内嵌 demo 预览面板
- 编辑器右侧加了 480px 宽的预览面板，iframe 内嵌同站 demo
- 生成成功后直接 `postMessage` 把关卡推给 iframe 加载，**不用下载、不用跳转**
- 面板顶部：难度信息条（张数 / 对数 / 分数 / 暗牌数 / 层数）+ 三个操作按钮
- demo 端加了 `message` 事件监听：收到 `{type:'loadLevel', level}` → 加载关卡 → 回复 `levelLoaded` 回执
- 关闭后面板折叠（`collapsed` 类，width:0），canvas 重新计算尺寸居中

### 三、关卡共享工作台（Netlify Functions + GitHub 存储）
**架构选型：**
- 第一版想用 Netlify Blobs → 遇到 `MissingBlobsEnvironmentError`（免费计划默认不开 Blobs 环境）
- 改方案 B：直接用 **GitHub 仓库当数据库**，每个关卡一个 JSON 文件存在 `data/levels/` 目录
- 后端函数用 GitHub REST API 读写文件（Content API），零额外依赖、零配置成本

**后端函数**（`netlify/functions/levels.js`）：
- `GET /levels` — 列出所有关卡元信息（从 GitHub 读目录，挨个解析）
- `GET /levels/:key` — 下载单个完整关卡 JSON
- `POST /levels` — 保存新关卡（需要 X-Workbench-Pass 头验证口令）
- `DELETE /levels/:key` — 删除关卡（需要口令）
- 跨域用 `Access-Control-Allow-Origin: *`
- 全部走 `fetch`，不依赖 `@netlify/blobs` 包

**前端（编辑器侧栏）：**
- 「关卡工作台（共享）」折叠区：口令输入框 + 连接按钮 + 状态条
- 连接成功后显示：关卡名输入框 + 保存当前按钮 + 关卡列表
- 每行关卡：加载到预览 / 下载 / 删除 三个操作
- 列表按保存时间倒序

**环境变量（Netlify 站点级）：**
- `WORKBENCH_PASSWORD` — 共享口令（读写都要）
- `GITHUB_TOKEN` — GitHub PAT（repo 权限）
- `GITHUB_REPO` — 仓库路径，如 `zhangjiwei0221/mahjong`
- `GITHUB_BRANCH` — 分支，默认 main

### 四、部署方式变更：Drop → GitHub 绑定
- 用户建了 GitHub 仓库 `zhangjiwei0221/mahjong`
- 从 Netlify Drop 旧项目（`clever-speculoos`）迁到新项目（`brilliant-cheesecake-412496`），绑定 GitHub
- 部署结构：
```
mahjong/
├── index.html                 落地页
├── demo/                      游戏 demo（6.3 MB，麻将图 base64 内嵌）
│   ├── index.html
│   └── 示例关卡/
├── editor/                    关卡编辑器 + 生成器
│   ├── index.html             编辑器
│   └── generator.js           形状生成 + 填色算法（浏览器版）
├── docs/                      说明文档
├── netlify/
│   └── functions/
│       └── levels.js          工作台后端函数
├── netlify.toml               部署配置（publish + functions 目录）
├── package.json               （目前只作占位，函数无 npm 依赖）
└── 关卡工作台_部署指南.md
```

### 五、踩过的坑
1. **Netlify Blobs 在免费环境里不能直接用**：`getStore(storeName)` 报 `MissingBlobsEnvironmentError`，要手动传 siteID + token 或开高级功能。换成 GitHub 存 JSON 更轻、更透明。
2. **环境变量加了但函数说没有**：最常见原因是"加完变量没重新部署"，旧部署用的还是旧变量。改完变量后必须 Trigger deploy 一次。
3. **仓库名/路径易错**：`GITHUB_REPO` 的值要是 `用户名/仓库名`（不是完整 URL）。
4. **git push 在 Git Bash 里连不上 GitHub**：Windows 上 Git Bash 的网络不走系统代理（即使浏览器开了梯子也没用）。解决方案：要么在 Git 全局配置里设代理，要么直接用 GitHub 网页上传（UI 拖文件更稳）。

### 心得
- **"共享存储"这个需求，小团队最便宜的方案就是直接用 Git 仓库当数据库**：免费、有版本历史、看得见摸得着、不需要学新服务。两个人协作、几十几百关，足够用。
- **Netlify Drop 适合单文件静态站，一旦涉及函数/环境变量就该迁 GitHub 绑定**：Drop 虽然快，但后续扩展几乎零空间。一开始就用 GitHub 绑定 + Netlify，后续加功能最顺。
- **用户要的"一体化"不是"功能变多"，是"流程变顺"**：原来 3 步（生成 → 下载 → 导入 demo）现在 1 步（生成 → 直接在旁边玩），按钮反而更少了。

---

## Day 17 补充 · 8月16日：生成拆成两个按钮 + 保存反馈 + 工作台部署成功

### 一、生成功能拆成两个（用户说"随机生成没用"→ 又发现"堆层没了"）

上一版把「随机生成一关」删了，结果用户发现：画一个底层后，生成出来只有一层（因为填色只给已有形状上色，不堆层）。用户其实要的是"只画底层、自动往上堆成塔"。于是拆成两个明确按钮：

| 按钮 | 机制 | 底层 |
|---|---|---|
| **🎯 按形状生成** | `fillEditorShape`（给当前形状填色） | 你画几层就几层 |
| **🏗 堆塔生成** | `generateStackFromShape`（新增，以你画的形状为模板，`generateFromTemplate` 往上堆层再填色） | 只画底层，自动堆塔 |

- 命名讨论：用户问"我只提供底层一层，自动往上堆，怎么命名好" → 定了 **「堆塔生成」**（麻将关卡就是一座塔，贴切）；另一个叫「按形状生成」。
- 「堆塔生成」旁边加了个**「自动堆层数」输入框**（默认 5，2–7）。
- 实测：画 10 张 L0 底层 → 堆塔 → 5 层 26 张（10/7/4/3/2 金字塔递减）→ 126 分 → 预览能玩。✅

### 二、保存按钮"点了没反应"——修复

- 根因排查：按钮 id / 函数都对，但用户看不到反馈。两个问题：
  1. 反馈文字只写在工作台顶部那一行小状态条里，不显眼；
  2. 如果 `currentLevel` 为空（没生成就点保存），旧代码 `if (!currentLevel) return;` **静默返回**，完全无提示。
- 修复：改成**按钮级反馈**——点保存 → 按钮变灰"保存中…" → 成功变绿"已保存 ✓" → 失败变红"保存失败+原因"；没生成关卡时明确提示"先生成关卡才能保存"。
- 顺手删掉了预览面板右上角那个**没接线的「☁ 保存到工作台」死按钮**（误导），只留左侧工作台列表的「保存当前」。

### 三、工作台部署踩坑记录（GitHub 存储方案，最终跑通）

1. **第一版用 Netlify Blobs → 502**：日志报 `MissingBlobsEnvironmentError`，免费计划默认不开 Blobs 环境，`getStore()` 拿不到 siteID/token。→ 放弃 Blobs，**改方案 B：直接用 GitHub 仓库当数据库**（每关一个 JSON 存 `data/levels/`，函数用 GitHub REST API 读写，零依赖）。
2. **函数写好、变量设好、还是 502 / "GITHUB_REPO 未配置"**：
   - 原因一：**加完环境变量没重新部署**，旧函数还是拿旧环境。→ 必须 Trigger deploy。
   - 原因二：最终是 **Netlify 部署没跟上 / 浏览器缓存旧函数响应** → 用 **Trigger deploy → Clear cache and deploy site** 清缓存重部署后成功。
3. **git push 连不上 GitHub**：Windows Git Bash 的 git **不走系统代理**（浏览器开梯子没用），curl 28 超时。解决：用户网页拖拽上传（web upload），或让 git 走代理。
4. **仓库被双端提交导致 push 非 fast-forward**：用户网页上传 `levels.js`/`package.json` 产生了远端提交，本地 push 被拒 → `git pull --rebase` 合并后再推成功。

### 四、最终环境变量（Netlify 站点级，免费）

- `WORKBENCH_PASSWORD` 共享口令
- `GITHUB_TOKEN`（PAT，repo 权限）
- `GITHUB_REPO` = `zhangjiwei0221/mahjong`
- `GITHUB_BRANCH` = `main`

注意：Netlify 的 **Shared environment variables 是付费功能**，别加错到团队级；要用站点级（Site settings → Environment variables）。

### 心得

- **"AI 能不能帮我推 GitHub"的边界**：关键在**凭证在不在本机**。用户给 Codex 授权过 → 凭证存本机 → Codex 能推。这台机器之前没有 gh/凭证，推不了；用户自己 push 一次（弹浏览器登录存进 Windows 凭据管理器）后，凭证就在了，之后我能直接帮他 push（用户明确指定仓库即可）。
- **小团队"共享存储"最省事的方案 = Git 仓库当数据库**：免费、有版本历史、看得见摸得着、零学习成本。两个人几十几百关绰绰有余。
- **填色不堆层**：`fillEditorShape` 只给已有形状上色；堆层是 `generateFromTemplate` 的事。功能拆分时别把用户隐含的"堆层"期待一起删掉——用户说"随机生成没用"是指"从模板生成",不是"自动堆层"。

---

## Day 17 又补充 · 8月16日：空白块修复 + 按钮改名 + 批量生成

### 一、demo 里"空白麻将块"的根因（typeId 错位）

现象：demo 里有些麻将块是**纯空白**（像没贴图）。排查后发现是**生成器与 demo 的 typeId→花色 对齐不一致**：

- 生成器 `NAMES` 顺序 = 东,西,南,北,中,发,白 → 「发」= typeId 33
- demo 的 `TYPE_WINDS_DRAGONS` = 东,南,西,北,中,发,白（顺序不同）→ 它以为 typeId 33 是「白」→ 把 typeId 33 映射到 `A空白麻将.png`（纯空白底图）

结果：生成器产的"发"牌（typeId 33）在 demo 里全部显示成空白块。

修法：demo 的 `buildTypeImgMap()` **删掉"白→空白图"特例**，所有 typeId 都从 45 张真实牌面随机分配；`A空白麻将.png` 只当底图不作牌面。验证：含 typeId 33 的关卡渲染 0 空白块、0 破图。✅

### 二、生成按钮改名（用户要求更直白）

- 「按形状生成」→ **「按当前形状生成」**
- 「堆塔生成」→ **「按当前底座搭建后生成」**

### 三、批量生成 + 难度分范围控制

用户反馈："一下生成一个"用着难受，且分数没法控制 → 加**批量生成**：

- 生成关卡区新增三组控件：
  - **目标分数** `0~999`（两个输入框，默认全开）
  - **生成数量**（1–50，默认 5）
  - **⚡ 批量生成** 按钮
- 逻辑：以当前底座为模板，`generateStackFromShape` 反复生成，只收**分数落在 [min,max]** 的，直到凑够数量（每个目标最多试 60 次，超限提示放宽范围）。异步 `setTimeout(step, 0)` 让出主线程保持界面响应。
- 结果弹窗：每行显示 `分数 / 张数/对数 / 层数 / 暗牌` + 「预览」「下载」按钮，顶部「⬇ 下载全部」（逐个延时触发防浏览器拦截）。
- 预览 → 关闭弹窗、加载进右侧 demo、`currentLevel` 置为该关（可继续保存到工作台）。
- 实测：宽范围 100~200 出 4 关、窄范围 120~130 出 3 关全是 126 分、预览 iframe 加载 26 张牌。✅

### 心得

- **typeId↔花色对齐是双端最容易埋雷的点**：生成器、编辑器、demo 各自维护一套花色→数字映射，顺序稍不一致就出现"看起来是 bug 的牌"。下次应该抽一个共享的映射表（或在需求文档里固定顺序）而不是各写各的。
- **批量 + 分数过滤对策划是真需求**：策划要按难度梯度出几十关，"一次一关"太慢，"分数可控"才能排难度表。

---

## Day 17 又补充2 · 8月16日：探测分数 + 不对称底座 + 生成流程重构

### 一、探测本底座可出分数（📊）

用户说"不知道分数的上下限" → 加「📊 探测本底座可出分数」按钮：对当前底座生成 20 个样本，显示 **最低 / 最高 / 平均分**，并**自动填入目标分数输入框**。实测 10 张底座：最低 69 / 最高 126 / 平均 106。

### 二、不对称底座支持（去掉硬性对称要求）

用户放九宫格报 `[对称] L0(3,-5)` → 生成器要求底座左右对称（镜像塔是游戏惯例），但用户觉得没必要。改：

- `generateFromTemplate` 加 `mirror` 选项：`mirror=false` 时 `generateLayer`/`expandMirrorWithLimit` 不做镜像展开
- `validateShape` 加 `skipSymmetry` 选项
- `generateStackFromShape` **自动检测底座对称性**：对称 → 正常镜像堆塔；不对称 → 关镜像 + 跳过对称校验，按实际形状堆
- 踩坑：不对称时层不镜像翻倍，**总牌数可能变奇数** → 生成后从顶层删一张凑偶（`toEditorJSON` 的奇数处理对不对称形状无效，因为它找"轴上牌"或镜像对，不对称时找不到）
- 九宫格（9 张）是**奇数**，二消无法配对 → 明确报错"底座是 9 张（奇数），请加/删一张"（数学硬约束，不是 bug）
- 实测：不对称 12 张底座 → 成功生成 14 张可玩关卡（44 分）✅

### 三、生成流程重构（常驻结果列表 + 批量模式）

用户两点不满：
1. **"批量生成到底按形状还是按底座？没规范"** → 加 **批量模式下拉框**（按底座堆塔 / 按当前形状），和"数量""分数"一起明确
2. **"点预览弹窗就关了，其他结果看不到"** → 弹窗改成**右侧面板常驻的「📋 生成结果」列表**：
   - 批量生成的**所有结果**都在列表里，每行 `#n 分数 张/对 层 暗` + 预览/下载
   - 点某行预览 → iframe 加载，**列表不关**，来回切换
   - 单个生成也进列表（生成 1 个 = 列表 1 项 + 自动预览），当前预览行红色高亮
   - 顶部「⬇ 下载全部」「清空」

最终生成规范：**形状来源（当前形状/底座堆塔）× 数量（单个/批量）× 分数（单个不控，批量可设）**。

### 四、Git Bash 连不上 GitHub 的解法（重要）

问题：Git Bash 的 git **不走系统代理**，即使浏览器/梯子开着也 `Could not connect to github.com:443`。用户终端能推是因为它走了代理。

发现：本机梯子监听 **127.0.0.1:7897**（Clash 系默认）。验证方法：
```bash
for p in 7890 7897 10809 1080 8888 10808 2080; do
  timeout 3 curl -s -o /dev/null -w "port $p: %{http_code}\n" --proxy http://127.0.0.1:$p https://github.com
done
```
推送时带代理（一次性）：
```bash
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push
```
可以永久配置（一次性写入，以后免带 flag）：
```bash
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

### 心得

- **"对称"是设计约定不是游戏约束**：镜像塔好看、且镜像层天然偶数（配合二消），但它不该成为挡用户的门槛。让工具自动检测、能对称就对称、不能就按实际来，比报错强。
- **奇数张是数学硬约束**：二消 9 张永远有一张落单。这类约束要给用户**能行动的提示**（加一张/删一张），而不是干巴巴报错。
- **UI 从"弹窗"走向"常驻面板"**：批量结果的预览不该破坏浏览上下文。常驻列表 + 点选预览，让策划能对比多个候选，比弹窗顺畅得多。

## Day 18 · 8月17日（周日）：编辑器 v4 重构 + 工作台独立成页 + 试玩计时 + 难度公式 v2

### 一、编辑器 v4.0：浅色重构 + 编辑刚需交互（外观+交互全面改）

- 两个页面（深色编辑器 + 浅色批量工具）风格统一为**浅色卡片风**：米白底 + 白卡片 + 赭金强调色，红色只留给危险操作（原来"激活层=红、删除确认=红"混用消除）
- 布局重构：顶栏（导入/导出/清空 + 撤销重做 + 视图开关 + 帮助弹层）、侧栏改可折叠卡片、画布底部状态栏（坐标/层/支撑/计数/缩放）
- 新交互：**撤销/重做**（Ctrl+Z/Y，笔画级快照）、**右键拖拽擦除**、**滚轮缩放**（以光标为中心）、空格/中键平移、Toast 替代 alert、**localStorage 自动保存**（刷新重开自动恢复）、图层复制
- 修复：本地预览 iframe 指向不存在路径必 404 -> serve_editor 加 `/demo/` 路由映射到旁边的 `游戏Demo`

### 二、棋盘收窄 + 默认填满（用户反馈两轮打磨）

- 第一轮：画布 14 格收到 12 格（牌区实际宽度），棋盘背景/网格紧贴牌区，四周只留 6px
- 第二轮（用户："滚轮上下滑看到大片空白"）：默认视图改为**填满可视区**（去掉 6% 边距系数），滚轮从"上下平移"改成"缩放"（不再扫空区），画布底色调深让棋盘轮廓突出
- 踩坑：格子边界点击会偏 1 格--浮点误差 `205.99999…/40 = 4.9999…` 被 `floor` 到上一格，`pixelToCell` 加 `+1e-6` 修正

### 三、工作台三轮演进：侧栏进度板 → 砍状态 → 独立成页

1. **v1 进度板**：每关加 order（第几关）+ status（制作中/待测试/已完成）三态徽章，顶部计数一览
2. **用户反馈**："关卡名和第几关分不清哪个代表关卡"、"状态没必要、策划文字也没必要"、"几百上千关挤在左下角太小不好看"
3. **v2 定稿**：
   - **工作台独立成页**（`workbench.html`）：口令连接、汇总计数、搜索、5 种排序、已排号/未排号筛选、表格行内直接改序号和名称、下载/删除；状态和作者字段砍掉，保留 关卡名（身份）+ 序号（排序）双字段
   - 编辑器侧栏工作台卡片移除，顶栏加「📋 工作台」入口；**保存挪到右侧预览面板**（生成 -> 试玩 -> 直接保存，口令存 sessionStorage 两页共享）
   - 后端 levels.js 加 PATCH /levels/:key（只改元数据不动 tiles）
   - **编辑器三栏（侧栏/画布/预览）之间加拖拽拉伸条**，宽度记忆

### 四、试玩计时：验证难度分的闭环工具

- demo 侧：`loadLevel` 记录 `levelStartTime`，通关（checkWin）时 `postMessage({type:'playFinished', durationMs})` 回传
- 编辑器侧：收到后挂到 `currentLevel.playTimeMs`，状态栏显示「已通关 · 试玩 Ns · 难度 M 分」；保存到工作台时随 POST 入库
- 工作台表格「试玩」列显示；后续用"分数 vs 实际通关时间"的相关性校准难度公式

### 五、难度公式 v2：钩子从"有/无开关"改为按数量计分（本次核心）

**问题发现过程**：用户连续追问"同一底座探测分数为什么每次不一样""钩子是什么""1 个钩子和 30 个钩子真的同分吗"，逐步挖出 v1 的结构性缺陷：

- v1 公式 `min(钩子数/总牌数×100, 1) × 30`：百来张牌时只要有 1 个钩子占比就 >1、直接拿满 30 分——**钩子项二值化**，1 个钩子和满盘钩同分
- 三重验证确认：① 公式反算之前批量生成的两关（123/121 分）逐位吻合；② 现场生成 6 关，1 个和 2 个钩子都拿 30 满分；③ 文档第六节自己承认"钩子/暗钩子易触顶"
- 顺带查清**探测分数波动的真正主因**：不是花色，而是堆塔本身随机（每层张数 0.7~1.3 抖动 + L2+ 随机布局），同底座每次塔形都不同，可点率随之波动
- 花色分布也确认了：倒推法随机分配、每花色上限 3 对，34 对摊 34 花色是生日悖论（全一对概率约 10 万亿分之一），典型分布约 12 种空缺/12 种一对/7 种两对/2 种三对

**v2 修改**（三处副本一次改齐：浏览器 generator.js / Node generate_fill_std.js / 仓库 editor/generator.js）：

```
钩子项：   min(钩子数 / 5, 1) × 30    每 1 个钩子 +6 分，5 个拿满
暗钩子项： min(暗钩子数 / 5, 1) × 20  每 1 个暗钩子 +4 分，5 个拿满
```

- 分母 5 是实测校准：80~110 张常规关卡钩子数观测 0~5 个（中位 2），5 个封顶正好覆盖
- **新旧对比实测**（同底座）：旧公式 1~5 个钩子全部挤在 121~127 分；新公式拉开为 86/97/106/116/121，每个钩子约 10 分台阶（钩 6 + 暗钩 4）；0 个钩子和 5 个钩子的关分数不变
- **v1 与 v2 分数不可混排**，旧批次需整批重新生成；《难度分说明.md》全文重写为 v2 版，《使用说明.md》公式段同步

### 六、踩坑记录

1. **git push 网络时好时坏**：重试多次才成功；遇远端领先（网页端工作台更新产生新提交）要先 `git pull --rebase` 再推。另：重试脚本要用退出码判断成败，`git push 2>&1 | tail` 管道后 `$?` 永远是 0，会误报成功
2. **Edit 工具改中文 md 匹配失败**：文档里的破折号是全角"--"，肉眼看着和"--"一样，逐字节对比才发现；长段落改不动就整文件重写
3. **画布元素没 id**：JS 用 `getElementById('sidebar')` 取 null 崩掉整页脚本，HTML 加 id 解决
4. **浏览器把合成 MouseEvent 的 clientX 取整**：格子边界点的 hit-test 有 ±1px 固有歧义，实际点牌中心不受影响，epsilon 修正后无需再纠结

### 心得

- **"有/无"型指标是难度公式的隐形陷阱**：占比封顶在百张规模下悄悄退化成二值开关，分数看着连续、实际缺了一个维度。数量型计分（每钩+6、5 个封顶）才配得上"钩子=贯穿整局记忆负担"这个设计初衷
- **用户连着追问就是最好的 code review**：这次 v2 不是主动重构，是"为什么每次不一样→钩子是什么→1 个和 30 个真同分？"三连问逼出来的。答不上来就实测，实测比读代码有说服力
- **验证公式正确性最快的办法是拿旧数据反算**：用之前批量日志里的真实关卡（可点/钩子/层）反算分数，逐位吻合就说明公式没读错，再谈改
- **试玩计时让难度公式第一次变成可证伪的东西**：分数和通关时间的相关性就是公式的"成绩单"，以后每次调权重都能用数据说话

### 七、需求表新增：主页每日奖励（抖音侧边栏）+ 七日签到（PROG-0105/0106）

- **PROG-0105 每日奖励（抖音侧边栏）**：主页入口，点击后判断启动来源——侧边栏启动直接弹窗领当日奖励；非侧边栏启动弹图文引导（示意抖音侧边栏入口），从侧边栏重进后可领。每自然日限领 1 次，非抖音环境降级为直接可领（便于测试/跨平台）
- **PROG-0106 七日签到**：弹窗 7 格奖励位（第 7 天大奖），每日可领 1 次，7 天一轮领完重开；**中断不清零**（暂定，待确认）；进度/最近签到日期存档（联动 PROG-0400）
- 编号：主菜单系列 0100~0104 已用 -> 新增 **PROG-0105/0106**（模块「运营活动」）；美术配套 **ART-0108~0111**（奖励/签到入口按钮、签到弹窗、侧边栏引导弹窗），从 0108 起编避开 regen_v3 脚本里已用过的 ART-0107（排行榜入口）
- 产出：两张清单表 xlsx 各追加行（脚本 `_add_daily_bonus_and_checkin.cjs`，ExcelJS 追加不破坏原表）+ 详细程序文档 `简化程序文档/PROG-0105.docx、PROG-0106.docx`（脚本 `_gen_PROG_0105_0106_docx.cjs`）
- 待确认项：① 签到中断是否清零（现暂定不清零）② 每日奖励在非抖音平台是直接可领还是隐藏入口（现暂定直接可领）③ 两功能奖励内容数值表（day1~day7、每日奖励）待配置
- 顺带发现（未处理）：程序清单表里 **PROG-0222/0224/0225 只有 docx、未进 xlsx**；表内有一行重复编号（PROG-0219 新手引导，与后面「暂停与继续」撞号，疑似应改编为 PROG-0225）；旧 `_gen_prog_docs_full.cjs` 第 2279 行写入文件名固定为同一个文件（bug，未修）——待下次统一清理

## Day 18 补充 · 8月17日：Cloudflare 迁移完成 + 试玩计时最佳成绩 + 版本检测 + 对称修复

### 一、从 Netlify 迁移到 Cloudflare Pages（背景：Netlify 免费构建额度耗尽）

- **Netlify 300 积分/月耗尽**，生产部署暂停，恢复要等整月。用户等不了 -> 迁移
- 迁移内容：
  - 后端函数从 Netlify 格式（`exports.handler`）改写为 Cloudflare Pages Function（`functions/levels/[[path]].js`，`onRequest` + `context.env`）
  - 前端 API 路径从 `/.netlify/functions/levels` 改为 `/levels`
  - GitHub 存储逻辑**原样保留**，数据零迁移
- 首次部署踩坑：Cloudflare 初始化向导把仓库根目录（含 `node_modules` 144MB workerd）全当静态文件上传 -> 撞 25MB 单文件上限。修法：改用 Pages 仪表盘标准建法（Build command 留空、Output directory `.`）
- **环境变量配置**（通过 wrangler CLI 自动化，不需要用户手动复制）：
  - `npx wrangler login` -> 用户点一次浏览器授权
  - `GITHUB_TOKEN` 从电脑凭据管理器 `git credential fill` 直接取出（不需要用户手动找）
  - `GITHUB_REPO` / `GITHUB_BRANCH` / `WORKBENCH_PASSWORD` 逐个 `wrangler pages secret put`
  - 关键：设完变量必须推空提交触发重新部署，否则变量不生效
- **验证链路**：GET 列表 → POST 测试关 → DELETE 清理 → 全通

### 二、试玩计时完整闭环（这次真正做好）

**用户反馈两轮打磨：**

1. 第一版：关卡加载就开始计时 -> 用户：「应该从真正开始玩才计时」
   - 改为 demo 内加 `gameStarted` 消息（首次点击牌时回传）
2. 第二版：首次点击牌开始计时 -> 用户：「改成按钮更好」
   - 改为**「⏱ 计时并开始游戏」按钮**（全屏遮罩盖住 demo，点击才开计时）
3. 第三版：通关后必须立刻保存才能记录 -> 用户：「想多测几次怎么办」
   - **最佳成绩**：demo 通关显示「本局 X · 最佳 Y」，localStorage 持久化（按关卡指纹分键）
   - **再玩一次**：通关后出现按钮，重玩同一关不重新生成
   - **编辑器侧记录试玩历史**（`levelPlayHistory`），保存时自动用**最佳成绩**（`Math.min`），预览面板显示最近 5 次
   - **demo 重玩 -> 编辑器计时器重置**（`levelLoaded` 消息触发）

### 三、UI 打磨（策划反馈）

- 删牌面 L 层标签（挡图案）
- 上层牌每层左偏 3px（露出下层牌边，直观看出堆叠高度）
- 保存栏改两行：第1行 口令+关卡名，第2行 第几关（带标签）+保存+收起（原来一行挤 5 个控件）
- 工作台从编辑器顶栏改为**新窗口打开**（`window.open`，编辑器不丢）

### 四、堆塔对称性 bug 修复（6×8 底座报错）

**现象**：对称底座 + 按底座生成 -> 约 20-30% 概率报「堆层校验失败: [对称] L4(x,y)」

**排查过程**（完整走了一遍死胡同才找到）：
1. 先怀疑校验和底座检测轴向不一致（std col-mirror vs internal row-mirror）-> 改了三处，反而更糟
2. 用 Node VM 直跑生成器（绕过浏览器缓存）复现 -> 确认是真 bug 不是缓存
3. 逐步排除法：直接调 `generateFromTemplate` + `validateShape` **0 失败** -> 缺口在 `generateStackFromShape` 独有的逻辑
4. **根因**：奇数张时的"删顶层一张凑偶"逻辑（第 660 行）删的是**任意一张**，破坏镜像对称。删掉 A 但留着 A 的镜像 B -> B 缺对称伙伴 -> 校验失败
5. **修法**：奇数时成对删除（一张 + 它的镜像对称牌）；顶层找不到镜像对则删中轴线上的牌（row=0，自身对称）

**验证**：100 次生成 0 失败（修复前 5-10 次/30 失败）

### 五、版本更新自动提示（双人协作的通知问题）

- 编辑器每 5 分钟 `fetch('/version.json')` 比对 `window.APP_VERSION`
- 版本不一致 -> 顶部弹横幅「🛠 工具已更新（新版本 xxx）· 请强刷页面（Ctrl+Shift+R）」
- 另一位策划不需要口头通知，页面自动提醒
- **部署纪律**：每次推新代码必须同步更新 `version.json` 和 `window.APP_VERSION` 两处 commit hash

### 六、Cloudflare Pages 免费额度（结论：完全够用）

| 项 | 限制 | 评估 |
|---|---|---|
| 构建分钟 | 500/月 | 每次部署约 30s-1min，月 500 次随便用 |
| 带宽 | **无限** | ✅ |
| Functions 请求 | 10 万次/天 | 两人用工作台绰绰有余 |
| 文件数 | 25,000 | 仓库才 17 个 |
| 构建并发 | 1 | 顺序排队，不影响 |

**与 Netlify 的区别**：Cloudflare 额度用完**网站还能跑**（只是不能部署新版），Netlify 会停整个站。

### 踩坑记录

1. **浏览器缓存旧 generator.js**：改完代码 reload 后仍报旧错误。`md5sum` 确认服务器文件一致 -> 强制重启 preview server 才清掉。教训：本地调试时注意缓存
2. **Edit 工具修中文 md 的全角破折号**：又踩了一次，长段落整文件重写
3. **Cloudflare 设密钥后不自动部署**：`wrangler pages secret put` 有时触发新部署有时不触发；最稳的办法是设完推一个空提交
4. **远端有别人提交**：另一位策划在工作台保存关卡会通过 GitHub API 产生新 commit，push 前先 `pull --rebase`

### 心得

- **最佳成绩比最后一次更有价值**：策划要的不是"我这次玩了多久"，是"这关到底多难"-> 取多次的最佳才是在测难度而不是测手抖
- **版本检测是双人工具的刚需**：改完不通知 = 改了白改。一个 `version.json` + 定时 fetch 就解决了，不需要 WebSocket
- **排查对称 bug 的方法论**：缩小范围（去掉外层函数直调核心）-> 隔离出问题段 -> 读代码找根因。这次花了太多时间在错误假设（轴向不一致）上，直接复现+二分法更快

---

## Day 19 · 8月17日（周一）：生成参数体系复盘 + 权重校准正式搁置

### 一、难度权重校准测试方案 → 搁置（重要决策）

**背景**：用户要求出一份"每个维度单独拉满、测通关时间、反推权重"的测试方案。方案已生成(docx)后,用户反思测试可行性,连续提出两个关键质疑:

1. **"一个维度拉到极端后,其他维度设多少?"** → 理论上设最低值才能隔离,但生成器维度之间**相关**(层数拉满 → 钩子自然变多),做不到完全独立
2. **"生成 3 关测通关时间,能算权重吗?"** → 诚实答案:只能给粗略排序,给不了精确权重;3 关样本太小,维度相关性会淹没信号

**决策**: 放弃一步到位的权重测量,**搁置为待执行项目**。三个备选路径留给未来:
- **A. 大样本**:每维度 5-8 关 × 5+ 人,回归分析(最准但最费工)
- **B. 粗略初版 + 上线迭代**:每维度 3 关 × 2-3 人定初版,上线后据玩家数据持续调(当前最务实)
- **C. 设计判断 + 小规模验证**:凭机制分析定权重,5-6 关验证"公式分排序 ≈ 实际难度排序"

**暂定走 B**:先用当前权重(钩 30 / 暗钩 20 / 可点 40 / 层 8)作为初版,上线后用"难度分 vs 最佳通关时间"的相关性做后续校准。试玩计时闭环已就绪,只差数据积累。

### 二、生成参数合理性复盘(核心问题)

**用户提出的关键问题**:"给定底板后设钩子=5、暗钩=5,但实际没这么多牌可摆,怎么办?目标数是否该改成百分比?"

**分析**:
- 钩子产生条件:同一花色一对牌层差 ≥ 2
- **硬约束(形状)**:扁平底板(所有牌在 L0/L1)层差最大为 1 → 钩子恒等于 0,设多少目标都没用
- **软约束(花色分配)**:backwardFill 随机分配,钩子数范围有限
- 当前处理:重试 30 次达不到就认输返回最后一次 → 出现"目标钩 5 实际钩 2"

**百分比方案的评估**:
- ✅ 解决规模适配:30 张和 110 张底板,50% 自动缩放数量
- ❌ 不解决结构不可能:扁平底板 0% 之外的任何目标都不可达

**决定方案:百分比 + 尽力匹配**:
1. 钩子/暗钩目标从绝对数(0~10)改成百分比(0~100%,占"总对子数"比例)
2. 容差 `TOL_HOOKS` 从绝对值 ±1 改成相对值 ±10%
3. 生成逻辑从"达到才停"改成"重试 N 次后取最优",清晰展示偏差
4. 现有 `structCompareText` 已能显示"钩5(+2)"这类偏差,只需改目标语义

**已实施**(用户确认后完成):
- `generator.js`:`evaluateDifficulty` 加入稀缺度加权 `effHooks = Σ(钩子实例数 / 该花色对子数)`,独占型钩子满分、泛滥型钩子打折;新增 `effHooks`/`effDarkHooks`/`totalPairs`/`hookDensity`/`darkHookDensity` 字段
- `index.html`:(a) 钩子/暗钩滑轮从绝对数改百分比 0~100%(默认 30%/20%);(b) 删掉可点旋钮(随机生成自然体现);(c) 容差改 ±10%;(d) 单关生成改"重试 30 次取最优";(e) 结果对比文字显示百分比
- `workbench.html`:/暗钩列改显示密度百分比;汇总改"平均钩子密度"
- `functions/levels/[[path]].js`:后端返回新增 `hookDensity`/`darkHookDensity`

**稀缺度加权的实测效果**:

| 场景 | 原始钩子 | 有效钩子 | 分数 |
|---|---|---|---|
| 3 个独占型(每型只有 1 对,被埋了就没替代) | 3 | **3.0** | 84 |
| 3 个泛滥型(每型有 3 对,被埋了还有替代) | 6 | **2.0** | 61 |

旧公式会把上面两行算成"3 钩 vs 6 钩",但实际上独占型更难(每对被埋都是硬瓶颈)。

### 三、当前生成参数体系汇总(截至 Day 19)

| 参数 | 控件 | 范围 | 备注 |
|---|---|---|---|
| 暗牌覆盖率 | 滑轮 + 数值 | 0~100% | — |
| 暗牌数量 | 滑轮 + 数值 | 0~100%(占总牌) | — |
| 钩子目标 | 滑轮 + 数值 | 0~100%(占对子数) | 含稀缺度加权 |
| 暗钩目标 | 滑轮 + 数值 | 0~100%(占对子数) | 含稀缺度加权 |
| 可点击率 | —(已删除) | — | 随机生成自然体现,公式保留计算 |

### 四、同日其他进展(简要)

- 暗牌双旋钮(覆盖率 + 数量)已在工作台/编辑器落地
- 难度公式 v2 已稳定运行(钩子数型计分)
- 权重校准 docx 已产出,归档备查

---

## Day 20 · 8月18日（周二）：Demo 动画 + 生成参数重构 + 冲突检查修复

### 一、Demo 试玩动画(三项改进)

**新增飞入卡槽动画**:
- `flyInToSlot(tile, slotIdx, onDone)`:克隆一张牌从棋盘当前位置飞向卡槽,动画结束后正式入槽
- CSS `@keyframes flyIn`:从起点位移到终点+缩小+淡出

**新增匹配消除动画**:
- `@keyframes matchedOut`:配对牌先缩放弹跳再消失(0.3s),然后才从卡槽移除
- `checkMatch` 中先加 `matching` 类播放动画,setTimeout 后再清除

**暗牌匹配流程修正**:
- 原:暗牌匹配时直接进槽+消除(看不到过程)
- 现:先翻开(设置 selectedDarkId,播放 flipIn 动画 320ms),再飞入卡槽

### 二、Demo 交互修复(三项问题)

**问题 1:点击后牌残留棋盘阻挡下方牌**
- 原:`placeInSlot` 等飞入动画结束后才把 tile 加入 slotTileIds
- 现:点击后**立即** `slotTileIds.add(tile.id)` + `render()`,飞入动画只作视觉反馈
- 实测:98 张 → 点击后立即 97 张

**问题 2:失败时没有"再来一次"按钮**
- `showMessage` 新增第三个参数 `showRetry`
- 为 true 时在消息下方添加 `<button onclick="restartLevel()">🔄 再来一次</button>`

**问题 3:重来后计时器不归零**
- `stopPreviewTimer` 新增 `el.textContent = '⏱ 00:00'` 重置显示

### 三、生成器冲突检查修复(核心)

**问题**:原冲突检查把间距为 1 的相邻牌误判为"重叠"
```js
// 原(错):边距 < 2 就冲突 → 相邻牌全被判死
if (arr[i].row < arr[j].row + 2 && arr[i].row + 2 > arr[j].row && ...)
```
- 田字格 2×2:修复前 0/10,修复后 **10/10**
- 实心 4×5:修复前 0/5,修复后 **5/5**

**修法**:只禁止完全重合,允许相邻
```js
if (arr[i].row === arr[j].row && arr[i].col === arr[j].col) { ... }
```

### 四、堆塔死形重搭

**问题**:`generateStackFromShape` 搭塔后如果形状不可解,旧代码用同一死形重试 100 次白白浪费

**修法**:
- 外层循环最多 30 次:每次重新 `generateFromTemplate` 搭新塔形
- 内层 `backwardFill` 最多 50 次填色
- 校验失败直接 `continue` 重搭
- 错误提示改为"底座形状可能导致无解,试试调整底座形状或减少堆层数"

### 五、批量生成不再为空

**问题**:默认暗钩目标 20%,多数形状实际 0~5%,±10% 容差永远命中,批量出 0 关

**修法**:加入候选池机制
- 分数范围内但结构未命中的关卡存入 `candidates` 池
- 最终未凑够数时,按 `structMismatch` 排序取最接近的
- 状态栏提示"部分取最接近目标的"

### 六、生成参数重构(删冗余)

**问题**:暗钩目标 = 钩子目标 × 暗钩比例,三者独立设置会矛盾

**改动**:
| 改动 | 说明 |
|---|---|
| 删除"暗钩目标"滑块 | 消除冗余 |
| "暗钩覆盖率"→"暗钩比例" | 更直观 |
| 新增"≈ 暗钩目标"只读显示 | 自动算出 = 钩子目标 × 暗钩比例 |

### 七、帮助按钮优化

- 问号按钮加大:18px→22px,红色边框+红色文字,更醒目
- 新增"目标分数"旁问号,弹出难度分计算公式:
  ```
  难度分 = (1-可点率)×40 + 层数×8 + min(有效钩子/5,1)×30 + min(有效暗钩/5,1)×20 + 10
  ```

### 八、预览面板修复

**问题**:没生成关卡就能点"计时并开始游戏",计时空跑

**修法**:
- `gameStartOverlay` 默认加 `hidden` 类
- `startGame` 开头加 `if (!currentLevel) return`
- 只在收到 `levelLoaded` 后才显示按钮

---

## Day 20 补充 · 8月18日（周二）：生成结果参数显示 + 批量序列号 + Demo 动画优化

### 一、生成结果行显示生成参数（编辑器）

**需求**：结果行里"42张/21对·4层"和"钩38%/19%·可点12%"之间有一大块空白，用户想填上生成时的预期参数值，方便对比。

**实现**（`editor/index.html`）：
- 新增 `attachGenParams(level, mode, layerCount)`：把生成时刻的参数（钩子目标 / 暗钩比例 / 暗牌总数 / 模式 / 堆层数）挂到 level 的 `_genParams` 上
- 三个生成入口（按当前形状 / 按底座堆塔 / 批量）全部挂上
- 新增 `genParamText()` 渲染短标签 + 结果行新增金色 chip：`参数 钩30 暗钩30 暗10 5L`（浅金底描边，一眼区分"预期"），与右侧灰色实际密度对比
- 结果行改为两行布局：第一行 `#序号 分数 张/对·层 [预览][下载]`，第二行 `参数 chip + 实际密度`——窄窗口也不互相挤压

### 二、批量生成左下角显示序列号

**需求**：点批量生成时虽然出了 5 个，但左下角只显示一次"5/5"，用户想看序列号推进过程。

**实现**：状态条改成每凑到一个结果就推一次：`批量生成中… 已就绪 1/5（尝试 7 次）→ 2/5 → 3/5…`，数字加粗，尝试次数也显示，进度清晰可见。

### 三、Demo 动画优化（`demo/index.html`）

**① 棋盘不再随消除缩放**
- 原：`render()` 用"当前剩余牌的包围盒"算麻将尺寸，消掉一行整个棋盘就放大
- 现：`loadLevel` 时用 `computeBounds(tiles)` 记下初始边界 `baseBounds`，`render()` 永远用固定边界算 cell → 消除过程棋盘纹丝不动

**② 卡槽大小与麻将一致**
- 原：卡槽 56px / 槽内牌 50px，棋盘牌最大 80px，尺寸不统一，动画位移生硬
- 现：卡槽改用 CSS 变量 `--slot-size`，`render()` 里同步成棋盘牌尺寸（实测 80px = 80px），牌飞到卡槽是等尺寸平移

**③ 动画：牌直接飞到卡槽落进去**
- 原：`flyInToSlot` 克隆牌飞过去时缩小到 40% + 淡出消失，卡槽里再"蹦"出一张（slideIn）——生硬
- 现：牌从棋盘原位**平移到卡槽中心落进去**，不缩小不消失，落地就在槽里
- 关键坑：卡槽在 `board-wrapper`（overflow:hidden）外面，clone 挂 board 里会被裁剪 → 改成挂 `document.body` + `position:fixed` 用视口坐标，飞行全程可见
- 配套：`placeInSlot` 先捕获牌当前屏幕位置 `fromRect`（render 移除牌后就取不到了）再传给动画

**④ 去掉卡槽内已有牌上下跳动**
- 根因一：`.slot-tile` 有 `slideIn` 动画，每次重渲染都从上方掉下来 → 删掉 slideIn
- 根因二：`renderSlots()` 每次都 `el.innerHTML=` 重建所有槽位 → 改成已有同牌直接跳过不重建（`existing.dataset.type === String(tile.typeId)` 时 continue）

**实测**（浏览器面板）：
- 进槽前后麻将尺寸恒为 80px（不缩放）✓
- 卡槽 80px 与麻将一致，两张牌入槽后第一张 DOM 元素不变（不跳）✓
- 配对消除（两张同色 → matchedOut 缩小消失）正常 ✓
- 全程无 JS 报错 ✓

---

（压缩包/交付件：用户自行打包，不再重复打 zip。）

---

## Day 21 · 8月18日（周二）：编辑器/Demo 各种修复 + 变更日志规范

### 一、编辑器优化

**① 拖拽分隔条修复（iframe 吞掉 mouseup）**
- 问题：拖拽右侧分隔条时，鼠标进入 iframe 区域，mouseup 在 iframe 内触发，父窗口收不到，松开后继续拖拽
- 修法：拖拽期间给所有 iframe 加 `pointer-events:none`，松手后恢复

**② 弹框位置智能避边界**
- 问题：`positionPop` 只算左边界，弹框可能挤到角落或溢出底部
- 修法：检测底部/右侧边界，超出则改为上方/左对齐显示
- CSS：给 `.gen-help-pop` 加 `max-height:70vh + overflow-y:auto`

**③ 麻将层级偏移 + 删除 L0/L1 层标签**
- 用户希望从侧面看到层级错开
- 编辑器偏移：3px → 10px → 25px → 40px（反复调整，最终 40px）
- 删除 Demo 中的 L0/L1 层标签（视野更清爽）

**④ 状态框固定高度**
- 问题：点击"探测分数范围"时，内容变化导致 `#genStatus` 框体跳动
- 修法：`.status-box` 加 `min-height:52px + overflow-y:auto`

**⑤ 探测分数范围失败处理**
- 问题：5×5=25张（奇数）底座，20次都失败但只显示"20次失败"没具体原因
- 修法：catch 捕获 `lastError`，全部失败时显示具体错误（如"填色无解"）
- 后续：用户指出"底座奇数≠总牌数奇数"（堆塔后会删牌凑偶），移除了奇数前置检查

### 二、Demo 修复

**① 暗牌与卡槽同花色时消除 bug**
- 问题：`placeInSlot` 调用 `checkMatch()` 的位置漏了一个分支（暗牌→卡槽已有同色时）
- 修法：补上 `checkMatch()` 调用

**② 快速点击两个麻将重叠 bug**
- 问题：点 A 后飞入动画期间点 B，两个麻将飞向同一个空槽位重叠
- 根因：`slots[emptyIdx]=tile` 在动画完成后才执行，B 点击时 `slots[0]` 还是 null
- 修法：立即更新 `slots[emptyIdx]`，不等动画完成

**③ 麻将堆叠层级偏移**（关键修复，搞了半天才搞对地方）
- 用户反馈：从右侧预览 demo 看不出层级高度差
- **根因：一直在改编辑器的 Canvas 代码，但 demo 是独立文件用 CSS 渲染**
- Demo 修复前：
  ```js
  const x = offsetX + (t.col - minCol) * cell;  // 没有水平偏移!
  const y = offsetY + (t.row - minRow) * cell - t.layer * layerOffset * cell;
  ```
- Demo 修复后：
  ```js
  const x = offsetX + (t.col - minCol) * cell - t.layer * layerOffset * cell;  // 加水平偏移
  const y = offsetY + (t.row - minRow) * cell - t.layer * layerOffset * cell;
  ```
- 配套调整：`totalW` 加上 `layerXShift`，`offsetX` 补偿居中
- **教训：用户报的是 demo 问题，不要先动编辑器代码！**

### 三、版本号规则 + 变更日志规范

**版本号规则（三段式语义化版本）**：`v主版本.次版本.修订号`

| 变更类型 | 版本号变化 | 举例 |
|---|---|---|
| 重大变更 | 主版本 +1 | v4.x → **v5.0** |
| 新增功能 | 次版本 +1 | v4.3 → **v4.4** |
| 修复/小调整 | 修订号 +1 | v4.3 → **v4.3.1** |

**变更日志位置**：`麻将消一消_在线/editor/index.html` 中的 `#changelogPop` 弹层

**更新时机**：每次改动完成后追加一条新版本记录（最新的在最上面）

### 四、新增功能

**① 变更日志按钮**（工作台旁边）
- 弹层显示版本历史，每条记录：版本号 + 日期 + 改动清单

**② 强制刷新按钮**（变更日志标题旁）
- 方便用户快速强刷，无需按 Ctrl+Shift+R

**③ 目标分数问号补充**
- 添加每个维度的详细说明：可点率/层数/有效钩子/有效暗钩/设计理由

**④ 弹层位置自适应**
- `positionPop` 检测视口边界，超出底部则显示在按钮上方，超出右侧则左对齐

### 五、CLAUDE.md 编写

创建了项目说明书 `麻将消一消/CLAUDE.md`，给 Claude Code 加载：
- 项目概述 + 双目录结构 + 同步规则 + 推送流程 + 协作规范
- 每次新对话自动加载，避免重复解释"工作日志在哪"、"推到哪个仓库"

---

**总耗时**：半天（主要是麻将偏移搞错了位置，从编辑器改到 demo）

---

## Day 22 · 8月18日（周二）：钩子定义扩展 + 分数公式封顶调整

### 一、钩子定义扩展（重大改动）

**用户反馈**："万一一块麻将 A1 和另一块麻将 A2 都在底层,但是 A2 被一堆待消除的麻将压着,这怎么算呢？要是想消除麻将 A1 的话,就必须先把压在 A2 上面的麻将都消除掉才能去拿。"

**原定义**（只算层差 ≥ 2 的对子）：
```js
if (t.layer - topLayer >= 2) buried++;
```

**问题**：同层被压、左右夹住的牌没被算作钩子，导致钩子数量偏少，调参数时分数波动很小。

**新定义**（任何形式的埋牌）：
```js
function isTileBuried(tile, allTiles) {
  // 1. 同层被压：有上层牌覆盖
  const hasUpper = allTiles.some(o => 
    o.id !== tile.id && o.layer > tile.layer && 
    Math.abs(o.row - tile.row) < 2 && Math.abs(o.col - tile.col) < 2
  );
  if (hasUpper) return true;
  // 2. 左右夹：同层左右都被夹住
  const sameLayer = allTiles.filter(t => t.layer === tile.layer && t.id !== tile.id);
  const hasLeft = sameLayer.some(t => t.col === tile.col - 2 && t.row === tile.row);
  const hasRight = sameLayer.some(t => t.col === tile.col + 2 && t.row === tile.row);
  return hasLeft && hasRight;
}
```

**埋牌三种形态**：
1. **层埋**（原定义）：同一对牌中，一张在顶层、一张深埋
2. **上压**（新加）：同层一张，被上层牌压住
3. **左右夹**（新加）：同层一张，左右都被夹住（边夹住）

### 二、分数公式封顶调整

**原公式**：`min(effHooks / 5, 1) × 30` — 5 个有效钩子就拿满分，再多没意义

**新公式**：`min(effHooks / 8, 1) × 30` — 8 个有效钩子才满分，让分数有更大波动空间

**效果**：
- 钩子数 5~8 之间能拉开分数差距（之前全是封顶 30）
- 暗钩封顶 5 → 8
- 可点率、层数维度不变

### 三、影响评估

| 项目 | 修改前 | 修改后 |
|---|---|---|
| 钩子定义 | 只看层差 ≥ 2 | 上压/左右夹都算 |
| 钩子封顶 | 5 | 8 |
| 暗钩封顶 | 5 | 8 |
| 钩子数 | 通常 0~5 | 通常 3~15 |
| 分数波动 | 小（个位数） | 大（10+） |

### 四、潜在风险

- 钩子数大幅增加，可能导致大多数关卡都接近封顶
- 需要实测一批关卡，确认分布合理
- 如有必要可再次调整封顶值

---

## Day 23 · 8月18日（周二）：加槽位压力维度 minSlots（解决"钩子拉满也不难"）+ 层级偏移回调

### 一、问题发现

用户反馈核心痛点：**"把钩子数量拉满，感觉也不会特别难"**。原版麻将有时会出现"消除顺序不对就死局"的情况——卡槽中已有的牌都被夹着或盖着，容错率暴跌。

用户举了一个具体例子：
> 一块麻将 A 可直接点击，另一块 A 被 B 盖住。如果你先点 B 进卡槽，而另一块 B 又深埋着，A 就会一直卡在槽里，容错率降低。原版困难关卡会有这种情况，但我们的游戏把钩子拉满也难出现。

**分析**：现有难度模型（钩子/可点率/层数）都是**静态指标**，只衡量开局一瞬间的状态。真正的难点是**动态的**——消除顺序导致的连锁困境（依赖链）。倒推法随机配对，依赖链普遍很短，所以"钩子拉满也不难"。

### 二、三个候选维度分析

要捕捉"动态难度"，需要新维度。分析了三个候选：

#### 候选 1：依赖深度（Dependency Depth）

**定义**：想消掉最深处的一对牌，需要先解开多少层前置依赖。

```
你点了 A1 进卡槽 → 想配对 A2，但 A2 被 B 压住
  → 想消 B，但 B2 被 C 压住
    → 想消 C，但 C2 又在更深的地方
      → 结果 A、B、C 都卡在槽里
```

- 依赖深度 1：A 被 B 压，B 可见 → 先消 B 就能消 A
- 依赖深度 3：A→B→C 链条 → 得消 C→B 才能碰 A

**问题**：和"层数"高度重复——层数已经在衡量"埋得深"。依赖深度只是层数的另一种表达。

#### 候选 2：陷阱牌比例（Trap Ratio）

**定义**：开局可点的牌里，有多少张是"点了就会让局面变难"的陷阱牌。

比如开局 10 张可点牌，其中 7 张点了之后会让最小槽位需求从 3 升到 5——玩家有 70% 概率第一手就踩坑。这解释了"看起来很简单的关卡，玩着玩着就死了"。

**问题**：计算量太大。每个可点选择都要跑一遍解算器看后续影响，生成速度会慢一个数量级。

#### 候选 3：最小槽位需求（minSlots）✅ 最终选择

**定义**：模拟一个聪明玩家的贪心打法，统计卡槽中最大同时存在的花色数（7 格上限）。

- minSlots = 2 → 容错极高，怎么玩都不容易死
- minSlots = 5 → 容错极低，走错一步槽就满了
- minSlots = 7 → 卡槽上限，几乎不可能完成

**为什么选它**：
1. 一次贪心模拟就能算，计算成本低
2. 直接对应玩家感知的"容错率"
3. 和现有维度（钩子/层数/可点率）不重复

### 三、贪心模拟算法（computeMinSlots）

```
board = 场上未点的牌（Set<id>）
slot  = 已进槽未配对的牌（Map<typeId, tile>）

while 还有牌或槽里有单张:
  1. 场上有可点的牌，其花色槽里已有 → 点它配掉槽里那张（槽位 -1）
  2. 场上有两张可点同花色 → 点掉（短暂过槽即消，不计峰值）
  3. 只能点单张 → 选"底下压牌最多"的进槽（尽早解锁被埋的牌），槽位 +1
  记录 slot.size 峰值
```

**策略设计的理由**：
- 优先级 1（配对槽内）：聪明玩家看到能配就会立即配，减少槽位占用
- 优先级 2（消现成对）：不消耗槽位
- 优先级 3（选压牌最多的）：尽早解锁被埋的牌，减少后续被迫存槽的情况

### 四、新难度公式

```
难度分 = (1-可点率)×40 + 层数×8
       + min(有效钩子/8, 1)×30
       + min(有效暗钩/8, 1)×20
       + min((minSlots-2)/4, 1)×30    ← 新增"槽位压力项"
       + 10
```

**槽位压力项解释**：

| minSlots | 分数 | 含义 |
|---|---|---|
| 2 | 0 | 容错极高 |
| 3 | 7.5 | 低压力 |
| 4 | 15 | 中等压力 |
| 5 | 22.5 | 高压力 |
| 6 | 30 | 极高压力（满分） |

### 五、生成控制：slotPressure 参数

倒推法加 `slotPressure`（0~1）参数控制配对策略。核心洞察：**倒推配对顺序 = 正向消除顺序的逆序**。

- **倒推前中期**（棋盘还满）按概率把一张最高层 + 一张最低层配对
- 正向玩时：高层那张早露、低层那张深埋 → 玩家被迫把早露那张存进槽里 → 槽位占用升高

```
slotPressure = 0   → 纯随机配对（不倒推叠层）
slotPressure = 0.5 → 默认（50% 概率叠层配对）
slotPressure = 1   → 最大化叠层配对
```

**实测数据**（T2 模板，各 15 次）：

| slotPressure | minSlots 范围 | 平均 | 分数平均 |
|---|---|---|---|
| 0 | 1~4 | 1.3 | 123.5 |
| 0.5 | 1~5 | 1.9 | 125.4 |
| 1 | 1~3 | 1.3 | 125.6 |

单个案例：minSlots=5 时分数 152（槽位压力项贡献约 22.5 分）。

### 六、UI 改动

**结果行新增槽位显示**：
```
钩96%/53% · 可点20% · 槽1/7
```
minSlots ≥ 5 时显示 🔥 标识高槽位压力。

**预览统计条新增**：`槽{minSlots}/7`

**目标分数问号弹层更新**：
- 公式加 `+ min((槽位压力 - 2) / 4, 1) × 30`
- "各项含义"加槽位压力说明
- "设计理由"加槽位压力解释

### 七、层级偏移回调

用户反馈编辑器麻将层级偏移之前改得太大（40px），恢复为 3px。

历史变更：3px → 10px → 25px → 40px（Day 21 反复调整后定在 40px）→ **3px**（本次回调）

改动：`editor/index.html` 第 1297-1299 行 `layerIdx * 40` → `layerIdx * 3`

### 八、改动文件

| 文件 | 改动 |
|---|---|
| `麻将消一消_在线/editor/generator.js` | 加 computeMinSlots；evaluateDifficulty 加 slotTerm；backwardFill 加 slotPressure |
| `麻将消一消_在线/editor/index.html` | 结果行/预览条/问号弹层加槽位显示和说明；层级偏移 40px→3px |
| `outputs/关卡搭建工具包/generator.js` | 同步（从在线版复制） |
| `outputs/关卡搭建工具包/shape_editor_std.html` | 结果行加槽位显示 |

### 九、心得

- **静态指标的天花板**：钩子/可点率/层数都是开局快照，无法捕捉"消除顺序不对就死局"的动态难度。minSlots 是第一个中盘指标。
- **贪心模拟足够好**：不需要完整 DFS 解算器。一个"优先配对、其次消现成对、最后选压牌最多的单张"的贪心策略，就能稳定反映槽位压力。
- **倒推法配对策略是关键**：纯随机倒推天然 minSlots 偏低（随机配对很少产生"同花色一张早露一张深埋"的结构）。slotPressure 在倒推时主动构造叠层配对，才能产出真正的"容错率低"关卡。

---

## Day 23 补充 · 8月18日（周二）：钩子加权改为"可见伴侣"加权

### 一、用户反馈的问题

用户看一关（74 张 / 37 对），数据说"钩子 95%"，但他**眼睛能看到 3 对可立即消除的牌**（1万、6万、龙）。直觉上"埋得不多"，但算法说有 35 对钩子。

### 二、根因分析

**原稀缺度加权**：每个钩子权重 = 1 / 该花色总对子数。

- 1 对 1 张埋 → 权重 1.0（独占型）
- 3 对 6 张埋 → 权重 0.33（泛滥型）

**问题**：如果某花色有 4 张（2 对），其中 3 张露在外面、1 张埋着：
- 算法：这是 1 对钩子，权重 1/2 = 0.5
- 玩家视角："埋的那张很容易找到，因为外面 3 张都长得一样"

### 三、新规则（用户提议）

**权重 = 1 / (同花色可见张数 + 1)**

| 场景 | 同色可见 | 权重 |
|---|---|---|
| 同色全部埋着，没人露 | 0 | 1.0 |
| 同色有 1 张露 | 1 | 0.5 |
| 同色有 2 张露 | 2 | 0.33 |
| 同色有 3+ 张露 | 3+ | ≤ 0.25 |

**直觉**：埋的牌同色**越容易看到**，就越**不容易忘**，权重越低。

### 四、代码改动

```js
// 新加权：每张埋牌的权重 = 1 / (同花色可见 + 1)
const buriedList = arr.filter(t => isTileBuried(t, tiles));
const hookInst = Math.min(buriedList.length, pairs);  // 按对数截断
for (let i = 0; i < hookInst; i++) {
  const t = buriedList[i];
  const sameVisible = arr.filter(o => o.id !== t.id && clickableSet.has(o.id)).length;
  effHooks += 1 / (sameVisible + 1);
}
```

### 五、实测对比

T2 模板各生成 5 个样本：

| 样本 | 原 raw 钩% | 新加权钩% | 分数 |
|---|---|---|---|
| 1 | 91% | 61% | 113 |
| 2 | 96% | 61% | 132 |
| 3 | 93% | 65% | 114 |
| 4 | 93% | 62% | 122 |
| 5 | 96% | 63% | 123 |

用户截图场景（slotPressure=0，模拟"看起来简单"）：

| 样本 | 原 raw 钩% | 新加权钩% | 分数 |
|---|---|---|---|
| 1 | 98% | 60% | 130 |
| 2 | 93% | 66% | 131 |
| 3 | 96% | 65% | 115 |
| 4 | 98% | 66% | 125 |
| 5 | 91% | 57% | 120 |

加权后的钩% 和玩家"看得到的牌数"基本对得上了。

### 六、改动文件

| 文件 | 改动 |
|---|---|
| `editor/generator.js` | `evaluateDifficulty` 钩子加权改为可见伴侣加权 |

---

## Day 23 补充 · 8月18日（周二）：参数匹配改为反向驱动（方案 C）

### 一、问题

用户发现钩子滑块和分数范围存在逻辑矛盾：
1. **单关生成（按形状/按底座）**：只匹配钩子/暗牌参数，**完全不管分数**——设钩子 100% 但底座是口字形（几乎没钩子），系统只会静默重试到上限后返回"最接近的"
2. **批量生成**：同时看参数和分数，但两者可能冲突（钩子拉满 → 分数必然高 → 和低分数目标矛盾）
3. **用户困惑**：钩子驱动分数，但用户又单独设分数——到底看哪个？

### 二、方案 C：反向驱动

**核心转变**：用户只设目标难度（分数），系统自动反推钩子/暗牌参数。

**UI 改动**：
- 顶部新增：`目标难度` 输入框 + `容差` 滑块 + `🎯 自动匹配参数` 按钮
- 原来的 3 个滑块（钩子/暗牌/暗钩）变成"结构参数——自动匹配后的建议值，可手动微调"
- 去掉"目标分数 min~max"输入框和"探测"按钮

**交互流程**：
1. 用户设目标难度 110、容差 ±15
2. 点"自动匹配参数"
3. 系统尝试钩子 0%/10%/20%/.../100%，各生成 4 样本看平均分数
4. 选最接近目标的钩子值，更新滑块
5. 用户可手动微调，然后正常生成

**搜索逻辑**：
```js
function autoMatchParams() {
  const target = parseInt(document.getElementById('targetScore').value);
  const tolerance = parseInt(document.getElementById('scoreTolerance').value) || 15;
  // 尝试 0%~100% 钩子，步长 10%
  for (let hook of [0, 0.1, 0.2, ..., 1.0]) {
    // 各生成 4 样本，算平均分数
    // 记录 |avgScore - target|
  }
  // 选最接近的 hook，更新滑块
}
```

### 三、实测

- 目标 120 分 → 匹配钩子 30%（平均 120 分）✓
- 目标 150 分 → 匹配钩子 10%（平均 128 分，最接近的选项）

### 四、改动文件

| 文件 | 改动 |
|---|---|
| `editor/index.html` | 加目标难度/容差/自动匹配按钮；3 滑块改为建议值；去分数范围输入；帮助弹层更新 |

### 五、心得

- **用户心智模型**：用户关心"我要多难"，不关心内部参数。让系统做翻译，比让用户理解"钩子→分数"的因果链更直觉。
- **滑块变建议值**：保留高级用户的微调能力，但默认由系统驱动——兼顾易用和灵活。
- **搜索粒度**：11 个钩子值 × 4 样本 = 44 次生成，异步执行不阻塞 UI，约 2~3 秒完成。

---

## Day 24 · 8月18日（周二）：Tab 双模式（参数模式 + 难度模式）+ 保存按钮 bug 修复

### 一、问题

钩子/暗牌/暗钩比例滑轮对另一名策划来说太抽象。
- 用户需要"我要多难的关卡"，不是"我要钩子 40% 的关卡"
- 钩子驱动分数但用户又单独设分数——两个来源矛盾

### 二、方案：Tab 双模式

**Tab 1：参数模式**（保留）
- 目标分数 min~max + 钩子/暗牌/暗钩三个滑轮
- 给老用户/精细调参用

**Tab 2：难度模式**（新增）
- 一个 1-10 滑轮
- 系统自动算出分数范围/钩子/暗牌比例
- 给新协作者/快速生成用

**难度映射**（实测分数范围 60~150）：

| 难度 | 分数范围 | 钩子 | 暗牌 |
|---|---|---|---|
| 1 | 50~70 | 10% | 5% |
| 5 | 82~106 | 41% | 12% |
| 10 | 122~151 | 80% | 20% |

**Tab 切换时同步滑轮值**：切到难度模式时，自动把对应的值写入参数模式的滑轮，避免两个 Tab 显示不一致。

**难度模式不严格匹配**：参数是自动分配的，不要求生成结果"命中"目标，直接接受第一个生成结果。

### 三、问题与修复

#### 1. 难度 10 的分数范围到不了 140~170

**原因**：文档说"实际多在 60~150"，170 是理论最大值。
**修复**：从 `60~170` 改为 `50~151`（d=1 ~ d=10）。

#### 2. 保存按钮没反应

**原因**：HTML 里的 id 是 `wbNewOrder`，但 JS 找的是 `wbSaveOrder`，对不上。`getElementById('wbSaveOrder')` 返回 `null`，`.value` 抛错，函数崩溃，**完全没反应**。
**修复**：
- 兼容两种 id（`wbNewOrder` 或 `wbSaveOrder`）
- 用 `try-catch` 包整个函数，再有异常弹 alert 而不是静默崩溃

#### 3. 错误提示是假的

**原因**：提示"试试调整参数或形状"，但用户不知道该调什么。
**修复**：
- 始终返回最接近的关卡（不再报错）
- 偏差说明客观化：只说"分数 X 高于上限 Y"，不再说"试试调什么"

### 四、改动文件

| 文件 | 改动 |
|---|---|
| `editor/index.html` | 加 Tab 系统（参数/难度）、Tab 切换同步滑轮、难度模式自动映射、保存按钮 bug 修复 + try-catch |
| `难度分说明.md` | 更新到 v3 公式 |
| `CLAUDE.md` | 简化工作流说明（旧工具包已废弃） |

### 五、心得

- **简化 ≠ 删功能**：Tab 模式让新人用难度滑轮、老用户用参数滑轮——同一个工具适配两类用户。
- **静默崩溃最可怕**：保存按钮"没反应"不是用户操作问题，是 JS 抛异常被吞了。今后所有关键 handler 都要 try-catch + 弹 alert。
- **id 不一致是隐性 bug**：HTML 改了 id 名字，JS 还在找老的——`getElementById` 静默返回 null，`.value` 才报错。改名后要全文搜引用。

---

## Day 24 · 8月21日（周三）：吐牌机编辑器支持 + 生成卡死修复

### 一、吐牌机机制设计

**核心概念**：吐牌机是被动供给装置——当它指向位置的牌被消除时，自动补一张新牌。

**用户流程**：
1. 画好底座形状（常规麻将布局）
2. 选"吐牌机"画笔（⚙）
3. 点要摆吐牌机的位置 → 摆放吐牌机（橙色标记）
4. 在吐牌机指向的位置多摆几层牌（同一 row/col 摆 2~4 张）
5. 配置吐牌机参数（方向 + count）
6. 点"按当前形状生成" → 系统自动填色，吐牌机队列的牌按 spitterOrder 排好

**数据结构**：
```json
{
  "tiles": [
    {"id": 1, "layer": 1, "row": -2, "col": 0, "typeId": 5, "spitterOrder": 1},
    {"id": 2, "layer": 1, "row": -2, "col": 0, "typeId": 12, "spitterOrder": 2},
    {"id": 3, "layer": 1, "row": -2, "col": 0, "typeId": 30, "spitterOrder": 3}
  ],
  "specialTiles": [
    {"id": 100, "type": "spitter", "layer": 3, "row": -2, "col": 0, "dir": "up", "count": 3}
  ]
}
```

**关键规则**：
- 吐牌机 tile 本身不参与填色（保持橙色 ⚙）
- 吐牌机 tile 不参与 backwardFill 配对（排除在外）
- 吐牌机队列牌必须有支撑（不能悬空）
- 总牌数校验排除吐牌机本身（吐牌机不需要配对）

### 二、编辑器改动

| 改动 | 说明 |
|---|---|
| 状态加 `stacks` | 每层一个 `stacks` 对象，存储堆叠的队列牌 |
| 吐牌机画笔 | 点位置时自动在指向方向堆 N 张队列牌（count 张） |
| `getSpitterTarget()` | 根据方向计算目标位置 |
| 渲染 | 队列牌用橙色绘制（`spitterQueue` 类型） |
| 导入 | `spitterOrder` 的牌 → `stacks` |
| `buildEditorShape` | 队列牌展开为独立 tile + `spitterOrder` |
| `fillEditorShape` | 排除吐牌机参与 backwardFill；验证支撑 + 偶数 |

### 三、问题与修复

#### 1. 吐牌机显示为"八筒"（普通麻将）

**原因**：`buildEditorShape` 没保留 `type: 'spitter'` 字段
**修复**：line 2228 改为 `if (tile.type && tile.type !== 'normal') tileEntry.type = tile.type;`

#### 2. 生成卡死（"正在生成"不动）

**原因**：吐牌机 tile 被传入 `backwardFill`，但它没有 typeId 无法配对 → 100 次重试全失败
**修复**：`fillTiles = tiles.filter(t => t.type !== 'spitter')` 排除吐牌机

### 四、改动文件

| 文件 | 改动 |
|---|---|
| `editor/index.html` | 状态加 stacks、吐牌机画笔逻辑、getSpitterTarget、渲染队列牌、导入 spitterOrder、buildEditorShape 展开队列 |
| `editor/generator.js` | fillEditorShape 排除吐牌机参与配对、验证支撑 + 偶数 |

### 五、心得

- **吐牌机是"位置型"而非"方向型"**：吐牌机指向一个具体位置（targetLayer/Row/Col），队列牌全部堆在该位置。方向只是编辑器里帮助定位的辅助。
- **"同一位置多张牌"是核心数据结构变化**：编辑器原本只支持"一个位置一张牌"，现在通过 `stacks` 对象支持堆叠。这是最大的架构改动。
- **排除法比包含法更干净**：`fillTiles = tiles.filter(t => t.type !== 'spitter')` 比手动跳过索引更不容易出错。


---

## Day 24 补充 · 8月21日（周三）：吐牌机 typeId 分配 + 重复声明 bug

### 一、吐牌机分配 typeId（v4.6.21）

**核心问题**：吐牌机应该是一张有花色的普通麻将，不是图标。

用户要求："吐牌机应该被附上了一个吐牌 ID，所以它被上成了一个颜色"。

**修复**：
- `fillEditorShape` 给吐牌机分配独立 typeId（用独立池避免和普通牌/队列牌重复）
- 队列牌也分配独立 typeId
- 吐牌机 demo 渲染：按普通麻将渲染（用 typeId 显示花色），不再用 ⚙ 图标

### 二、修复 usedTypeIds 重复声明（v4.6.22）

**Bug**：`fillEditorShape` 里 `usedTypeIds` 被 `const` 声明了两次 → 语法错误 → 生成卡死，"生成中"一直转。

**修复**：
- 去掉重复声明，吐牌机和队列牌共用同一个 `usedTypeIds` Set
- 按顺序分配 typeId（先吐牌机，再队列牌）

### 三、吐牌机放置验证（v4.6.18）

**新增校验**：
- 吐牌机放置时检查目标位置是否被覆盖
- 队列牌必须有支撑（不能悬空）
- 总牌数必须为偶数（排除吐牌机本身）

### 四、demo 渲染吐牌机（v4.6.20）

**改动**：
- demo 读取 `specialTiles` 找出吐牌机位置
- 吐牌机位置渲染为普通麻将（用 typeId）
- 队列牌（`spitterOrder != null`）不直接渲染
- 加了 `.tile.spitter` CSS 样式

**还没做**：
- 吐牌机的游戏逻辑（点击触发队列）
- 队列牌依次出现
- count 用完后吐牌机消失

### 五、心得

- **`const` 重复声明是隐蔽 bug**：JavaScript 不会报运行时错误，但行为异常。生成函数"卡死"可能就是这个原因。
- **吐牌机是有 typeId 的普通麻将**：用户已经明确这点了。关键是不显示为特殊图标，而是按普通麻将渲染。

---

## Day 25 · 8月21日（周三）：吐牌机游戏逻辑完整实现（推翻之前的部分方案）

### 一、之前的实现没做完 + 用户决策反转

之前 Day 24 实现吐牌机编辑器 + 生成器时犯了一个错误：**给吐牌机本体分配了 typeId**，把它当成"有花色的普通麻将"参与配对；队列牌也用了独立 typeId 池避免和普通牌重复。

用户这次明确决策：
1. **触发条件**：指向位置的**那一张**牌被消除时
2. **吐牌机不是麻将**：不分配 typeId，队列释放完后**从棋盘上消失**（不参与配对）
3. **队列牌释放时**：淡入 + 轻微缩放动画

### 二、生成器改动（`editor/generator.js`）

**fillEditorShape（line 749-768 改前）**：
- 删掉给吐牌机本体分配独立 typeId 的整块（10 行）
- 队列牌 typeId **从独立池改为共享池**：从普通牌已分配的 `result.assigned` 中随机选（可重复）
- 理由：队列牌释放后会进卡槽/和场上同花色配对，必须共用一个 typeId 池

**generateStackFromShape（line 954）**：原本只写 `type: 'spitter'`，**补上 `dir` 和 `count`**——否则 demo 端无法算出指向位置和释放次数。

**编辑器 `buildEditorShape`**：原本 spitters 数组只有 `{layer, row, col, count}`，**补上 `dir` 字段**——和 tile 上的 dir 对齐。

### 三、Demo 游戏逻辑（`demo/index.html`）

**新增 4 个全局状态**：
```js
const spitterReleasedCount = new Map();  // spitterId → 已释放数
const spitterRevealedIds = new Set();    // 本次新释放的队列牌 id
const spitterExhaustedIds = new Set();   // 已耗尽的 spitter 本体 id
```
`loadLevel` 开头 clear 三个 Set/Map。

**CSS**：加 `.tile.spitter-reveal` + `@keyframes spitterReveal`（scale 0.7→1 + opacity 0→1，0.3s）。

**`render()` 改动**：
- `spitterIds` 来源从 `currentSpecialTiles`（空）改为 `tiles.filter(t => t.type === 'spitter' && !exhausted)`
- 队列牌不再无脑跳过，改为**条件渲染**：`spitterOrder <= owningSpitter 已释放数` 才渲染
- 拼接 class：spitter 本体加 ` spitter`（棕边 + 阴影 CSS 即生效），新释放的队列牌加 ` spitter-reveal`

**`getActiveTiles()`**：新增 `!spitterExhaustedIds.has(t.id)` 过滤。关键：**未耗尽的 spitter 本体仍计入 active**（否则棋盘还没空就会过早胜利）。

**`clickTile()` 开头**：spitter 本体 → `showHint` 抖动，不响应。

**新增 `triggerSpitters(matchedNow)`**：
```js
function triggerSpitters(matchedNow) {
  for (const matched of matchedNow) {
    // 找到指向该位置的 spitter
    const owner = spitters.find(sp => spitterTargetPos(sp) matches matched 的 row/col);
    if (!owner) continue;
    const released = spitterReleasedCount.get(owner.id) ?? 0;
    const nextOrder = released + 1;
    // 找队列牌（spitterOrder === nextOrder, 指向位置 row/col）
    const queueTile = tiles.find(...);
    if (queueTile) { releasedCount.set(owner.id, nextOrder); revealedIds.add(queueTile.id); }
    // 队列用尽 → spitter 进 exhausted（从棋盘消失）
    if (nextOrder >= owner.count) exhaustedIds.add(owner.id);
  }
}
```

**`checkMatch` setTimeout 回调（320ms 配对动画后）**：
```
render() → triggerSpitters(matchedNow) → 如果有触发再 render()（队列牌淡入） → checkWin()
```

### 四、踩坑记录

1. **`spitter.dir` 在 tile 写出时被丢**：原 `editorSpitters.forEach(sp => assignedTiles.push({type:'spitter'}))` 没保留 dir/count，demo 端 `spitterTargetPos()` 算不出指向位置。修法：补 dir + count。
2. **队列牌 row/col = 指向位置，不是 spitter 自身**：triggerSpitters 一开始用 `t.row === owner.row` 找队列牌，找不到。修法：用 `spitterTargetPos(owner).row === t.row` 匹配。
3. **`.tile.spitter` CSS 定义了但没人拼**：原来 demo 算 spitterIds 算了，但 cssClass 拼接里没用。改完才生效。

### 五、待验证

- 浏览器实测：画吐牌机 + 3 张队列 → 生成 → 预览 → 点指向位置 → 第一张队列淡入 → 反复 → 吐牌机消失 → 通关
- 边界：吐牌机被点应抖动；棋盘空 + 队列还有牌不胜利；队列耗尽时棋盘还有牌不阻塞消除

### 六、改动文件

| 文件 | 改动 |
|---|---|
| `麻将消一消_在线/editor/generator.js` | fillEditorShape 删本体 typeId 分配 + 队列牌共享池；generateStackFromShape spitter 写出加 dir/count |
| `麻将消一消_在线/editor/index.html` | buildEditorShape spitters 数组加 dir；#changelogPop 顶部加 v4.6.23；APP_VERSION 占位 |
| `麻将消一消_在线/demo/index.html` | 加全局状态 + CSS + render 改 + clickTile 拦截 + getActiveTiles 过滤 + 新增 triggerSpitters + checkMatch 触发 |
| `麻将消一消_在线/version.json` | 更新到 v4.6.23（version 占位 PENDING，commit 后回填真实 hash） |
| `麻将消一消/麻将消一消_工作日志.md` | 母本追加 Day 25（本节） |
| `麻将消一消_在线/docs/工作日志.md` | 同步追加 Day 25 |

### 七、心得

- **"做完"≠"能用"**：之前编辑器能摆吐牌机、生成器能导出 JSON，就以为完成了。但没有 demo 端的游戏逻辑，整个机制对玩家是"不存在的"——只能在关卡 JSON 里看到数据，没法玩。**功能闭环 = 编辑端可摆 + 生成端可导出 + 游玩端可玩**，任何一环缺了都不算做完。
- **用户决策反转是好事**：原方案"吐牌机 = 有 typeId 的普通麻将"是从代码惯性（编辑器 tile 都有 typeId）推出来的，但用户明确说"它不是麻将"。这种时候别强行维护一致性，听用户的——因为一致性是手段，不是目的。
- **数据保留链路要逐层盯**：`buildEditorShape` → `editorSpitters` 数组 → `options.spitters` → `generateStackFromShape` 写出 spitter tile → demo `t.type === 'spitter'`，任何一环字段丢失都会让下游失效。这次 dir/count 丢了就是这种问题。

---

## Day 25 补充 · 8月21日（周三）：生成前偶数校验（按当前形状 + 按堆塔）

### 一、问题

之前生成器对偶数的处理是"事后校验"——填色 / 堆塔跑完了才发现总牌数是奇数，抛异常。这有几个问题：
1. 用户可能在编辑器花了几分钟搭好形状，提交生成才发现"底座是奇数"，浪费了大量工作
2. spitter 的 count 字段没有范围校验（理论上能填 999）
3. 队列牌数没和 spitter.count 联动校验，用户可能画了 count=3 但漏摆队列牌

### 二、按当前形状生成（fillEditorShape）填色前校验

新增 4 项前置校验（在 backwardFill 之前）：

| 校验项 | 错误信息 |
|---|---|
| 普通牌数偶数（不含 spitter 和队列牌） | "场上普通牌 N 张为奇数，二消要偶数张才能成对，请加一张或删一张" |
| spitter.count 在 1~6 范围 | "吐牌机 [L?,?,?] 的 count=N 不合理（应为 1~6）" |
| 队列牌数 == spitter.count | "吐牌机 [L?,?,?] 配置 count=N 但实际队列牌有 M 张" |
| 每个 spitter 的队列牌数偶数 | "吐牌机 [L?,?,?] 队列牌 N 张为奇数，二消要偶数张，请调整 count 为偶数" |

**关键细节**：队列牌的 row/col 是 spitter 的**指向位置**（不是 spitter 自身），校验时要先算 `targetRow = sp.row + dirOffset`，再用 target 匹配队列牌。

### 三、按堆塔生成（generateStackFromShape）底座校验

底座校验也加强：
- 普通牌数偶数（不含 spitter 本体）
- **队列牌总数偶数**（所有 spitter.count 之和必须偶数）

```js
const totalQueueCount = editorSpitters.reduce((s, sp) => s + (sp.count || 0), 0);
if (totalQueueCount % 2 !== 0) {
  throw new Error('吐牌机队列牌总数 ' + totalQueueCount + ' 张为奇数，二消要偶数张。请调整每个吐牌机的 count 为偶数');
}
```

### 四、堆塔凑偶逻辑考虑队列牌

原堆塔凑偶（line 913-932）只让 `shape.tiles.length` 为偶数，没考虑队列牌。改后让 `shape.tiles.length` 与 `totalQueueCount` 同奇偶（凑偶后总和偶数）：

```js
const targetParity = totalQueueCount % 2;
if (shape.tiles.length % 2 !== targetParity) {
  // 顶层成对删除（保留镜像对称性）
  ...
}
```

如果连续 30 次搭塔都凑不到偶数 → 抛 `堆塔后无法完成填色(底座形状可能导致无解...)`（已存在的兜底错误）。

### 五、最终偶数校验同步修正

line 790（fillEditorShape）和 line 1010（generateStackFromShape）的最终校验，原来只排除 spitter 本体：
```js
const nonSpitterCount = tiles.filter(t => t.type !== 'spitter').length;
```
改为同时排除队列牌：
```js
const nonSpitterCount = tiles.filter(t => t.type !== 'spitter' && t.spitterOrder == null).length;
```
否则队列牌会被算入"参与配对的牌数"，影响校验语义。

### 六、踩坑记录

1. **测试时第一版用 `t.row === sp.row` 找队列牌 → 找不到**：队列牌 row/col 是 target 位置，spitter 自身在另一位置。修法：用 dir 偏移算出 target 再匹配。
2. **第一版校验只看了 `t.type !== 'spitter'`，把队列牌当普通牌**：普通牌数算多了，导致"偶数校验"误判为奇数。修法：明确排除 `t.spitterOrder == null`。
3. **预览时浏览器缓存了旧版 generator.js**：reload 后看 `GEN` 仍是旧代码。教训：preview 实测要先 reload。

### 七、改动文件

| 文件 | 改动 |
|---|---|
| `麻将消一消_在线/editor/generator.js` | fillEditorShape 加 4 项前置校验；generateStackFromShape 加底座校验 + 队列总数校验；凑偶逻辑考虑队列牌奇偶；最终校验排除队列牌 |
| `麻将消一消_在线/editor/index.html` | #changelogPop 顶部加 v4.6.24；APP_VERSION 占位 |
| `麻将消一消_在线/version.json` | 加 v4.6.24 changelog；version 改 PENDING |
| `麻将消一消/麻将消一消_工作日志.md` | 母本追加 Day 25 补充（本节） |
| `麻将消一消_在线/docs/工作日志.md` | 同步追加 |

### 八、心得

- **校验前置 > 校验后置**：填色前发现"普通牌奇数"vs 填色后发现"不可解"，对用户体验差几个数量级。前置校验成本极低（O(n)），收益巨大。
- **"偶数"在含 spitter 的关卡里要分两段看**：spitter 本体永远不算参与配对，队列牌必须配对消除，普通牌必须配对消除。三个数之和的奇偶性 = (普通牌 + 队列牌) 的奇偶性（spitter 不影响）。
- **用户用"数字"指代 spitter.count**：一开始没理解用户说"吐牌机内部的数字"是什么意思，问了之后明白是 count 字段。下次描述抽象功能时，先问清楚每个名词的具体指代。

---

## Day 25 补充 2 · 8月21日（周三）：修复偶数校验冲突

### 一、用户反馈的 bug

场景：7 普通 + 1 spitter(count=2) + 2 队列牌 → 总 10 张
1. 点"按当前形状生成" → **报"场上普通牌 7 张为奇数"**（fillEditorShape 内部校验正确）
2. 加一张普通牌 → 8 普通 + 1 spitter + 2 队列 = 11 张
3. 再次点生成 → **报"牌数必须为偶数（当前 11）"**（editor/index.html:2563 旧校验触发）

两条校验信息互相冲突：先报"普通牌 7 张为奇数"，加了 1 张后又报"牌数必须为偶数（当前 11）"——用户无所适从。

### 二、根因

`editor/index.html:2563`（旧校验）检查的是 `tiles.length`(所有牌总数，包括 spitter 本体和队列牌)。这是 spitter 设计改之前的旧逻辑——当时 spitter 是有 typeId 的普通麻将，totalPairs = 总牌数 / 2。

新设计下 spitter 本体**不参与配对**，所以总牌数奇偶性不再决定可解性。新的 fillEditorShape 内部校验已经精确判断：
- 普通牌偶数（不含 spitter 和队列牌）
- 每个 spitter 的 count 偶数

### 三、修法

移除 `editor/index.html:2563` 的冗余校验，让 fillEditorShape 的细粒度校验独占报错位。

```js
// 旧（line 2563, 已删）
if (tiles.length % 2 !== 0) { setGenStatus('牌数必须为偶数（当前 ' + tiles.length + '）'); return; }

// 新（注释说明由 fillEditorShape 接管）
// 总牌数偶数校验已移到 fillEditorShape 内部（含 spitter/队列牌的细粒度校验更准确）
```

### 四、改动文件

| 文件 | 改动 |
|---|---|
| `麻将消一消_在线/editor/index.html` | 移除 generateCurrentShape 中旧的总牌数校验(line 2563);#changelogPop 加 v4.6.25 |
| `麻将消一消_在线/version.json` | 加 v4.6.25 changelog;version 改 30f3f49 |

### 五、心得

- **"防御性校验"会和"精确校验"打架**：旧校验是粗粒度的防御，新校验是细粒度的精确。两者并存会让用户在同一场景下看到不同口径的错误信息。**新校验就位时，要主动清掉旧校验**，不要"加一层保险"思维。
- **校验粒度要和数据语义对齐**：旧校验假设"每张牌都参与配对"，所以总牌数偶数 = 可解。新设计里 spitter 不参与配对，总牌数奇偶就失去了语义。**校验规则 = 数据语义的可计算投影**，数据语义变了，校验规则要同步更新。

---

## Day 25 补充 3 · 8月21日（周三）：吐牌机本体显示吐牌机标识（不再被误认成普通麻将）

### 一、用户反馈

用户截图：编辑器按当前形状生成后，demo 里吐牌机本体位置显示成了一张"鸟"图（某种花牌的麻将图），下方另一张牌显示成"枫叶"图。用户说"吐牌机的位置变成了一张麻将，吐牌机下面的那张牌被识别成了吐牌机"。

实际：用户看到的"鸟"和"枫叶"是**两张被错认的普通麻将图**，用户希望吐牌机本体显示成"吐牌机"之类明显标识，而不是普通麻将图。

### 二、根因

`demo/index.html` 的 `tileImgSrc(typeId)` 当 typeId 为 null 时 fallback 到 `TILE_FRONT_IMAGES[Math.abs(null) % length] = TILE_FRONT_IMAGES[0]` — 一张随机麻将图。所以 spitter 本体（typeId=null）被渲染成了普通麻将图，看起来就是"一张麻将"。

### 三、修法

1. **render() 区分 spitter 本体和普通牌**：
   - `isSpitterBody = spitterIds.has(t.id)`
   - spitter 本体 src 用 `SPITTER_BODY_IMAGE`（纯白底 SVG）
   - 普通牌和暗牌走原来的 `tileImgSrc(t.typeId)`

2. **CSS 隐藏 spitter 本体的 img，用 overlay 画吐牌机标识**：
   ```css
   .tile.spitter .tile-img { display: none; }
   .tile.spitter { background: 象牙白渐变 + 棕边; }
   .spitter-overlay { ♻(已改用 ⤴)+ 方向箭头 + ×剩余数 }
   ```

3. **spitter 本体上方叠加一个 overlay div**：
   ```html
   <div class="spitter-overlay">
     <div class="spitter-arrow">▼</div>
     <div class="spitter-count">×N</div>
   </div>
   ```
   - `⤴`（U+2904 向上弧形箭头）：表达"吐出来"的语义
   - `SPITTER_DIR_ARROW[dir]`：箭头方向指示（up=▲, down=▼, left=◀, right=▶）
   - `spitterRemainingQueueCount(id)`：spitter.count - 已释放数

### 四、踩坑记录

1. **`em` 单位字号溢出**：tile 实际约 50x50px，第一版用 `font-size: 50%; font-size: 1.2em`（最终 9.6px），太小看不清；第二版用 px 绝对值 20/22/14/12px 才合适。
2. **♻ Unicode 字符在某些字体下显示不像回收符号**：改用更通用的 ⤴ 箭头符号。
3. **SVG data URI 在 utf8 模式下解析失败**（naturalWidth=0）：直接用 `display: none` 隐藏 img，纯 CSS 绘制更稳定。

### 五、改动文件

| 文件 | 改动 |
|---|---|
| `麻将消一消_在线/demo/index.html` | 加 SPITTER_BODY_IMAGE / SPITTER_DIR_ARROW / spitterRemainingQueueCount；render 区分 spitter 本体；CSS .tile.spitter/.spitter-overlay/.spitter-arrow/.spitter-count |

### 六、心得

- **特殊机制一定要有明确视觉标识**：玩家无法从代码理解"这张牌有特殊行为"，UI 上必须用独特外观传达。spitter 本体如果看上去像普通麻将，玩家会按普通麻将的预期去操作（点了应该进卡槽），结果发现"怎么没反应"，体验是"这是 bug"。
- **typeId=null 的 fallback 行为要谨慎**：`tileImgSrc(null)` 返回 `TILE_FRONT_IMAGES[0]` 这个设计很危险——任何 typeId 缺失的 tile 都会被随机分配一张麻将图，掩盖数据结构错误。**遇到"应该不是麻将"的 tile，要么显式用专门资源，要么显式报错**，不能依赖 fallback。
- **Unicode 字符在跨平台 UI 上不可靠**：♻ / ⤴ 等字符在不同浏览器/操作系统的渲染差异很大。生产环境最好用 SVG 图标（自定义字体也行），不要依赖系统字体对 Unicode 的支持。

---

## Day 25 补充 4 · 8月21日（周三）：修复 buildEditorShape 丢失 spitter dir/count + spitters 数组错存位置

### 一、用户反馈的两个问题

1. **`生成失败: 吐牌机 [L0,0,0] 的 count=undefined 不合理`**
2. **`吐牌机的方向好像反了`**
3. **`它也没有在消除完之后继续补牌`**

三个问题看似独立，实际根源是同一个：`buildEditorShape` 写 `tiles` 数组里的 spitter tile 时**只保留了 `type` 字段，丢掉了 `dir` 和 `count`**。

### 二、根因分析

#### 问题 1：count=undefined

`buildEditorShape` line 2397-2398 原代码：
```js
const tileEntry = { id: 0, layer: layerIdx, row: r, col: c, typeId: null };
if (tile.type && tile.type !== 'normal') tileEntry.type = tile.type;
tiles.push(tileEntry);
```

只保留 `type`，丢 `dir`/`count`。然后 `fillEditorShape` 校验 `sp.count` 就是 `undefined`，触发"不合理"错误。

#### 问题 2 + 3：方向反 + 没补牌

`buildEditorShape` 的 `spitters` 数组 line 2402-2406 写了 `target` 位置（`row: r + offset[0], col: c + offset[1]`），但**生成器把它当 spitter 本体位置用**：
```js
// generateStackFromShape line 962-968（修复前）
editorSpitters.forEach(sp => {
  assignedTiles.push({
    id: 0, layer: sp.layer, row: sp.row, col: sp.col,
    typeId: null, type: 'spitter',
    dir: sp.dir, count: sp.count,
  });
});
```
写出的 spitter tile 在 **target 位置**而非 spitter 本体位置——视觉上 spitter 跑到了"目标位置"，方向看上去反了。

**fillEditorShape 路径不受这个 bug 影响**（直接用 `editorTiles` 数组里的 spitter tile），所以"按当前形状"路径方向是对的；"按堆塔"路径方向是错的。

但 count=undefined 让 fillEditorShape 校验抛错，按当前形状生成也走不通——两个路径都受影响。

补牌不工作的连带原因：fillEditorShape 抛错 → 整个关卡生成失败 → 用户的关卡是上次的旧版（如果缓存了）。

### 三、修法

1. **`buildEditorShape` 写 spitter tile 时保留 `dir`/`count`**：
   ```js
   if (tile.type === 'spitter') {
     tileEntry.dir = tile.dir || 'up';
     tileEntry.count = tile.count || 3;
   }
   ```

2. **`spitters` 数组改存 spitter 本体位置**（不是 target）：
   ```js
   spitters.push({
     layer: layerIdx, row: r, col: c,  // 之前是 r + offset[0]
     count: tile.count || 3, dir: dir,
   });
   ```
   这样 `generateStackFromShape` 用 `sp.row` 写 spitter tile 时就是本体位置，target 反向在 demo 端 `spitterTargetPos()` 计算。

3. **fillEditorShape 校验里 target 计算**保持不变（用 `sp.row` + dir 偏移），因为 sp.row 现在是 spitter 本体。

### 四、改动文件

| 文件 | 改动 |
|---|---|
| `麻将消一消_在线/editor/index.html` | `buildEditorShape`:spitter tile 保留 dir/count；spitters 数组改存本体位置 |

### 五、心得

- **同名数组在不同上下文里含义不同**:`spitters` 数组在 buildEditorShape 里"应该是 spitter 本体位置"，但在编辑器别的代码里"存的是 target 位置"——这种语义漂移是 bug 的高发区。**函数返回的字段命名要稳定，含义要单一**——这里 spitters 应该就是本体位置（语义清晰），target 在需要时单独算。
- **多入口代码的 bug 容易被单入口测试遗漏**：fillEditorShape 路径不受 spitters 数组 bug 影响，所以即使按当前形状生成 OK 也掩盖了堆塔路径的 bug。**每个入口路径都要单独跑测试**，不要假设"修了一条就都修了"。
- **编辑器 → 生成器的数据契约要明确**：buildEditorShape 是生成器的唯一入口，它的返回值（tiles / specialTiles / spitters）必须明确每个字段的含义和坐标系。这种"内部约定"应该写在 JSDoc 里，避免后人误读。

---

## Day 26 · 8月22日（周六）：吐牌机机制重构 + 队列牌参与配对

### 一、用户反馈驱动重构

用户提了一个根本性问题：**"整个棋盘的麻将是能够两两消除的"**——之前队列牌 typeId 是从普通牌池随机选的，可能导致某张队列牌"找不到配对"（同花色普通牌被消完了）。

### 二、队列牌参与 backwardFill（核心修复）

**旧逻辑**：队列牌在 backwardFill **之后**生成，typeId 从普通牌池随机选，不参与配对计算。

**新逻辑**：队列牌在 backwardFill **之前**生成（typeId=null），参与配对计算，保证每张队列牌都有对应配对牌。

```
Queue tile spitterOrder=1 (layer=2): typeId=17 → matches normal tile typeId=17 ✓
Queue tile spitterOrder=2 (layer=1): typeId=31 → matches normal tile typeId=31 ✓
```

修改文件：
- `editor/generator.js`：队列牌生成移到 backwardFill 之前
- 移除"从普通牌已分配 typeId 里随机选"的逻辑
- 新增校验：`普通牌 + 队列牌` 必须偶数

### 三、吐牌机支撑校验

用户要求"吐牌机的麻将吐出的位置支撑限制未通过，则生成报错"。

新增校验：
- L1+ 吐牌机：target 位置必须有支撑（下方有下层牌托住），否则生成报错
- L0 吐牌机：target 在底层，地面就是支撑，无需校验

### 四、L0 吐牌机跳过支撑校验（用户纠错）

用户反馈："怎么第一层的吐卡机也会检测支撑"。

修正：L0 吐牌机跳过支撑校验（地面就是支撑），只有 L1+ 才需要检查。

### 五、count 语义变更（核心）

**用户原话**："count 等于 2，意思就是他有两张牌。然后他初始吐了一张牌，就还剩一张牌，那一张牌才是最后一张牌。"

**旧语义**：count=N = 1 初始牌 + (N-1) 队列牌
**新语义**：count=N = N 张队列牌（全部都是吐牌机吐出的牌），初始显示第 1 张

修改：
- 队列牌数量 = `sp.count`（不是 `sp.count - 1`）
- 最后一张队列牌（spitterOrder === count）点入卡槽时 spitter 消失

### 六、移除自动补充的支撑牌

用户反馈："支撑牌是什么鬼啊？怎么还有支撑牌？"

移除：
- target 位置自动补充的占位牌
- `autoPlacedSupport` 相关逻辑
- 奇偶校验简化为：`普通牌 + 队列牌` 必须偶数

### 七、吐牌机点入卡槽时立即吐出下一张

**用户原话**："第一张牌进入卡槽之后，第二张牌就要被吐出来了。"

**旧逻辑**：消除第 1 张队列牌后才吐出第 2 张（等 320ms 配对动画）
**新逻辑**：第 1 张点入卡槽时立即吐出第 2 张（不等配对）

实现位置：从 `triggerSpitters`（配对后）移到 `placeInSlot`（点入卡槽时）。

### 八、吐牌机消失时机

**用户原话**："他补出最后一张牌，并且这张牌进入卡槽的时候，他才会消失。"

最后一张队列牌（spitterOrder === count）点入卡槽时 spitter 立即消失（不等配对）。

### 九、多吐牌机时正确找到 owner

用户反馈："怎么我两个的涂卡机，我点击第一张的时候，它涂卡机就消失了？"

原因：`tiles.find` 只找第一个吐牌机，可能找错对象。

修复：遍历所有非耗尽的吐牌机，根据位置匹配找到真正的 owner。

### 十、吐牌机方向性弹出动画

**用户需求**："点击吐牌机吐出的第一张牌时，下一张牌有一种从吐牌机中吐出来的动画感觉。"

实现：
- 新增 `spitterRevealedDirs` Map 记录每个队列牌的弹出方向
- CSS 添加 4 套方向性 keyframes：
  - `spitterRevealUp`：从下方弹出（direction=up）
  - `spitterRevealDown`：从上方弹出（direction=down）
  - `spitterRevealLeft`：从右侧弹出（direction=left）
  - `spitterRevealRight`：从左侧弹出（direction=right）
- 弹出效果：scale(0.2) → scale(1.1) → scale(1)，配合 cubic-bezier(0.34, 1.56, 0.64, 1) 缓动

### 十一、吐牌机动画触发修复

用户反馈："点击其他的普通牌的时候，吐牌机上面的牌都会蹦一蹦、播放动画，这个显然是不对的"

原因：`spitterRevealedDirs` 在 `setTimeout(320ms)` 后才清除，期间任何 render 都会重新触发动画。

修复：在 `placeInSlot` 的 `render()` 调用后立即清除 `spitterRevealedDirs`。

### 十二、编辑器吐牌机重复点击可删除

**用户需求**："吐牌机的画笔在编辑器上也要加上'重复点击就消除'的功能"

修复：点击已有吐牌机时，不再要求 dir/count 匹配，直接删除。

### 十三、队列牌 layer 同层（不叠放）

用户需求："出来的牌的视觉上的叠放应该和普通牌一样"

但实际上用户后续澄清是"吐牌式"机制——一次只显示一张，点一张吐一张。

最终方案：所有队列牌放在同一层（与 target 同层），demo 根据 `spitterReleasedCount` 决定显示哪张。

### 十四、堆塔路径支持奇数队列牌

**用户反馈**："吐卡机的总容量如果为奇数的话，它就会说无法完成填色"

原因：`toEditorJSON` 强制让 shape 是偶数，叠加奇数 queue 后永远是奇数。

修复：
- `toEditorJSON` 新增 `skipParity` 参数
- 堆塔路径：队列牌总数为奇数时，`skipParity = true`，让 shape 也保持奇数
- 这样 fillTiles = 奇数 + 奇数 = 偶数，可以正常配对

### 十五、心得

- **用户反复纠错是好事**：count 语义从"1 初始 + N-1 队列"到"N 张队列"经历多轮对话，每次用户描述都比之前更清晰。**用户决策反转是好事**，别强行维护一致性，听用户的。
- **"两两消除"是最朴素的玩家预期**：如果有一张牌没有配对，玩家会觉得"这不是 bug 吗"。**typeId 应该由配对算法分配**，而不是随机分配——这是本轮修复的根本出发点。
- **前置校验 vs 后置校验**：堆塔路径的奇偶校验一开始是前置的（"队列牌总数必须偶数"），但堆上去的牌数是随机的，**总牌数奇偶由 stacked + queue 决定**，前置校验不合理。改为让 `backwardFill` 抛错 + try-catch，让重试机制处理。
- **动画 class 的生命周期**：CSS 动画只在 class 首次添加时播放。如果 `spitterRevealedDirs` 不立即清除，每次 render 都会重新触发动画。**"立即 render + 立即清除"是关键**。
- **同名数组在不同上下文里含义不同**：`tiles.find` 在单吐牌机时没问题，多吐牌机时就找错对象。**遍历所有匹配项**比"找第一个"更稳妥。

---

## 待跟进

- [ ] 浏览器实测：完整跑一遍"画底座→堆塔生成→预览→点击队列牌→spitter消失"的流程
- [ ] 与另一位策划协作测试多吐牌机场景
- [ ] 难度公式校准（之前已搁置，等数据积累后继续）

---

## Day 27 · 8月22日（周六）：吐牌机队列压力维度

### 一、用户反馈

加入吐牌机后，原难度公式（可点率+层数+钩子+暗钩+槽位压力）没法准确反映吐牌机的难度：
- 队列牌按顺序释放，玩家看到第 1 张后才能意识到"后面还有几张不知道的"
- 颜色未知，每张都得"揭开才知道""

### 二、用户洞察（核心）

> "吐牌机就是第一张牌以后的那些牌，跟暗牌几乎差不多，只不过每一张牌都约等于一个暗钩的。"

**关键等价**：每张队列牌 ≈1 个暗钩贡献难度。

### 三、用户进一步纠错（智能判断）

> "如果吐牌机的第一张牌可以立即与牌面上的另一张普通牌进行消除，那下一张牌基本上就不算是暗钩了，因为消除这一张之后，它立刻就出来了。"

**关键洞察**：队列牌的"暗钩属性"取决于它前面那张能否立即消除。
- 第 1 张能消 → 不算暗钩（出牌即消）
- 第 1 张消了 → 第 2 张立即露出 → 第 2 张如果出牌前"等于可见"，也不算暗钩
- 找到第一张**无法立即配对**的队列牌，从它开始才算压力

### 四、新维度公式（智能版）

```
遍历队列牌（按 spitterOrder）：
  对第 i 张，检查它的 pair（同 typeId 的另一张牌）是否在场上且未被埋
  如果 pair 可见 → 这张牌立即可消 → 不算压力，继续下一张
  如果 pair 被埋或不存在 → 这张牌及之后所有牌都算压力

hiddenCount = 第一张不可立即配对的牌及之后的数量
spitterQueuePressure = (0.5 + hiddenCount - 1) × 8
```

| 场景 | hiddenCount | pressure |
|------|------------|----------|
| 3 张队列牌，全部 pair 都可见 | 0 | 0 |
| 3 张队列牌，前 2 张可见，第 3 张的 pair 被埋 | 1 | 4 |
| 3 张队列牌，第 1 张的 pair 就被埋 | 3 | 20 |

### 五、完整公式（含新维度）

```
难度分 = (1-可点率)×40 + 层数×8 + min(有效钩子/8,1)×30
 + min(有效暗钩/8,1)×20 + min((minSlots-2)/4,1)×30
       + spitterQueuePressure + 10
```

### 六、代码实现

```javascript
function computeSpitterQueuePressure(tiles, queueTiles) {
  if (!queueTiles || queueTiles.length === 0) return { pressure: 0, hiddenQueueCount: 0 };
  const sorted = queueTiles.slice().sort((a, b) => a.spitterOrder - b.spitterOrder);
  let hiddenCount = 0;
  for (const qt of sorted) {
    const pair = tiles.find(t => t.id !== qt.id && t.spitterOrder == null && t.typeId === qt.typeId);
    if (pair && !isTileBuried(pair, tiles)) {
      continue; // 立即可消，不算压力
    } else {
      hiddenCount += sorted.length - sorted.indexOf(qt);
      break;
    }
  }
  const pressure = hiddenCount > 0 ? (0.5 + hiddenCount - 1) * 8 : 0;
  return { pressure, hiddenQueueCount: hiddenCount };
}

const queueTiles = tiles.filter(t => t.spitterOrder != null);
const queueResult = computeSpitterQueuePressure(tiles, queueTiles);
const spitterQueuePressure = queueResult.pressure;
);
```

返回对象增加字段：`queueCount`、`spitterQueuePressure`。

### 七、实测验证（智能版）

测试场景：5 普通牌 + count 张队列牌（同花色配对），所有 pair都在 L0（可见未埋）。

| 队列牌 | hiddenQueueCount | pressure | 说明 |
|--------|-----------------|----------|------|
| 1 | 0 | 0 | 唯一1 张立即可消，不算压力 |
| 3 | 0 | 0 | 所有配对都可见，全部立即可消 |

智能版公式正确：只算"实际不可见/不可消"的队列牌。如果所有队列牌都能立即配对，就完全不算压力。

### 八、UI 更新

- 目标分数问号弹层：补充吐牌机队列压力维度说明 + 设计理由
- 变更日志：v4.6.41
- 版本号：APP_VERSION、version.json 同步

### 九、心得

- **维度叠加 vs 百分制**：用户明确说"现在是累加状态，不要百分制"。确实，对于难度评估，**累加 + 封顶**比百分制更直观（每个维度上限明确，分数直接可读）。
- **"约等于"是设计捷径**：用户说"约等于一个暗钩"，不用纠结精确数学，**找最相近的现有维度做类比**是设计新维度的有效方法。
- **首张折半 vs 全量计算**：完全按暗钩算会让"1 张队列牌的关卡"被过度惩罚。**已可见/未知**是合理的折半依据。
- **"立即配对"是关键洞察**：用户再次纠错——如果第1 张就能立即配对，它就不算暗钩。**从前向后找第一个不可立即配对的牌**才是正确的算法，盲目按数量算会高估难度。

---

## Day 27 · 8月22日（周六）：旋转机制设计 + 探测分数bug修复

### 一、用户提出新机制：Vita 麻将旋转

**机制描述**：
- 画一个 5×5 底盘（外圈一圈麻将）
- 每点击任何一张牌进入卡槽，整个外圈顺时针旋转一格
- 旋转后牌的可见性变化：原来露出的牌被压住、原来被压的牌露出
- 配对关系（typeId）不变，只是位置变化

### 二、JSON 数据结构设计

在关卡 JSON 顶层加 `rotation` 配置（和 `specialTiles` 平级）：

```json
{
  "levelId": 1,
  "totalPairs": 25,
  "tiles": [...],
  "specialTiles": [],
  "rotation": {
    "centerRow": -2,
    "centerCol": 0,
    "radius": 4,
    "direction": "clockwise",
    "trigger": "onAnyClick"
  }
}
```

**字段说明**：

| 字段 | 含义 | 示例 |
|------|------|------|
| `centerRow/Col` | 旋转中心（棋盘坐标） | (-2, 0) |
| `radius` | 旋转半径（只旋转外圈 N 格内的牌） | 4 |
| `direction` | 旋转方向 | `clockwise` / `counterclockwise` |
| `trigger` | 触发时机 | `onAnyClick` / `onQueueRelease` |

### 三、为什么 JSON 不需要改 tile 结构

旋转是在**棋盘坐标层面**重新映射 tiles 数组的 position 字段：
- 旋转前：`(row, col)` 在外圈
- 旋转后：`(row, col)` 旋转 90° 到新位置
- 配对逻辑（typeId）不变 → 两两消除关系不变
- 压牌关系可能变（之前露的牌可能被压住）

程序同学只需要：
- 每帧根据当前旋转状态重新计算每个 tile 的 position
- 触发旋转时只更新 position 字段，typeId 不动

### 四、编辑器 UI 设想

在画笔栏加 **"🎡 旋转"** 按钮（和吐牌机并列）：

```
[普通] [暗牌] [⚙ 吐牌] [🎡 旋转]
```

点击后：
1. 在棋盘上点一个位置 → 设置旋转中心
2. 弹出小输入框 → 设置半径和方向
3. 旋转中心用红色十字标记，半径圈用虚线高亮

### 五、对难度公式的影响（新维度设计）

新增 `rotationPressure`：

```javascript
rotationPressure = radius × 1.5
```

| 半径 | rotationPressure | 含义 |
|------|-----------------|------|
| 0 | 0 | 无旋转机制 |
| 3 | 4.5 | 小范围（外圈） |
| 5 | 7.5 | 中范围 |
| 8 | 12 | 大范围（几乎全棋盘） |

**设计理由**：旋转会打乱玩家的预判，每次点击都会"洗牌"，半径越大越需要实时规划。

### 六、bug 修复：探测分数范围报 "spitters is not defined"

**问题**：`probeScoreRange()` 调用 `buildEditorShape()` 时只解构了 `tiles`，没有解构 `spitters`，导致后续使用时报 ReferenceError。

**修复**：改为 `({ tiles, spitters } = buildEditorShape())`。

**教训**：解构返回值时必须列出所有需要的字段，或者用 `({ tiles, ...rest } = ...)` 配合命名访问。重构返回结构时，**所有调用点都要同步更新**。

### 七、心得

- **机制设计 vs 数据结构**：对于"位置会变"的机制（如旋转），最干净的做法是**在数据结构顶层加配置**，而不是改每个 tile 的字段。这样程序只需要重新映射 position，typeId 关系不变。
- **用户提机制时必问的三个问题**：(1) 触发时机是什么？(2) 影响范围多大？(3) 是否影响配对关系？旋转的答案是 onAnyClick / 半径圈 / 不影响。这三个回答决定了数据结构长什么样。
- **重构时的隐性破坏**：改动 `buildEditorShape()` 的返回值（加了 spitters 字段）后，调用方如果不更新解构就会静默报错。**TypeScript 能避免这类问题**——但当前项目是纯 JS，只能靠仔细 review。

---

## Day 28 · 8月24日（周一）：吐牌机交互重做 + 放置逻辑理顺 + 工作台关卡预览

### 一、吐牌机"再次点击即消除"（对齐普通牌）

**问题**：之前把"再次点击吐牌机"误做成"双击"，用硬编码 400ms 窗口检测，导致用户稍慢就删不掉。

**修复**：spitter 画笔下，**再次单击已放置的吐牌机 → 直接消除 + 清理对应队列牌 stack**。和普通牌"点已存在的牌就擦掉"完全一致，不再依赖双击。

**心得**：用户说"再次点击就消"时，指的是**单击 toggle**，不是鼠标双击（双击要遵守系统双击速度且手感差）。以后遇到"再点一次"默认做 toggle，别做双击。

### 二、吐牌机放置左右不对称 bug

**问题**：正中间放一个吐牌机后，右侧能放（±2、±4）、左侧却一个都放不下（反之亦然）。

**根因**：`paintAt` 的 spitter 放置分支有一个**镜像位置重叠检查**——即使 spitter 明确是"不对称放置"（写入时跳过镜像），这个检查仍假设镜像位置会放，于是"哪边已放，就把另一边镜像全拦住"。

**修复**：删除 spitter 放置时的镜像重叠检查。现在左右对称，只保留"同层 spitter 视觉 2×2 重叠（邻接 1 cell 拒绝）"。

### 三、同层禁止把吐牌机塞进两张普通牌中间的缝

原逻辑只检查"与其他吐牌机重叠"，完全忽略普通牌，所以吐牌机能自由落进两张普通牌中间。现在跟普通牌一致：

- 视觉 2×2 **完全落在空处** → 允许
- **恰好覆盖一张普通/暗牌的 2×2**（对齐）→ 允许（替换）
- 与任何普通牌**部分重叠**（夹缝，shared 1~3）→ 禁止

跨层不限制（上层吐牌机跨压下层两张牌仍可）。

### 四、吐牌机"输出目标位"也要做支撑校验

之前只校验吐牌机本体有没有支撑，导致"吐牌机有底，但它吐出来的牌落点下面是空的"这种情况。

**修复**：新增 `hasSupportAtLayer(layerIndex, col, row)` 通用函数（`hasSupportAt` 改为复用）。放吐牌机前先算目标位 `getSpitterTarget(...)`，若当前层 > 0 **且目标位下方无支撑** → 禁止放置 + toast；目标位越界也提前拦截（不再放置后才报错）。

**注意**：这只限制"落下的牌要有底"，不影响之前要保留的"跨层本体布局自由"。

### 五、非当前层吐牌机淡出

切到第二层时，第一层的吐牌机是纯橙色，太抢眼压住第二层。现在**只对非当前层上的 spitter（含队列牌）**做半透明淡出（globalAlpha 0.4），普通麻将牌保持不透明。

### 六、工作台新增"关卡预览" demo

`workbench.html` 每行加"预览"按钮，弹出一个内嵌 demo iframe（`/demo/index.html`）的模态层：

- 用 demo 已支持的 `parent.postMessage({ type:'loadLevel', level }, '*')` 协议传关卡
- 首次点击先加载 iframe，等 `onload` 注册好 message 监听再发（避免首帧丢失）；之后直接发
- 关闭时清空 iframe `src`，停止 demo 后台循环
- 本地静态服务器 `/levels` API 不可用（连不上属正常），线上部署版才能连上取关卡

### 七、版本与变更日志

- 版本提升至 **v4.7.0**（新增功能 → 次版本 +1）
- `editor/index.html` 变更日志 `#changelogPop` 顶部新增 v4.7.0 条目
- `version.json` version/updated/notes 同步为 v4.7.0，changelog 顶部新增对应条目
- `APP_VERSION` 更新为 `v4.7.0`（与 version.json 一致，触发协作者强刷）

### 待办

- 网络恢复后 `git push origin main`（本地已 commit）
- 需把 docs/工作日志.md、版本号等同步到部署副本后推送

---

## Day 28 补充 · 8月24日（周一）：追踪"堆塔上层牌延伸出来"的真凶 = demo 渲染层偏移(非生成器)

### 一、现象
用户反馈:按底座堆塔生成的麻将,上层"延伸出来、底下没牌托着、不满足支撑";并强调是**加了吐牌机之后**才坏的,之前是好的。

### 二、排查过程(误导与澄清)
- 以为生成器漏了支撑判断 → 给 `generateStackFromShape` 加了 `levelHasFullSupport()` 兜底(最终输出每张可消牌都验支撑,悬空则重搭)。**这确实堵住了一个真 bug**(mirror 展开/偶化删牌/回填重排会让上层牌悬空),但**拿用户真实底座(3×4,12张)跑的生成数据是干净金字塔**(12→8→4→2,零悬空),说明生成器数据没问题。
- 一度怀疑是浏览器缓存/旧代码,反复让强刷。

### 三、真凶:demo 渲染的 layerOffset 过大(吐牌机时代改的)
`demo/index.html` 渲染普通牌时:
```js
const layerOffset = 0.85;
const x = offsetX + (t.col-minCol)*cell - t.layer*layerOffset*cell;
const y = offsetY + (t.row-minRow)*cell - t.layer*layerOffset*cell;
```
每层累积向左上漂 `层数×0.85×格子`(格子最大 40 → 每层约 34px)。L1 保持底座全宽又左漂,探出底座左边;L2/L3 漂 70~100px → 视觉上上层**延伸出底座**。

提交历史里 `layerOffset` 为了露吐牌机被一路调大 `0.2→0.4→0.6→0.85`,副作用是把**普通牌**层叠也越拉越大,才"看起来"支撑坏了。

### 四、修复
把 demo 的 `layerOffset` 从 **0.85 调回 0.3**(保留 3D 层叠,不再漂出底座)。吐牌机本来用 `layerOff=0`,不受影响。**生成器没改它的堆形逻辑。**

### 五、经验
- **"延伸出来/没支撑" 未必是生成器几何错,可能是渲染层偏移造成的视觉假象。**数据正确≠看着正确。
- 排查时先用 git 历史对比"曾经正常 vs 现在",比在生成器里反复加 check 更直接——问题往往是**渲染/偏移参数**被某个功能(吐牌机)改大了。
- 用户举例"金字塔"只是说"每张牌要有支撑",**不是真的要求堆成尖塔**——上层往上收窄是支撑限制的自然结果,别为了"尖顶"去加代码。

### 六、同步说明
demo/index.html、工作台预览、编辑器试玩用的是**同一个文件**,改一处三处生效,无需额外同步(主目录 outputs 已废弃)。

---

## Day 28 补充 2 · 8月24日（周一）：暗牌总数语义改直观 + 确认被夹=钩子

### 一、暗牌总数 = 0 时"真的没有暗牌"
**现象**：用户把"暗牌总数"调到 0，但还有暗牌。
**根因**：`assignDarkTiles` 里"暗钩"（把钩子的被埋牌标暗）是**独立**于"暗牌总数"运行的——总数设 0 只关了"随机补暗牌"，暗钩还是把钩子埋牌标暗。用户觉得荒谬："没有暗牌，哪来的暗钩？"

**改法**：让"暗牌总数"成为**总上限**：
- `totalCount <= 0 / undefined` → 直接返回 `[]`，**一张暗牌都没有，自然也没有暗钩**。
- `totalCount > 0` → 暗钩数量 = `min(钩子对数×暗钩比例, 总数上限)`，随机的补满到上限。
- 语义：**暗钩 = 暗牌总数里，标在"钩子被埋牌"上的那部分（由暗钩比例决定）**。总数=0 → 暗牌/暗钩都为 0；总数>0 → 按比例把一部分暗牌放到钩子上。

> 注：这**废掉了旧的"纯钩子模式"**（以前"暗牌总数0% + 暗钩100%"＝只有钩子暗）。现在想"只有钩子暗"＝把暗牌总数调>0、暗钩比例≈100%。

### 二、确认：被夹也算钩子（不只被压）
用户强调，确认并记档：`isTileBuried` 判定"被埋" = **上压(被上层牌盖住) OR 左右夹(同层左右都被挡住)** 都算。所以**被夹住的牌同样参与钩子**，不是只有被压的才算。（策划案 11.1 已记录此定义。）

### 三、涉及
- `editor/generator.js`：`assignDarkTiles` 加"总数≤0→无暗牌"早退 + 暗钩封顶在总数内；`isTileBuried`（本就含左右夹）。
- `editor/index.html`：暗牌总数说明改为"0%＝无暗牌（也无暗钩）"。
- 待同步：策划案暗牌语义说明。

## Day 29 · 8月25日（周二）：目标分数→可点率反驱动 + 导入支持形状+TypeID + 一串工作台/网络修复

### 一、目标分数 → 可点率 反向驱动（堆塔自动调层）
用户：放了个 4×5 底座，想要 60~70 分，但怎么生成都 85~90；并希望"目标分数低 → 可点率相对高、目标高 → 可点率低"。

**先量化再实现**：实测 4×5 平底，层2→64分/可点22%、层3→76/18%、层4→86/12%、层5→94/10%。结论：可点率**只由塔形几何（埋多深）决定，花色填充改不了**；450 要的角色性其实就是堆层数。

**落地**：`generateStackFromShape` 单关+批量都按目标分数**自动调层数**起步自用户设的层数，够不到目标才调：目标低→降层（可点率↑）；目标高→升层（可点率↓）。加 `desiredClickRatio(scoreMid)` 软目标；结果状态显示 可点%。探测分数范围改为**扫层 2~7** 报真实区间。
- 浏览器实测：目标 60~70 → 自动降到 2层/67分/可点22%；目标 95~105 → 升到 4层/95分/可点12%，双向符合。

### 二、一串 bug/网络修复（v4.8.x）
1. **探测"本底座可出分数"按钮无反应**：改扫描层数时残留对已删变量 `N` 的引用（未声明→ReferenceError，函数没跑）。修（v4.8.1）。
2. **推塔生成后右侧预览不出现**：预览起始对 demo 的 `HEAD` 探测把"超大文件（6.3MB）HEAD 被取消 ERR_ABORTED"误判为"demo 缺失"→ 整场禁用预览。改为**仅确认 404 才禁用**，其余照常加载（v4.8.2）。核实：demo 本地渲染正常（98张），是加载太重+网络慢。
3. **网络现实**：大陆直连 `mahjong-90y.pages.dev`（Cloudflare Pages 免费版）/GitHub 很慢不稳，用户需**开梯子**才能秒开（尤其 6.3MB 的 demo）。push 反复失败→用后台重试。
4. **保存到工作台后刷新看不到**：口令存 `sessionStorage`（**每标签独立**），工作台另一标签 getPass 为空→`refresh()` 静默不加载。改 **localStorage** 跨标签共享（v4.8.3）。
5. **工作台列表一直停旧快照**：Function 读 GitHub `data/levels`——单关读取**实时**、唯独**目录列表子请求**被 Cloudflare 缓存（列表恒 49、git 已 56）。给 Function 的 GitHub 子请求加 `cache:'no-store'`（v4.8.5）。工作台 API 响应也加 no-store（v4.8.4）。**注意：Cloudflare 从 Day 28 末到 Day 29 持续没把这几版部署上去（webhook/构建链路卡住），已建议用户后台手动触发构建。**

### 三、v4.9.0 · 导入支持「形状 + Type ID」
用户要"导入时能带上 TypeID（牌面）"。
- **改法（无需模式开关）**：导入 `loadFromEditorJSON` 读普通牌的 `typeId` 存进 state（文件没有则仍全 null＝"只导形状"，一次性兼容）。`serializeLayers` 是 stringify，typeId 随 undo/redo/自动保存天然带过。
- **显示**：工具条新增「🀫 牌面ID」开关，`drawTile` 在普通牌中央画 typeId 数字。
- **生成**：`buildEditorShape` 带过 typeId；`fillEditorShape` 检测"完整导入（无队列牌、每张都有 typeId、计数成对、`forwardEliminable` 可解）"→ **直接保留原花色复现同一关**；不完整/不可解→照旧 backwardFill 全量重填（结果仍合法）。加了 `forwardEliminable`（正向贪心可解检查）护栏。
- **导出**：写回普通牌 typeId。
- 验证：node 完整导入 typeId 逐位一致；部分/不可解回退重填且可解；浏览器端到端 shapeIds===outIds。

### 待办
- Cloudflare 持续未部署，需后台手动触发构建或等 webhook 恢复。

## Day 30 · 8月26日（周三）：「保存后看不到」真正根因落定 + 工作台删除幂等化

### 一、保存到工作台"看不到新关"——真根因落定（v4.9.6，彻底根治）
之前多轮的"缓存/破缓存"（v4.9.1~v4.9.5）都在治错症。这次用诊断字段实测定位到**真正根因**：

> **Cloudflare Worker 单次调用最多 50 个 fetch 子请求。** 旧实现列列表 = 逐个读每个关卡文件（69 关 → 70 次 fetch），超出上限被砍剩 **47**。所以**最新保存的关卡永远排不进列表**——我和另一位同学两人的存档都中招。

**修复**：所有关卡元数据聚合存到 `data/levels/_index.json`（带 name/order/status/playTime/difficulty 等）。列列表 = **1 次读取**，不再逐关 fetch、不再超限；保存/改名/排序/删除都走 `syncIndexMeta` 同步索引 → **保存后立即实时可见**。单关读取仍走 `raw.githubusercontent.com` + 每次随机 nonce `_t` 破缓存。

**另加可靠性兜底**：函数已写好 **Cloudflare KV** 支持（绑定了名为 `LEVELS` 的 KV 则列表读写全走 KV，不经 HTTP 子请求、无缓存、无 50 上限；未绑定则优雅回退 raw 索引）。代码就绪，但**需用户在 Cloudflare 后台手动加 `LEVELS` KV 绑定**（Settings→Functions→KV namespace bindings）才能真正免疫陈列表读出"幽灵关"。

**验证**：`/levels` 从 47 提升到完整 69→66（协作删关后），含用户"第二关"，HTTP 200。

### 二、工作台"点击删除显示删除失败"——幂等化（v4.9.7）
列表兜底路径读 raw 索引偶发陈旧 → 工作台会显示**幽灵关**（其文件其实已被删，如协作者删过）。此时删除 → 后端删文件 404 → 前端报"删除失败"。

**修复**：DELETE 改为**幂等**——文件存在则删；文件已不存在则仅清索引条目；两种情况都返回成功（带 `ghost:true` 标记）。幽灵关随手点一下就清掉，不再误报。已用"对已删关再次 DELETE"实测：新代码返回 `{"ok":true,"ghost":true}` + 200（旧代码是 404）。

### 三、推送脚本踩坑：被误判的"PUSH OK"
我的后台推送循环用 `grep "main -> main"` 判成功，但它也会匹配 **rejected** 输出行 `! [rejected] ... main -> main` → 误报成功，导致 v4.9.7 一直没真推上去（本地以为已部署）。**教训：判断 push 成功不要 grep 输出，应对比 git 远端 HEAD 与本地 commit 的 SHA**。后面改用 `ls-remote` 校验后才确认落地。

### 四、清理测试残留
端到端测试存的临时关（第三关-E2E / TMP-DELETE-TEST / TMP-GHOST 等）已从实时列表清干净。当前线上列表干净 66 关，含正式关。

### 待办
- **用户后台补 `LEVELS` KV 绑定**（约 2 分钟），根治陈旧列表/幽灵关，让保存/删除彻底稳定。
- 观察协作者删关是否正常（已见一次"129"被工作台正常删除）。


## Day 35 · 8月30日(周日):圆盘机制 + 推送脚本稳态

### 一、圆盘机制(v4.10.0,新增功能)

需求:圆盘插入在两层麻将之间(不占层),盖住麻将阻止点击,被麻将盖住时该麻将消除则圆盘淡出消失,所有圆盘消失即通关(与'麻将全清'任一即可)。半径固定 = 1 张麻将长度,不通过生成器,手动放置。

实现:
- **编辑器**:画笔面板新增「🟣 圆盘」+「插入到第 N/N+1 层之间」下拉(L0/L1 ~ L4/L5 五档),选哪个缝哪个缝以上的层半透明预览;点画布 = 放置,再点同位置 = 删除;与所在层麻将重叠时画笔拒绝放置;discs 作为独立数组与 layers 并列存 JSON(serializeState/restoreState/exportJSON/buildEditorShape 全链路打通)
- **Demo**:SVG 单节点批量绘制圆盘(避免 N 个 div 性能开销),定位用 cellPx/offsetX/offsetY 与牌的 3D 层偏移同步;新增 `discOverlapsTile` 子格级重叠判定、`discHasUpperTiles` 上方麻将检查、`sweepDiscsToFade` 任意麻将被消除后触发淡出、`animateDiscFade` 用 setInterval 链驱动淡出(原 RAF 在浏览器面板未激活时会被暂停,改 setInterval 更稳);`isClickable` 加 disc 遮挡分支
- **工作台后端**:`functions/levels/[[path]].js` 的 GET/POST 都加 `discs` 字段透传,保存到工作台随 discs 一起写
- **示例关卡**:`demo/示例关卡/示例_圆盘.json`(5 圆盘),URL 加 `?sample=disc` 加载(init 加 SAMPLE_FILE_MAP 别名映射)
- **通关条件**:`checkWin` 改为 `allTilesCleared || allDiscsCleared`(任一即胜利);disc 淡出完成时自动 checkWin

核心四点验证(都用 preview 实测):
1. ✓ disc 覆盖麻将阻止点击(`isClickable` 返回 false)
2. ✓ disc 被麻将覆盖(圆盘作为"钥匙"):消除上方麻将 → `sweepDiscsToFade` 触发 → `discAlpha` 从 0.05 增长到 1(12 帧 × 60ms ≈ 720ms)
3. ✓ 所有 disc 消失 → `gameOver = true`(实测 5 个 disc 全清后 gameOver 立刻变 true,胜利消息弹出)
4. ✓ 固定半径 = 1 张麻将长度(子格 × 2),不改

### 二、推送 rebase 流程固化

推 v4.10.0 时遇 remote 有新 workbench idx update commit(协作期间保存的),本地 push 被拒(non-fast-forward)。已加 `git pull --rebase origin main` 步骤,rebase 后再 push 成功(commit 2785168)。规则记下:push 前先 ls-remote 看 remote SHA 与本地是否一致,不一致就 rebase。

### 待办
- 等 Cloudflare 部署生效,实际加载 demo `?sample=disc` 端到端跑一遍(预览环境已验证)
