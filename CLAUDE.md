# 麻将消一消 · 项目说明书(给 Claude Code 的工作指引)

> 本文件是 Claude Code 的项目上下文说明,每次对话自动加载。
> 最后更新: 2026-08-18

---

## 一、项目概述

**项目名称**:麻将消一消(Mahjong Match-3)
**类型**:单机休闲消除手游
**核心玩法**:经典"上海麻将"类二消——点击两张相同花色且左右不被挡住的牌即可消除,特殊牌含暗牌(需翻面再消除)和吐牌机(消耗次数补充新牌)。底层采用 6 列 × 8 行的棋盘,多层 3D 堆叠(奇数层半格偏移)。
**当前阶段**:MVP 早期,重点是**批量关卡生产工具链**——关卡数量需求大(数百关),手工设计不现实。

---

## 二、双目录结构(核心!)

本项目有**两个目录**,职责不同,切勿混淆:

| 目录 | 角色 | 是否 git | 说明 |
|---|---|---|---|
| `麻将消一消/` | **主工作目录** | ❌ 不是 | 所有开发、编辑、文档维护都在这里。当前 Claude 对话的默认工作目录。 |
| `麻将消一消_在线/` | **部署副本** | ✅ 是 | git 仓库,推送到 GitHub,部署到 Cloudflare Pages。只放行"上线版本"。 |

### 关系示意

```
麻将消一消/                    ← 你在这里(主工作目录)
├── 麻将消一消_工作日志.md     ← 工作日志"母本"(在这里编辑)
├── 策划案_编辑器工作台难度v2.md ← 策划案"母本"(在这里编辑)
├── 使用说明.md / 难度分说明.md / ...
└── outputs/
    └── 关卡搭建工具包/        ← 实际开发的地方
            ├── generator.js       (生成器 + 难度公式)
            ├── index.html         (编辑器)
            ├── workbench.html     (工作台)
            └── ...

         ↓ 开发完成后,选择性同步 ↓

麻将消一消_在线/               ← 部署副本(git 仓库)
├── editor/                    ← 从工具包的 index.html + generator.js 复制
├── workbench.html             ← 从工具包复制
├── demo/                      ← 游戏 demo
├── docs/                      ← 工作日志/策划案(从主目录母本复制)
│   ├── 工作日志.md
│   ├── 策划案_编辑器工作台难度v2.md
│   └── ...
├── functions/                 ← Cloudflare Pages Function(API)
├── version.json / package.json
└── .git/                      ← remote: github.com/zhangjiwei0221/mahjong
```

---

## 三、同步规则

### 3.1 工作日志 / 策划案 / 说明文档

- **编辑位置**:主工作目录 `麻将消一消/` 根目录
  - `麻将消一消_工作日志.md`
  - `策划案_编辑器工作台难度v2.md`
  - `使用说明.md` / `难度分说明.md` / `游戏Demo说明.md`
- **同步到部署副本**:开发完成后,复制到 `麻将消一消_在线/docs/`
- **命名差异**:主目录用全名(工作日志.md),部署副本在 docs/ 下
- **更新时机**:
  - 工作日志:每次改动后,添加 Day X 条目
  - 策划案:重大改动时更新版本号

### 3.2 工具包(编辑器 / 工作台 / 生成器)

- **开发位置**:`麻将消一消/outputs/关卡搭建工具包/`
- **同步到部署副本**:
  - `关卡搭建工具包/index.html` + `generator.js` → `麻将消一消_在线/editor/`
  - `关卡搭建工具包/workbench.html` → `麻将消一消_在线/workbench.html`
- **注意**:部署副本的结构和工具包不同(editor/ 单独目录,generator.js 在里面)

### 3.3 游戏 Demo

- **位置**:`麻将消一消/outputs/游戏Demo/`
- **同步到部署副本**:`麻将消一消_在线/demo/`

---

## 四、推送流程(部署到公网)

### 4.1 前置步骤(在主工作目录完成)

1. 开发改动完成(工具包内)
2. 更新工作日志 + 策划案
3. 测试通过

### 4.2 同步到部署副本

```bash
# 工作日志/策划案
cp "麻将消一消/麻将消一消_工作日志.md" "麻将消一消_在线/docs/工作日志.md"
cp "麻将消一消/策划案_编辑器工作台难度v2.md" "麻将消一消_在线/docs/策划案_编辑器工作台难度v2.md"

# 编辑器
cp "麻将消一消/outputs/关卡搭建工具包/index.html" "麻将消一消_在线/editor/index.html"
cp "麻将消一消/outputs/关卡搭建工具包/generator.js" "麻将消一消_在线/editor/generator.js"

# 工作台
cp "麻将消一消/outputs/关卡搭建工具包/workbench.html" "麻将消一消_在线/workbench.html"
```

### 4.3 Git 提交并推送

```bash
cd "麻将消一消_在线"
git add -A
git commit -m "描述"
git push origin main
```

### 4.4 部署

- **Cloudflare Pages**:git push 后自动部署(几分钟生效)
- **旧 Netlify**:已冻结,不再使用
- **域名**:`mahjong-90y.pages.dev`

### 4.5 版本号同步

每次推送,同步更新两处:
- `麻将消一消_在线/version.json` 的 `version`(commit hash 前 7 位)
- `麻将消一消_在线/editor/index.html` 的 `window.APP_VERSION`

---

## 五、协作规范

### 5.1 工作日志更新

- **时机**:每次改动结束后,用户说"更新工作日志"时
- **格式**:`## Day X · 月.day(周X):一句话主题`
- **内容**:当天改动清单、决策理由、遇到的问题、待办
- **位置**:先在主目录编辑,同步到部署副本

### 5.2 变更日志与版本号

**版本号规则(三段式语义化版本)**:`v主版本.次版本.修订号` 例: `v4.3.1`

| 变更类型 | 版本号变化 | 举例 |
|---|---|---|
| 重大变更 | 主版本 +1 | v4.x → **v5.0**(架构重写、玩法变化) |
| 新增功能 | 次版本 +1 | v4.3 → **v4.4**(新增功能、重要改进) |
| 修复/小调整 | 修订号 +1 | v4.3 → **v4.3.1**(修复 bug、UI 调整) |

**变更日志位置**:`麻将消一消_在线/editor/index.html` 中的 `#changelogPop` 弹层

**更新时机**:每次改动完成后,追加一条新版本记录(最新的在最上面)

**格式**:
```html
<div class="gh-item">
  <div class="gh-title">v4.3.1 · 2026-08-18</div>
  <div class="gh-desc">
    • 修复内容 1<br>
    • 修复内容 2
  </div>
</div>
```

### 5.3 策划案更新

- **时机**:重大改动(架构变化、新系统、重大交互调整)
- **位置**:先在主目录编辑,同步到部署副本

### 5.4 双人协作注意

- 另一策划也在用这个工具 → 版本检测(version.json + APP_VERSION)会在新版本部署后提示强刷
- 数据(关卡 JSON)存在 GitHub `data/levels/`,两人共享
- 口令环境变量:`WORKBENCH_PASSWORD`(当前值 `1`)

---

## 六、当前进度(2026-08-31)

### v4.10.6.2 已推送(本地)

圆盘 z-index 分层:按 gapLayer 分组,每组一个 SVG,z-index=(gapLayer+1)*1000 → L(N+1) tile(z ≥ (N+1)*1000+1)压在 disc 之上(tile 视觉压 disc),L(N) tile(z < (N+1)*1000)在 disc 之下(disc 盖 L(N))。修复 v4.10.6.1 的方向错误(此前 single SVG z=50000 永远在最上,L(N+1) tile 被圆盘压住,视觉上 L1 tile 在圆盘后面)。disc 半径同步回到 2 cell。

### v4.10.6.1 已推送(b40280d)

五组圆盘机制修复:
1. 工作台预览秒开(`closePreview` 保留 iframe,二次预览 0ms)
2. 编辑器生成关卡保留圆盘数据(`discs` 全链路打通:三处解构 + 生成器三个函数)
3. 圆盘机制三个 demo bug:空 disc 立即消失 / 视觉盖住上层 tile / render 时 board 未 layout 保护
4. 圆盘 disc 上方最后一张 tile 进卡槽后该 disc 不淡出(`checkMatch` 开头加 `sweepDiscsToFade`,无论是否配对都扫一遍)
5. 圆盘半径 2 cell → 2.5 cell 修复 L1 tile 4 角凸出 disc 圆外

### 待办

- 云端验证 v4.10.6.1(L1 tile 4 角完全在 disc 圆内)

---

## 七、常见问题

**Q: 为什么有两个目录?**
A: 历史原因。工具包是开发时的自包含包(可发压缩包给协作者),部署副本是 git 仓库。开发在主目录,部署时同步。

**Q: 工作日志/策划案在哪编辑?**
A: 主目录 `麻将消一消/`。编辑后复制到 `麻将消一消_在线/docs/`。

**Q: 推送前要做什么?**
A: 主目录开发完成 → 同步到部署副本 → git add/commit/push → 更新 version.json + APP_VERSION。

**Q: 网络不通怎么办?**
A: commit 会留在本地,网络恢复后 `git push origin main` 即可。不要重复 commit。

---

## 八、相关资源

- **GitHub 仓库**:https://github.com/zhangjiwei0221/mahjong
- **部署地址**:https://mahjong-90y.pages.dev
- **Netlify 旧域名**:已冻结,仍可访问旧版本
