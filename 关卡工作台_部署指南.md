# 关卡工作台 · 部署指南（Netlify Functions + Blobs）

> 给关卡编辑器加上「共享工作台」功能后，就不能再用 Netlify Drop（纯拖文件夹）了，因为需要后端函数存数据。
> 本说明一步步带你迁到 **GitHub 仓库 + Netlify 绑定**（全免费，一次性设置）。

---

## 前置准备

你需要：
- 一个 **GitHub 账号**（免费，https://github.com 注册）
- 一个 **Netlify 账号**（免费，你已经有了）
- 你本地的 `麻将消一消_在线/` 文件夹

---

## 第一步：把 `麻将消一消_在线/` 上传到 GitHub

### 1.1 新建 GitHub 仓库

1. 打开 https://github.com/new
2. Repository name（仓库名）：比如 `mahjong-workbench`（英文好记）
3. 选 **Public**（公开）——免费版 Netlify 可以自动部署 Public 仓库
4. 不要勾 "Add a README" / ".gitignore" / "License"（空仓库就行）
5. 点 **Create repository**

### 1.2 上传文件夹

**最简单的方式：网页直接拖拽上传**

1. 新建好仓库后，页面会显示 "Quick setup"，下面有一行：**"…or upload files"**，点它
2. 把你本地 `麻将消一消_在线/` 文件夹里的**所有内容**（不是文件夹本身，是里面所有文件和子文件夹）全选拖进去
   - `index.html`
   - `demo/`
   - `editor/`
   - `docs/`
   - `netlify/` ← 这个最重要，是后端函数
3. 底部点 **Commit changes**

等 10 秒左右，刷新仓库主页，能看到所有文件就 OK。

---

## 第二步：Netlify 绑定这个 GitHub 仓库

1. 登录 Netlify，进入你现有的项目（`clever-speculoos-4c1cb2` 那个）
2. 左侧栏点 **「Deploys 部署」**
3. 顶部找 **「Site settings 网站设置」** 或者「Deploy settings」
4. 找 **「Build & deploy」→「Continuous deployment」→「Link repository」** 或「Import from Git」
5. 选 **GitHub**
6. 授权 Netlify 访问你的 GitHub 仓库（只授权你刚建的那个就行）
7. 选中你的 `mahjong-workbench` 仓库
8. 配置 Build settings：
   - **Base directory**：留空（或者 `/`）
   - **Build command**：留空（我们不需要构建）
   - **Publish directory**：留空（或者 `.` 或 `/`）
9. 点 **Deploy site**

等它部署完（1-2 分钟），你的域名就指向新版了。

---

## 第三步：设置共享口令（环境变量）

1. Netlify 项目页面 → 左侧栏 **「Site settings」→「Environment variables」**
2. 点 **「Add a variable」**
3. **Key**：`WORKBENCH_PASSWORD`
4. **Value**：你和另一个策划商量好的口令（比如 `mahjong2026`，别太简单）
5. 保存

设置完**需要重新触发一次部署**才能生效：
- 左侧「Deploys」→ 右上角「Trigger deploy」→ 「Deploy site」
- 等 1 分钟完成

---

## 第四步：验证

1. 打开你的网站 → `/editor/`
2. 左侧找到 **「关卡工作台（共享）」**
3. 口令框里输入你刚才设的口令
4. 点「连接」→ 显示 **「已连接 · 0 关」** → 成功！

试一下完整流程：
1. 画形状 → 点「生成关卡」→ 预览里能玩
2. 工作台里填一个**关卡名**（比如「关卡 1」）→ 点「保存当前」
3. 刷新页面 → 重新连接口令 → 列表里应该有你刚存的关卡
4. 点「加载到预览」→ demo 里加载成功 → 成功！

---

## 常见问题

**Q: 口令忘了/要改怎么办？**
A: Netlify → Site settings → Environment variables → 找到 `WORKBENCH_PASSWORD` → 编辑 → 重新部署。

**Q: 另一个策划怎么使用？**
A: 给他网站链接 + 共享口令。他打开编辑器 → 输口令 → 连接 → 就能看到所有关卡、存新关卡。

**Q: 数据会丢吗？**
A: Netlify Blobs 是正规存储服务，但**免费额度不保证 SLA**。重要的关卡本地也留一份 JSON 备份最稳妥。

**Q: 能删关卡吗？**
A: 每个关卡卡片有「删」按钮，需要口令（已连接状态下）。

**Q: 免费额度够吗？**
A: 完全够。Netlify Functions 免费额度是每月 12.5 万次调用，Blobs 是 1 GB 存储 + 100 GB 流量。两个人做关卡用到天荒地老都不会碰。

---

## 文件结构确认

部署前确认 `麻将消一消_在线/` 里有这些：

```
麻将消一消_在线/
├── index.html
├── demo/
│   ├── index.html
│   └── 示例关卡/
├── editor/
│   ├── index.html
│   └── generator.js
├── docs/
│   ├── 使用说明.md
│   ├── 难度分说明.md
│   └── 游戏Demo说明.md
└── netlify/
    └── functions/
        └── levels.js      ← 后端函数，Netlify 自动识别
```

Netlify 看到 `netlify/functions/` 目录会自动部署成 Serverless Functions，路径是 `/.netlify/functions/levels`。

---

## 附 · Cloudflare Pages 迁移（2026-08-17 起主用）

工作台从 Netlify 迁到 Cloudflare Pages（Netlify 免费构建额度耗尽，重启要等整月）。**数据不变**——关卡仍存在本仓库 `data/levels/`，只是"服务器"换了家。

**结构与 Netlify 版对照：**

| 层 | Netlify 版 | Cloudflare 版 |
|---|---|---|
| 静态网页 | `publish = "."` | Pages 项目 output = `.` |
| 后端函数 | `netlify/functions/levels.js` | `functions/levels/[[path]].js`（Cloudflare Pages Functions） |
| API 路径 | `/.netlify/functions/levels` | `/levels` |
| 环境变量 | Netlify 站点环境变量 | Cloudflare 项目环境变量 |

**部署步骤：**

1. Cloudflare 控制台 → Workers & Pages → **Pages → Create project → Connect to Git** → 选 `zhangjiwei0221/mahjong`
2. Build command：**留空**（纯静态站）；Build output directory：**`.`**
3. 保存并部署前，先配环境变量（Settings → Environment variables）：

| 变量名 | 值 | 建议 |
|---|---|---|
| `WORKBENCH_PASSWORD` | 与 Netlify 里那个相同 | 从 Netlify 站点环境变量里复制过来 |
| `GITHUB_TOKEN` | 与 Netlify 里那个相同（或新生成 PAT） | 设为 **Secret**（加密） |
| `GITHUB_REPO` | `zhangjiwei0221/mahjong` | |
| `GITHUB_BRANCH` | `main` | |

4. 部署完成后，用「📋 工作台」页**试存一关**验证 API 通不通（口令 + 保存）。

**注意：**

- 前端 API 路径已全局改为 `/levels`（Cloudflare 的路径），旧 Netlify 部署冻结在旧代码、不影响；若哪天想回 Netlify，把两处 `'/levels'` 改回 `'/.netlify/functions/levels'` 即可（前端文件和函数都在仓库里，都能找回）。
- Netlify 的 `netlify/functions/levels.js` 保留未删，作为回退。
- Cloudflare Pages 免费额度宽松（函数 10 万次调用/天），正常使用远用不完；但和 Netlify 一样，**尽量攒批推代码、减少部署次数**。
